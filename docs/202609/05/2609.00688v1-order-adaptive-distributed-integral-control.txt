Title: Order-Adaptive Distributed Integral Control

URL Source: https://arxiv.org/pdf/2609.00688v1

Published Time: Wed, 02 Sep 2026 00:41:53 GMT

Number of Pages: 16

Markdown Content:
> 1

# Order-Adaptive Distributed Integral Control 

Fei Chen, Senior Member, IEEE 

Abstract —We address a structural tradeoff in distributed dynamic coordination: when the target complexity is unknown, a low controller order saves states but may leave a persistent tracking error, whereas a high order improves tracking but may burden every agent with unnecessary dynamics. To remove this choice without resorting to computationally more involved nonlinear feedback or chattering-prone nonsmooth feedback, we develop an order-adaptive distributed integral controller (OADIC). Specifically, we start with proportional feedback and add integral states only when locally measurable relative errors show that the current order is inadequate. Meanwhile, we organize the candidate controllers in a nested form, thereby pre-serving the existing states and gains and maintaining continuous control inputs during order transitions. To provide a theoretical basis for this design, we first characterize the consistency of prescribed relative displacements on the augmented agent–target graph. Next, we construct gains that stabilize all admissible fixed-order subsystems and establish a uniform input-to-state stability bound for the variable-dimension closed loop. Furthermore, we prove that OADIC rejects every insufficient order after finitely many decision intervals and explicitly bound the rejection time of the critical order. 

Index Terms —Distributed dynamic coordination, order-adaptive control, distributed integral control, internal model, multi-agent system. 

I. I NTRODUCTION 

How much internal dynamics should a distributed controller carry? The internal model principle offers a partial answer: exact regulation requires the controller to reproduce the dy-namics that generate the signal to be tracked or rejected [1]. Yet the principle does not specify how to choose the internal-model order when the complexity of that signal is unknown. This uncertainty therefore creates a structural tradeoff: an order chosen too low leaves unmodeled dynamics, whereas the highest anticipated order forces every agent to carry un-necessary integral states whenever the actual motion is simpler. This tradeoff is particularly restrictive in large-scale distributed networks because the controller dynamics are replicated at every node: even one superfluous integral state per agent creates network-wide storage, computation, and initialization overheads that grow with the number of agents, whereas underestimating the order leaves a persistent coordination error throughout the network. Moreover, because each agent has access only to local relative information, distinguishing an inadequate internal model from a slowly decaying network transient is itself highly nontrivial. The scalability issue mentioned above has motivated exten-sive efforts to reduce the global information and model knowl-edge required by distributed controllers. A common approach        

> Fei Chen is with the College of Artificial Intelligence, Nankai University, Tianjin 300350, China. Correspondence should be addressed to Fei Chen (e-mail: fchen@nankai.edu.cn).

nevertheless assigns each agent a copy or an estimate of a prescribed reference-signal model. This idea provides the basis for output synchronization conditions [2] and for distributed observer–regulator architectures that coordinate heterogeneous agents [3]. More recently, fully distributed adaptive protocols have removed the need for global network information while retaining consensus guarantees [4]. Such advances make dis-tributed coordination more scalable and less dependent on global network information. However, they still specify the reference-signal model and its dimension before operation. Rather than reconstructing an explicit reference-signal model, another line of research embeds the target dynamics through integral or filter states in the relative-feedback loop. Within this framework, distributed integral action improves regulation and disturbance rejection [5], whereas dynamic average consensus and the closely related distributed average tracking methods enable networks to follow time-varying signals [6, 7]. This interpretation is especially transparent for polynomial targets: each additional integrator incorporates one more constant derivative into the controller and thereby leads naturally to fixed PI n-type tracking schemes [8]. Alongside these dynamic compensation methods, recent adaptive and data-driven approaches have further reduced the need for ac-curate agent models. In particular, output-feedback adaptation estimates unknown agent parameters online [9], whereas data-driven synchronization constructs controllers directly from measured trajectories [10]. Although these approaches re-duce prior model knowledge in different ways, they retain a controller architecture whose state dimension is prescribed in advance. Consequently, none of them determines during operation whether the observed tracking performance calls for an additional integral state. In parallel, nonlinear and nonsmooth control methods seek stronger transient guarantees by reshaping the feedback law. Fixed-time control, for example, provides a settling-time bound independent of the initial condition [11], and its dis-tributed extensions carry this property over to networks of in-tegrators [12, 13]. Building on this foundation, recent designs combine distributed observers with sliding-mode feedback to achieve fixed-time formation tracking [14], while nonlinear output-feedback schemes enforce prescribed tracking perfor-mance for uncertain high-order networks [15]. These stronger performance guarantees, however, come with a more involved controller structure. In particular, nonlinear gains, fractional-power or sliding-mode terms, and additional observer states must often be implemented at every agent, so their aggregate cost increases with the network size. Moreover, nonsmooth terms may cause chattering and require special solution defini-tions. Consequently, this line of research improves tracking by increasing the complexity of the feedback law, but it still leaves the internal-model dimension as an a priori design choice. arXiv:2609.00688v1 [eess.SY] 1 Sep 2026 2

The above results leave a clear gap between low- and high-order linear controllers. A low-order controller is inexpensive but may leave a nonzero tracking error, whereas a high-order controller can track richer motions but carries more states at every agent. A fixed-order design must choose one of these two outcomes before operation and cannot move from one to the other when the target motion changes. As a related alternative, nonlinear or nonsmooth feedback can improve transient performance or robustness, but it generally retains an a priori controller dimension and introduces additional implementation complexity. Thus, the methods reviewed above do not provide online order adjustment within a purely linear distributed architecture. To close this gap, we develop an order-adaptive distributed integral controller (OADIC) for multi-agent networks with consistent relative-displacement constraints. Rather than se-lecting a high fixed order in advance or introducing nonlinear feedback, OADIC begins with the simplest linear controller and increases its order only when the observed tracking progress is insufficient. Importantly, at every active order, the control input remains a purely linear combination of the coordination error and a nested sequence of integral states. To determine whether this order remains adequate, the agents then construct a distributed monitor from their local relative errors. Accordingly, at each decision instant, OADIC retains the current order if the monitor decreases sufficiently; otherwise, it activates one additional integral state. The main contributions are summarized as follows. 1) We develop OADIC as a linear distributed control frame-work that achieves dynamic coordination while adapting controller complexity to the observed tracking perfor-mance. To determine whether more controller states are needed, OADIC constructs a distributed monitor from local relative errors. If the monitor indicates that the cur-rent order is inadequate, OADIC activates one additional integral state. Because the controllers are nested, this activation preserves the existing states and gains while maintaining continuity of the control input. 2) We give a constructive nested gain condition that simul-taneously stabilizes every admissible fixed-order subsys-tem. By accounting for both continuous target forcing and the reset maps, we establish a uniform input-to-state stability bound for the complete variable-dimension dy-namics. We also derive convergence estimates connecting the threshold schedule with the tracking performance of the final unsaturated order. 3) We prove that every insufficient order is rejected after finitely many decision intervals and obtain an explicit rejection-time bound for the critical order. This explains why OADIC can identify missing integral action even when the active fixed-order subsystem is itself stable. The remainder of the paper is organized as follows. Sec-tion II introduces the notation and auxiliary results. Section III formulates the dynamic coordination problem. Section IV characterizes consistency of the prescribed relative displace-ments. Section V presents OADIC, its distributed monitor, the switching rule, and the nested gain design. Section VI estab-lishes stability and tracking properties, and Section VII ana-lyzes finite-time detection of insufficient orders. Section VIII provides a numerical comparison with fixed-order controllers, and Section IX concludes the paper. II. P RELIMINARIES 

A. Notation 

We use R and Z+ to denote the sets of real numbers and positive integers, respectively, and we write Rn and Rn×m for the spaces of real n-vectors and n × m matrices. We use the superscript T for transposition. For a vector x, we use ∥x∥ for the Euclidean norm; for a matrix M, we use the same notation for the induced Euclidean norm. We use 1n, 0n, In, and 0 n×m

for the all-ones vector, the zero vector, the identity matrix, and the zero matrix of the indicated dimensions. We omit a subscript when its dimension is clear from the context. For a switching instant ts and a signal ξ (t) with one-sided limits, we write ξ (t− 

> s

) ≜ lim t↑ts ξ (t) and ξ (t+ 

> s

) ≜ lim t↓ts ξ (t).For scalars a1, . . . , an, we use diag (a1, . . . , an) for the diago-nal matrix with these entries and ⊗ for the Kronecker product. We define [a]+ ≜ max {a, 0} and use ⌈a⌉ for the smallest integer not less than a. For a sufficiently differentiable signal 

x(t), we write x(r)(t) for its rth derivative and set ˙ x = x(1).For a real symmetric matrix M ∈ Rn×n, we order its eigen-values as λ1(M) ≤ · · · ≤ λn(M) and denote the corresponding orthonormal eigenvectors by z1(M), . . . , zn(M).We use the standard Landau notation for scalar- or vector-valued functions. Suppose that g(t)̸ = 0 sufficiently close to a specified limit point. As t approaches that point, we write f (t) = O(g(t)) if there exist a constant C > 0 and a neighborhood of the limit point such that ∥ f (t)∥ ≤ C|g(t)|.We write f (t) = o(g(t)) if ∥ f (t)∥/|g(t)| → 0. Consequently, 

f (t) = o(g(t)) implies f (t) = O(g(t)) . We state the relevant limit, such as t → ∞ or t → 0, explicitly unless it is clear from the context. 

B. Auxiliary lemmas 

Lemma 1 ([16]) . Let M ∈ Rn×n be symmetric. Then, for every x ∈ Rn,

λ1(M)∥x∥2 ≤ xT Mx ≤ λn(M)∥x∥2.

Moreover, for i = 2, . . . , n, 

λi(M) = min   

> x̸=0,x⊥z1(M),..., zi−1(M)

xT Mx xT x .

Lemma 2 ([17]) . Let P p(θ ) ≜ ∑pl=0 al θ l , where p ≥ 2 and al > 0 for l = 0, . . . , p. If a2 

> l

− 4al−1al+1 > 0, l = 1, . . . , p − 1, (1) 

then all roots of P p are real and distinct. 3

III. P ROBLEM FORMULATION 

We consider a network of n ∈ Z+ agents governed by the single-integrator dynamics ˙xi(t) = ui(t), i = 1, . . . , n, (2) where xi(t) ∈ R and ui(t) ∈ R represent the state and control input of agent i, respectively. Equivalently, we write (2) in vector form as ˙x(t) = u(t),

where x(t) ≜ [x1(t), · · · , xn(t)] T and u(t) ≜ [u1(t), · · · , un(t)] T .The single-integrator model (2) isolates the role of controller order from additional agent dynamics, thereby allowing us to focus on how much integral action is required for agents. Likewise, we use scalar states in (2) only for notational sim-plicity; the same formulation extends to higher-dimensional states through Kronecker products. We model communication among the agents using an undi-rected graph G = ( V, E), where V = {1, . . . , n} denotes the set of agents and E ⊆ {i, j} : i, j ∈ V, i̸ = j denotes the set of communication links. We define the adjacency matrix 

A = [ ai j ] ∈ Rn×n by setting ai j = 1 if {i, j} ∈ E and ai j = 0otherwise. Because G is undirected, ai j = a ji . For each agent i,we define its neighbor set as Ni = { j ∈ V : ai j = 1} and its degree as di = ∑nj=1 ai j . Using these degrees, we construct the degree matrix D and the graph Laplacian L as 

D = diag (d1, . . . , dn), L = D − A.

Let x0(t) ∈ R denote the state of a dynamic target whose trajectory specifies the common time-varying motion of the desired multi-agent configuration. To indicate whether agent i

has access to the target state, we define ai0 ∈ { 0, 1} such that 

ai0 = 1 if agent i can access x0(t) and ai0 = 0 otherwise. We collect these access indicators in the diagonal matrix 

A0 = diag (a10 , . . . , an0).

We impose the following standard connectivity and target-access assumption. 

Assumption 1. The graph G is connected, and at least one agent has access to the target; equivalently, ∑ni=1 ai0 > 0.

Under Assumption 1, L + A0 is symmetric and positive defi-nite [18]. Throughout the remainder of this paper, eigenvalues written without an explicit matrix argument refer to those of 

L + A0; namely, 

λi ≜ λi(L + A0), 0 < λ1 ≤ · · · ≤ λn.

For each communication edge {i, j} ∈ E, we prescribe ¯ζi j as the desired value of the relative displacement xi(t) − x j(t) and impose ¯ζ ji = − ¯ζi j so that the requirement remains independent of the ordering of the two agents. For each agent i with ai0 = 1, we similarly prescribe ¯ζi0 as its desired displacement from the target. To represent both types of requirements in a unified form, we treat the target as node 0 and define the augmented graph 

Gaugm = ( Vaugm , Eaugm ),

Vaugm = {0} ∪ V,

Eaugm = E ∪ {i, 0} : i ∈ V, ai0 = 1 .

Thus, for every edge {i, j} ∈ Eaugm , the constant ¯ζi j specifies the desired value of xi(t) − x j(t).We now formulate the problem of achieving dynamic coor-dination through linear distributed control. 

Problem 1. Given the agent dynamics (2) and a general smooth target x 0(t), design linear distributed control laws ui(t), i = 1, . . . , n, using only x i(t) − x j(t) and ¯ζi j for j ∈ Ni,together with x i(t) − x0(t) and ¯ζi0 when a i0 = 1, such that the resulting closed-loop system satisfies 

lim  

> t→∞

xi(t) − x j(t) − ¯ζi j = 0, {i, j} ∈ Eaugm .

Problem 1 provides a unified formulation of several clas-sical multi-agent coordination problems. If ¯ζi j = 0 for every 

{i, j} ∈ Eaugm , all agents asymptotically track the target, and the problem reduces to leader-following consensus [19, 20]. If the desired displacements specify a nontrivial configuration, the problem reduces to formation control for a stationary target or formation tracking for a moving target [21]. In the higher-dimensional extension, if the target-relative displace-ments place the agents around the target and the agent– agent displacements specify the corresponding surrounding geometry, the problem becomes a target-surrounding control problem [22]. The next section characterizes when the desired displacements define a consistent coordination pattern. IV. C ONSISTENCY 

Arbitrary assignments of the desired relative displacements may be mutually inconsistent, rendering Problem 1 infeasible. The following example illustrates this issue. 

Example 1. Consider the augmented network of four agents shown in Fig. 1. The solid nodes represent the agents, and   

> 1234
> x0(t)
> ¯ζ12
> ¯ζ14
> ¯ζ32
> ¯ζ34
> ¯ζ31
> ¯ζ10
> Fig. 1. An inconsistent set of desired relative displacements.

the dashed node represents the dynamic target. The arrows specify the orientation used to define each desired relative displacement. Let ¯ζ12 = ¯ζ32 = 1 and ¯ζ31 = 2. The first two equalities require x 1 − x2 = x3 − x2 and hence x 3 − x1 = 0,which contradicts ¯ζ31 = 2. Therefore, these desired displace-ments are inconsistent, and Problem 1 has no solution. 4

Example 1 raises a natural question: under what conditions do the prescribed relative displacements make Problem 1 solv-able? The following lemma relates this edge-wise coordination objective to the tracking of consistent desired trajectories. 

Lemma 3. Suppose Assumption 1 holds and there exist constants ¯d0, . . . , ¯dn, with ¯d0 = 0, such that 

¯di − ¯d j = ¯ζi j , {i, j} ∈ Eaugm . (3) 

Define 

¯xi(t) ≜ x0(t) + ¯di, i = 0, 1, . . . , n. (4) 

Problem 1 is solved if and only if 

lim  

> t→∞

|xi(t) − ¯xi(t)| = 0, i = 1, . . . , n.

Proof: (Necessity) Starting from the agents that have access to the target, define the layers 

H1 ≜ {i ∈ V : ai0 > 0},

Hp ≜

(

i ∈ V \

> p−1[
> q=1

Hq

!

: ∃ j ∈ Hp−1 such that ai j > 0

)

,

(5) for p = 2, . . . , h, where h denotes the number of nonempty layers. Assumption 1 ensures that these layers partition the agent set, i.e., Shp=1 Hp = V and Hp ∩ Hq = /0 for p̸ = q.We now proceed by induction over the layers. For any i ∈

H1, the coordination objective on the edge {i, 0} yields 

xi(t) − x0(t) → ¯ζi0. (6) By (3), ¯di = ¯ζi0 because ¯d0 = 0. Therefore, 

xi(t) → x0(t) + ¯ζi0 = x0(t) + ¯di = ¯xi(t), (7) which establishes the result for the first layer. Suppose the result holds for every agent in Hp, namely, 

xi(t) → ¯xi(t), i ∈ Hp. (8) For any i ∈ Hp+1, the definition of the layers guarantees the existence of a neighbor j ∈ Hp ∩ Ni. The coordination objective on the edge {i, j} then gives 

xi(t) − x j(t) − ¯ζi j → 0. (9) By the induction hypothesis, 

x j(t) → ¯x j(t) = x0(t) + ¯d j. (10) Combining (10) and (9) yields 

xi(t) → x0(t) + ¯d j + ¯ζi j .

Since (3) implies ¯di = ¯d j + ¯ζi j , we obtain xi(t) → ¯xi(t). This completes the induction and proves necessity. 

(Sufficiency) Suppose xi(t) − ¯xi(t) → 0 for all i = 1, . . . , n.Moreover, x0(t) − ¯x0(t) ≡ 0 by (4). Hence, for every {i, j} ∈ 

Eaugm ,

xi(t) − x j(t) − ¯ζi j = [ xi(t) − ¯xi(t)] − [x j(t) − ¯x j(t)] + ¯di − ¯d j − ¯ζi j → 0,

where the last equality follows from (3). Therefore, the co-ordination objective in Problem 1 holds on every augmented edge, proving sufficiency. Lemma 3 shows that solving Problem 1 requires each agent trajectory xi(t) to converge to the desired trajectory ¯ xi(t) = 

x0(t) + ¯di. The constants ¯di must reproduce all prescribed relative displacements through (3). This requirement motivates the following definition. 

Definition 1 (Consistency) . The desired relative displacements in Problem 1 are consistent if there exist constants ¯d0, . . . , ¯dn,with ¯d0 = 0, that satisfy (3) .

We next characterize consistency using the incidence matrix. Arbitrarily orient and index the edges of Gaugm , and let Eaugm ∈

R(n+1)×| Eaugm | be the corresponding incidence matrix. For an edge oriented from node i to node j, its column has 1 in row 

i, −1 in row j, and zeros elsewhere. Let Eaugm  

> x

∈ Rn×| Eaugm | be obtained by deleting the target-node row, and let ¯ζ collect the desired displacements in the same edge order, with component ¯ζi j for an edge oriented from i to j.For the augmented graph in Fig. 1, use the node order (0, 1, 2, 3, 4) and the oriented-edge order 

(1, 2), (1, 4), (3, 2), (3, 4), (3, 1), (1, 0). Then, 

Eaugm =



0 0 0 0 0 −11 1 0 0 −1 1

−1 0 −1 0 0 00 0 1 1 1 00 −1 0 −1 0 0

 , ¯ζ =



¯ζ12 

¯ζ14 

¯ζ32 

¯ζ34 

¯ζ31 

¯ζ10 



.

Deleting the first row gives 

Eaugm  

> x

=



1 1 0 0 −1 1

−1 0 −1 0 0 00 0 1 1 1 00 −1 0 −1 0 0

 .

The following theorem provides an algebraic characteriza-tion of consistency. 

Theorem 1. The desired relative displacements are consistent if and only if 

¯ζ ∈ range  (Eaugm  

> x

)T .

Proof: Let ¯d = [ ¯d1, . . . , ¯dn]T . Stacking (3) in the chosen edge order and using ¯d0 = 0 yields 

(Eaugm  

> x

)T ¯d = ¯ζ . (11) This equation is solvable if and only if ¯ζ ∈ range (( Eaugm  

> x

)T ).The condition in Theorem 1 has a direct graph-theoretic interpretation: the signed sum of the desired displacements along every cycle of Gaugm must be zero. For example, the cycle formed by agents 1, 2, and 3 in Fig. 1 requires ¯ζ12 − ¯ζ32 + ¯ζ31 = 0, which is violated by the values in Ex-ample 1. Moreover, under Assumption 1, Eaugm  

> x

has full row rank; hence, whenever the displacements are consistent, the offset vector ¯d satisfying (11) is unique. 5

V. A LGORITHM 

Building on the coordination objective established above, we now develop OADIC and its distributed performance monitor. OADIC starts at the lowest controller order and activates an additional integral state whenever the regulation error fails to decrease by a prescribed percentage over a decision interval. Thus, order adequacy is assessed from the relative improvement achieved by the current controller, rather than from the absolute magnitude of the error. This process continues until the controller reaches a specified maximum order. Suppose that the desired displacements in Problem 1 are consistent. We define the desired network trajectory and the corresponding tracking error as ¯x(t) ≜ 1x0(t) + ¯d, e(t) ≜ x(t) − ¯x(t). (12) Here ¯d = [ ¯d1, . . . , ¯dn]T denotes the offset vector that satis-fies (3). The stacked proportional error is then given by 

σ0(t) ≜ (L + A0)e(t). (13) Define Q ≜ {1, . . . , qmax }, where the prescribed maximum order satisfies qmax ≥ 2. This lower bound ensures that at least one order increase is possible: q = 1 corresponds to proportional feedback, whereas q = 2 activates the first integral state. For each q ∈ Q, the order-q controller contains q − 1integral states, and agent i implements 

ui(t) = −

> q−1

# ∑

> l=0

κl σil (t), (14) ˙σil (t) = σi,l−1(t), l = 1, . . . , q − 1, (15) where κl > 0 and 

σi0(t) = ∑

> j∈Ni

xi(t) − x j(t) − ¯ζi j 

 + ai0

xi(t) − x0(t) − ¯ζi0

.

(16) Agent i can compute every term in (16) locally using relative measurements from its neighbors and, when ai0 = 1, its relative measurement with respect to the target. To assess the regulation performance locally, agent i com-putes 

ρ2 

> i

(t) ≜ 12 ∑

> j∈Ni

xi(t) − x j(t) − ¯ζi j 

2 + ai0

xi(t) − x0(t) − ¯ζi0

2.

(17) The factor 1 /2 compensates for double counting of the agent– agent terms. Indeed, summing (17) over all agents counts the squared error for each undirected edge {i, j} once through 

ρ2 

> i

(t) and once through ρ2 

> j

(t). The factor 1 /2 therefore gives this error a total coefficient of one. By contrast, only the agent with access to the corresponding target measurement contributes each target-related term. We define the network-wide regulation indicator as 

J(t) ≜ max  

> i=1,..., n

ρi(t). (18) Let 0 = t0 < t1 < t2 < · · · denote the decision instants, with 

tk → ∞ and tk+1 −tk ≥ Td > 0, where Td specifies the minimum interval between consecutive order decisions. To quantify the improvement achieved during [tk,tk+1], the agents evaluate the network-wide indicator at the beginning and end of the interval. Define the relative error reduction over this interval as 

rk ≜



J(t+ 

> k

) − J(t−

> k+1

)

J(t+ 

> k

) , J(t+ 

> k

) > 0,

1, J(t+ 

> k

) = J(t−

> k+1

) = 0,

−∞, J(t+ 

> k

) = 0 < J(t−

> k+1

).

(19) Thus, 100 rk% is the percentage by which the monitored error decreases during the interval. In particular, rk < 0 indicates error growth, while the convention rk = 1 at zero error prevents an unnecessary order increase when exact regulation has already been achieved. 

Remark 1. The endpoint values J (t+ 

> k

) and J (t−

> k+1

) can be computed in a fully distributed manner. At each endpoint, agent i initializes a finite-time max-consensus algorithm with its local value ρi(t+ 

> k

) or ρi(t−

> k+1

) and exchanges only its current estimate with neighboring agents. Because G is connected by Assumption 1, all agents obtain the same network-wide maximum after the max-consensus procedure terminates [23]. 

The following lemma establishes that the network-wide indicator J(t), although constructed from the local quantities 

ρi(t), provides an equivalent measure of the global tracking error. 

Lemma 4. Under Assumption 1, 

> n

# ∑

> i=1

ρ2 

> i

(t) = eT (t)( L + A0)e(t),

r λ1

n ∥e(t)∥ ≤ J(t) ≤ pλn∥e(t)∥.

Proof: By (12), xi(t) = ei(t) + x0(t) + ¯di. Moreover, the consistency condition together with (3) implies ¯di − ¯d j = ¯ζi j 

for every agent edge and ¯ di = ¯ζi0 whenever ai0 > 0. Therefore, 

xi(t) − x j(t) − ¯ζi j = ei(t) − e j(t) + ¯di − ¯d j − ¯ζi j 

= ei(t) − e j(t),

xi(t) − x0(t) − ¯ζi0 = ei(t) + ¯di − ¯ζi0 = ei(t).

Substituting these identities into (17) and summing over all agents yields 

> n

# ∑

> i=1

ρ2 

> i

(t) = 12

> n

# ∑

> i=1

# ∑

> j∈Ni

[ei(t) − e j(t)] 2 + eT (t)A0e(t)= eT (t)( L + A0)e(t).

This proves the first equality in Lemma 4. Since J(t) = max i ρi(t), we also have 

J2(t) ≤

> n

# ∑

> i=1

ρ2 

> i

(t) ≤ nJ 2(t).

Finally, applying Lemma 1 to L + A0 gives 

λ1∥e(t)∥2 ≤ eT (t)( L + A0)e(t) ≤ λn∥e(t)∥2.6

Using the established identity in the preceding two inequalities yields λ1∥e(t)∥2 ≤ nJ 2(t) and J2(t) ≤ λn∥e(t)∥2. Taking square roots gives pλ1/n∥e(t)∥ ≤ J(t) ≤ √λn∥e(t)∥, as stated in Lemma 4. 

Remark 2. Lemma 4 shows that J(t) is a distributedly computable measure equivalent to the global tracking error 

∥e(t)∥. In particular, J (t) = 0 if and only if e (t) = 0. Therefore, the controller order can be selected using the local regulation quantities ρi(t) and a network-wide maximum operation with-out explicitly reconstructing the global error vector e (t).

Let qk ≜ q(t+ 

> k

) denote the controller order applied over 

[tk,tk+1). To determine whether this order remains adequate, choose ε0 > 0, ε∞ ≥ 0, and α > 0 such that ε0 + ε∞ < 1, and define the required relative reduction sequence 

εk ≜ ε∞ + ε0

(k + 1)α . (20) Here, 100 εk% is the minimum percentage reduction expected over the kth decision interval. The parameter ε∞ specifies the long-run minimum improvement, ε0 adds a stricter ini-tial requirement, and α controls how quickly this additional requirement decays. Starting from q0 = 1, update the common controller order at tk+1 according to 

qk+1 =

(

qk + 1, if rk ≤ εk and qk < qmax ,

qk, otherwise , (21) Under (21), a relative reduction no greater than εk indicates that the current internal-model order has not improved the tracking error sufficiently during the decision interval. OADIC therefore activates one additional integral state, provided that 

qk < qmax . If the achieved reduction is larger than the required percentage, or if the maximum order has already been reached, the order remains unchanged. We specify the controller-state transfer accompanying an order increase as follows. At a switching instant ts from order 

q to order q + 1, retain all existing integral states and initialize only the newly activated state: 

σl (t+ 

> s

) = σl (t− 

> s

), l = 1, . . . , q − 1,

σq(t+ 

> s

) = 0, ˙σq(t) = σq−1(t). (22) Because both x(t) and x0(t) are continuous at ts, the tracking error also remains continuous, i.e., e(t+ 

> s

) = e(t− 

> s

). It then follows from (13) that σ0(t+ 

> s

) = σ0(t− 

> s

). Combining this iden-tity with the reset rule (22), the order-(q + 1) control input immediately after the switch is 

u(t+ 

> s

) = −

> q

# ∑

> l=0

κl σl (t+ 

> s

)= −

> q−1

# ∑

> l=0

κl σl (t− 

> s

) − κqσq(t+ 

> s

)= −

> q−1

# ∑

> l=0

κl σl (t− 

> s

) = u(t− 

> s

). (23) Thus, u(t+ 

> s

) = u(t− 

> s

), and the proposed state-transfer rule yields a bumpless change in controller order. 

Remark 3. OADIC, consisting of the controller (14) –(15) , the local monitor (17) –(19) , the switching law (21) , and the reset 

(22) , remains distributed. Agent i computes ρi(t) from local relative measurements and records its endpoint values over 

[tk,tk+1], while J (t+ 

> k

) and J (t−

> k+1

) are obtained distributively as described in Remark 1. Each agent then evaluates the same relative reduction r k and applies the same order update. 

It remains to choose the controller gains κl . Because the controller order is adjusted online, designing an unrelated set of gains for each admissible order would require gain replacement at every switch and would complicate the stability analysis. We therefore use a single nested gain sequence, so that an order increase only activates the next gain while leaving all previously used gains unchanged. The sequence is selected constructively to provide a common sufficient stability condition for every fixed-order subsystem. Specifically, choose 

κ0 > 0, 0 < κ1 < λ1

4 κ20 ,

0 < κl+1 < κ2

> l

4κl−1

, l = 1, . . . , qmax − 2, if qmax ≥ 3. (24) Thus, the order-q controller uses the prefix κ0, . . . , κq−1 of the same sequence. When qmax = 2, only the first line of (24) is needed. When qmax ≥ 3, the last recursion, obtained by setting l = qmax − 2, determines the admissible range of 

κqmax −1. Distributed Laplacian-spectrum estimation algorithms allow agents to estimate λ1 through neighbor-to-neighbor ex-changes [24]. Alternatively, any certified lower bound λ 1 ≤ λ1

can replace λ1 in (24), yielding a more conservative but still sufficient gain selection. For the analysis, let 

η(t) ≜ eT (t) · · · [e(q(t)−1)(t)] T T ,

w(t) ≜

h

˙x0(t) · · · x(qmax ) 

> 0

(t)

iT

,

and define 

A1 ≜ −κ0(L + A0), B1 ≜ −1,

Aq ≜

 0n(q−1)×n In(q−1)

−κq−1(L + A0) − κq−2 · · · κ0

 ⊗ (L + A0)



,

q = 2, . . . , qmax ,

Bq ≜ 0 0 · · · 0 −1T T , q = 2, . . . , qmax .

Between two consecutive switching instants, q(t) = q is con-stant. Stacking (14), and using (12) and ˙ ¯x(t) = 1 ˙x0(t), gives ˙e(t) = −

> q−1

# ∑

> l=0

κl σl (t) − 1 ˙x0(t). (25) Differentiating (25) q − 1 times gives 

e(q)(t) = −

> q−1

# ∑

> l=0

κl σ (q−1) 

> l

(t) − 1x(q) 

> 0

(t). (26) Equation (13) and repeated use of (15) give 

σ (q−1) 

> 0

(t) = ( L + A0)e(q−1)(t),

σ (q−1) 

> l

(t) = σ (q−1−l) 

> 0

(t) = ( L + A0)e(q−1−l)(t),

l = 1, . . . , q − 1.

(27) 7

Substituting (27) into (26) gives 

e(q)(t) + 

> q−1

# ∑

> l=0

κl (L + A0)e(q−1−l)(t) = −1x(q) 

> 0

(t). (28) By the definition of η,˙η(t) = [e(1)(t)] T · · · [e(q−1)(t)] T [e(q)(t)] T T .

The first q − 1 block rows of Aqη(t) reproduce 

e(1)(t), . . . , e(q−1)(t), while its last block row is the left-hand side of (28) solved for e(q)(t). The input term in that last block row is −1x(q) 

> 0

(t) = Bqx(q) 

> 0

(t). Hence ˙η(t) = Aqη(t) + Bqx(q) 

> 0

(t). (29) The next lemma establishes a linear reset map that uniquely determines the post-switch derivative-state vector from its pre-switch value and the target derivatives when the controller order increases from q to q + 1. 

Lemma 5. For each q = 1, . . . , qmax − 1, there exist matrices 

Rq ∈ Rn(q+1)×nq and Sq ∈ Rn(q+1)×qmax such that, whenever the controller order increases from q to q + 1 at t s,

η(t+ 

> s

) = Rqη(t− 

> s

) + Sqw(ts).

Proof: The proof proceeds in three steps. First, we recover the pre-switch integral states from η(t− 

> s

) and w(ts). From Equations (13)–(15) and ˙ ¯x(t) = 1 ˙x0(t), we have ˙e(t) = −κ0(L + A0)e(t) −

> q−1

# ∑

> l=1

κl σl (t) − 1 ˙x0(t).

Differentiating this equation j − 1 times and using ˙σl (t) = 

σl−1(t) and σ0(t) = ( L + A0)e(t) yields, for j = 1, . . . , q − 1, 

e( j)(t− 

> s

) = −

> j−1

# ∑

> l=0

κl (L + A0)e( j−1−l)(t− 

> s

)

−

> q−1

# ∑ 

> l=j

κl σl− j+1(t− 

> s

) − 1x( j) 

> 0

(ts). (30) The quantities e( j)(t− 

> s

) and x( j) 

> 0

(ts) in this identity are compo-nents of η(t− 

> s

) and w(ts), respectively. For q ≥ 2, the integral states can therefore be recovered recursively. In particular, setting j = q − 1 yields 

κq−1σ1(t− 

> s

) = −e(q−1)(t− 

> s

) −

> q−2

# ∑

> l=0

κl (L + A0)e(q−2−l)(t− 

> s

)

− 1x(q−1) 

> 0

(ts),

which uniquely determines σ1(t− 

> s

) because κq−1 > 0. Now suppose that σ1(t− 

> s

), . . . , σm−1(t− 

> s

) have already been recov-ered. Setting j = q − m in (30), the second sum becomes 

κq−1σm(t− 

> s

) + 

> q−2

# ∑

> l=q−m

κl σl−q+m+1(t− 

> s

).

All terms in this expression except σm(t− 

> s

) are known from the preceding recursion. Thus, σm(t− 

> s

) is also uniquely deter-mined. Repeating this argument for m = 2, . . . , q − 1 recovers all pre-switch integral states. If q = 1, the entire reconstruction step is omitted because the controller has no integral state before the switch. Second, we construct the post-switch derivative state. The continuity of e and the reset rule (22) imply 

e(t+ 

> s

) = e(t− 

> s

),

σl (t+ 

> s

) = σl (t− 

> s

), l = 1, . . . , q − 1,

σq(t+ 

> s

) = 0.

Replacing q with q + 1 in (30) and evaluating the resulting identity at t+ 

> s

yields, for j = 1, . . . , q,

e( j)(t+ 

> s

) = −

> j−1

# ∑

> l=0

κl (L + A0)e( j−1−l)(t+ 

> s

)

−

> q

# ∑ 

> l=j

κl σl− j+1(t+ 

> s

) − 1x( j) 

> 0

(ts). (31) Setting j = 1 in (31) gives 

e(1)(t+ 

> s

) = −κ0(L + A0)e(t+ 

> s

) −

> q

# ∑

> l=1

κl σl (t+ 

> s

) − 1 ˙x0(ts)= −κ0(L + A0)e(t− 

> s

) −

> q−1

# ∑

> l=1

κl σl (t− 

> s

) − 1 ˙x0(ts), (32) where the second equality follows from the continuity of e,the preservation of the existing integral states, and σq(t+ 

> s

) = 0. All quantities in the final expression of (32) are known from the first step and w(ts), so e(1)(t+ 

> s

) is uniquely determined. Moreover, comparison with (25) evaluated at t− 

> s

shows that 

e(1)(t+ 

> s

) = e(1)(t− 

> s

). Substituting this result into the j = 2 case of (31) determines e(2)(t+ 

> s

). Proceeding successively through 

j = q determines the entire vector η(t+ 

> s

).Finally, both reconstruction steps involve only linear op-erations. Specifically, solving (30) successively with j = q −

1, q − 2, . . . , 1 expresses each pre-switch integral state σm(t− 

> s

),

m = 1, . . . , q − 1, as a linear combination of η(t− 

> s

) and w(ts).Next, substituting these expressions and the reset relations in (22) into (31), and evaluating the latter successively for 

j = 1, . . . , q, expresses every component of η(t+ 

> s

) as a linear combination of η(t− 

> s

) and w(ts). Collecting the corresponding coefficients therefore gives matrices Rq and Sq satisfying 

η(t+ 

> s

) = Rqη(t− 

> s

) + Sqw(ts)

at every switch from order q to order q + 1, which proves the lemma. Lemma 5 shows that increasing the controller order does not introduce an independent jump variable into the derivative-state dynamics in (29). Once the pre-switch derivative state and the target derivatives at the switching instant are known, the reset rule (22) determines the complete post-switch derivative state, which admits a linear representation in terms of these quantities. Thus, although the dimension of η increases from 

nq to n(q + 1), no additional independent state is introduced at the switch. This representation connects the bumpless con-troller implementation with the switched state-space stability analysis that follows. 8

VI. S TABILITY 

This section establishes the stability of OADIC by analyzing the continuous dynamics associated with each active controller order together with the state-reset relation and the relative-reduction switching law. 

Theorem 2. Consider system (2) under OADIC, defined by the controller (14) –(15) , the monitor (17) –(19) , the switching law (21) , and the reset rule (22) . Suppose that Assumption 1 holds, Problem 1 is consistent, q max ≥ 2, x 0 ∈ Cqmax , and the gains satisfy (24) . Then, the following statements hold. (i) The state matrix Aq in (29) is Hurwitz for every q ∈ Q.(ii) The complete switched derivative-state dynamics, con-sisting of the continuous evolution (29) between switch-ing instants and the state jumps induced by the reset rule 

(22) , is uniformly input-to-state stable with state η(t) and input w (t). In particular, 

∥η(t)∥ ≤ CISS exp 



− a

2t



∥η(0)∥ + γISS sup 

> 0≤τ≤t

∥w(τ)∥,

(33) 

where CISS ≜



max  

> q∈Q

λmax (Pq)

min  

> q∈Q

λmin (Pq) μqmax −1



> 1/2

,

γISS ≜

 G

min  

> q∈Q

λmin (Pq)



> 1/2

,

Pq ≜

Z ∞

> 0

exp (ATq τ) exp (Aqτ)dτ,

a ≜ min 

> q∈Q

12λmax (Pq) ,

μ ≜ max 

> 1≤q<qmax

max 

(

1, 2λmax (RTq Pq+1Rq)

λmin (Pq)

)

,

G ≜ 2μqmax −1

a max  

> q∈Q

∥PqBq∥2

+ 2 max 

> 1≤q<qmax

P1/2

> q+1

Sq 

> 2qmax −2

# ∑

> j=0

μ j,

and Rq and Sq are the reset-map matrices in Lemma 5. (iii) The final order q ∞ ≜ lim k→∞ qk ∈ Q exists. If q ∞ < qmax ,then, for some K ∈ Z+ and all k ≥ K, 

∥e(tk)∥ ≤ 

r n

λ1

J(tk) ≤

r n

λ1

J(tK )

> k−1

# ∏

> ℓ=K

(1 − εℓ). (34) 

Hence, e (tk) → 0 if ∑∞ 

> ℓ=0

εℓ = ∞; under (20) , this holds if ε∞ > 0, or if ε∞ = 0 and 0 < α ≤ 1.If q ∞ = qmax , then 

lim sup 

> t→∞

∥e(t)∥ ≤ 

Z ∞

> 0

∥ exp (Aqmax s)Bqmax ∥ds



× lim sup 

> t→∞

|x(qmax ) 

> 0

(t)|. (35) 

For either final order, x (q∞) 

> 0

(t) → 0 implies e (t) → 0.Proof: (i) By Assumption 1, L + A0 is symmetric and positive definite. Hence, there exists an orthogonal matrix U

such that 

UT (L + A0)U = Λ ≜ diag (λ1, . . . , λn),

where 0 < λ1 ≤ · · · ≤ λn are the eigenvalues of L + A0. The Hurwitz property concerns the zero-input dynamics ˙ η = Aqη.Therefore, when analyzing the eigenvalues of Aq, we set the input x(q) 

> 0

(t) in (29) to zero. The corresponding zero-input form of (28) is 

e(q)(t) + 

> q−1

# ∑

> l=0

κl (L + A0)e(q−1−l)(t) = 0. (36) Introduce the transformed error z(t) ≜ UT e(t). Multiplying (36) by UT and using UT (L + A0)U = Λ gives 

z(q)(t) + 

> q−1

# ∑

> l=0

κl Λz(q−1−l)(t) = 0.

Because Λ is diagonal, the n coordinates of z are decoupled. In particular, the coordinate associated with the eigenvalue λi

satisfies 

z(q) 

> i

(t) + λiq−1

# ∑

> l=0

κl z(q−1−l) 

> i

(t) = 0. (37) Hence, each transformed coordinate zi(t) evolves indepen-dently of the others according to (37). To express (37) as a first-order system, define 

ξi(t) ≜

h

zi(t) ˙zi(t) · · · z(q−1) 

> i

(t)

iT

.

For q ≥ 2, equation (37) is equivalent to ˙ξi(t) = Cq,iξi(t), where 

Cq,i ≜

0(q−1)×1 Iq−1

−κq−1λi −λi

κq−2 · · · κ0



.

When q = 1, the corresponding state matrix is C1,i = −κ0λi.The transformation z(t) = UT e(t), followed by a reordering that groups the derivatives of each zi(t), transforms Aq into diag (Cq,1, . . . , Cq,n). Thus, the eigenvalues of Aq are precisely the eigenvalues of these scalar-coordinate state matrices. To calculate the eigenvalues of Cq,i, let s ∈ C and seek a nonzero solution of (37) in the form zi(t) = c exp (st ), where 

c̸ = 0. Substituting z(r) 

> i

(t) = src exp (st ) into (37) and dividing the resulting equation by the nonzero factor c exp (st ) gives 

pq,i(s) ≜ sq + κ0λisq−1 + κ1λisq−2 + · · · + κq−1λi = 0. (38) Therefore, pq,i(s) = 0 is the characteristic equation of Cq,i, and its roots are the eigenvalues of Cq,i.It remains to show that all these roots are negative. For 

q = 1, p1,i(s) = s + κ0λi, whose root is −κ0λi < 0. Now let 

q ≥ 2 and write 

pq,i(s) = 

> q

# ∑

> m=0

amsm,

aq = 1, aq−1−l = κl λi, l = 0, . . . , q − 1.9

By the ordering of the eigenvalues and the first inequality in (24), 

κ1 < λ1

4 κ20 ≤ λi

4 κ20 .

Since λi > 0, multiplying the last inequality by 4 λi yields 

κ20 λ 2 

> i

− 4κ1λi > 0.

Using aq = 1, aq−1 = κ0λi, and aq−2 = κ1λi, this inequality becomes 

a2 

> q−1

− 4aq−2aq > 0.

For l = 1, . . . , q − 2, the remaining gain inequalities give 

a2 

> q−1−l

− 4aq−2−l aq−l = λ 2 

> i

(κ2 

> l

− 4κl−1κl+1) > 0.

Therefore, a2 

> m

− 4am−1am+1 > 0 for every m = 1, . . . , q − 1. Lemma 2 therefore implies that all roots of pq,i are real. We next determine their signs. Since every coefficient am is positive, 

pq,i(0) = a0 = κq−1λi > 0,

whereas a root at zero would require pq,i(0) = 0. Therefore, zero is not a root. Next, for any s > 0, each term amsm is positive, so 

pq,i(s) = 

> q

# ∑

> m=0

amsm > 0.

A positive number s could be a root only if pq,i(s) = 0; the above inequality therefore excludes every s > 0. Hence, no root belongs to [0, ∞). Because all roots are real, every root must lie in (−∞, 0) and is therefore strictly negative. As established above, the eigenvalues of Aq are precisely the roots of pq,i over 

i = 1, . . . , n. Consequently, all eigenvalues of Aq are strictly negative. Since q ∈ Q was arbitrary, Aq is Hurwitz for every 

q ∈ Q.

(ii) Between two consecutive switching instants, the order is constant and the state equation (29) applies. Part (i) shows that Aq is Hurwitz. Hence, there exist constants Mq > 0 and 

βq > 0 such that 

∥ exp (Aqτ)∥ ≤ Mq exp (−βqτ), τ ≥ 0.

This exponential bound guarantees that the integral defining 

Pq converges and therefore that Pq is a finite symmetric matrix. Consequently, for every nonzero vector y,

yT Pqy =

Z ∞

> 0

∥ exp (Aqτ)y∥2dτ > 0,

where strict positivity follows because the integrand is con-tinuous and equals ∥y∥2 > 0 at τ = 0. Thus, Pq is positive definite. Moreover, 

ATq Pq + PqAq =

Z ∞

> 0

ATq exp (ATq τ) exp (Aqτ)+ exp (ATq τ) exp (Aqτ)Aq

 dτ

=

Z ∞

> 0

ddτ

exp (ATq τ) exp (Aqτ) dτ

= exp (ATq τ) exp (Aqτ)∞

> 0

= 0 − I = −I.

The first equality follows by substituting the integral definition of Pq and using the commutation identities Aq exp (Aqτ) = 

exp (Aqτ)Aq and their transposes. The product rule gives the second equality. Finally, exp (Aqτ) → 0 as τ → ∞ because 

Aq is Hurwitz, whereas both exponential matrices equal the identity at τ = 0. Define Vq(t) ≜ ηT (t)Pqη(t). Between two consecutive switching instants, ˙Vq(t) = ηT (t)( ATq Pq + PqAq)η(t) + 2ηT (t)PqBqx(q) 

> 0

(t)= −∥ η(t)∥2 + 2ηT (t)PqBqx(q) 

> 0

(t)

≤ −∥ η(t)∥2 + 2∥η(t)∥ ∥ PqBq∥ | x(q) 

> 0

(t)|≤ − 12 ∥η(t)∥2 + 2∥PqBq∥2|x(q) 

> 0

(t)|2

≤ − aV q(t) + 2 max  

> p∈Q

∥PpBp∥2∥w(t)∥2. (39) The first line follows from ˙ η(t) = Aqη(t) + Bqx(q) 

> 0

(t). Substi-tution of the Lyapunov identity ATq Pq + PqAq = −I gives the second line, and the Cauchy–Schwarz inequality gives the third line. Young’s inequality 2 xy ≤ x2/2+2y2, with x = ∥η(t)∥ and 

y = ∥PqBq∥| x(q) 

> 0

(t)|, gives the fourth line. For the last line, the definitions of w and a yield 

|x(q) 

> 0

(t)| ≤ ∥ w(t)∥,

12 ∥η(t)∥2 ≥ Vq(t)

2λmax (Pq) ≥ aV q(t),

and ∥PqBq∥2 ≤ max p∈Q ∥PpBp∥2.At a switching instant ts, Lemma 5 gives 

η(t+ 

> s

) = Rq(t− 

> s)

η(t− 

> s

) + Sq(t− 

> s)

w(ts).

Since every switch increases the order by one, q(t+ 

> s

) = q(t− 

> s

)+ 

1. Substituting the reset relation into the Lyapunov function and using ∥x + y∥2 ≤ 2∥x∥2 + 2∥y∥2 yield 

Vq(t+ 

> s)

(t+ 

> s

)= P1/2 

> q(t+
> s)

η(t+ 

> s

) 2

≤ 2 P1/2 

> q(t+
> s)

Rq(t− 

> s)

η(t− 

> s

) 2

+ 2 P1/2 

> q(t+
> s)

Sq(t− 

> s)

w(ts) 2

= 2η(t− 

> s

)T RTq(t− 

> s)

Pq(t+ 

> s)

Rq(t− 

> s)

η(t− 

> s

) + 2 P1/2 

> q(t+
> s)

Sq(t− 

> s)
> 2

∥w(ts)∥2

The first term satisfies 2η(t− 

> s

)T RTq(t− 

> s)

Pq(t+ 

> s)

Rq(t− 

> s)

η(t− 

> s

)

≤ 2λmax 



RTq(t− 

> s)

Pq(t+ 

> s)

Rq(t− 

> s)



∥η(t− 

> s

)∥2

≤

2λmax 



RTq(t− 

> s)

Pq(t+ 

> s)

Rq(t− 

> s)



λmin (Pq(t− 

> s)

) Vq(t− 

> s)

(t− 

> s

)

≤ μVq(t− 

> s)

(t− 

> s

),

where the second inequality uses Vq(t− 

> s)

(t− 

> s

) ≥

λmin (Pq(t− 

> s)

)∥η(t− 

> s

)∥2, and the last one follows from the definition of μ. Similarly, the second term is bounded by 2 P1/2 

> q(t+
> s)

Sq(t− 

> s)
> 2

∥w(ts)∥2 ≤ 2 max 

> 1≤q<qmax

P1/2

> q+1

Sq

> 2

∥w(ts)∥2.10 

Combining these two bounds gives 

Vq(t+ 

> s)

(t+ 

> s

) ≤ μVq(t− 

> s)

(t− 

> s

)+ 2 max 

> 1≤q<qmax

P1/2

> q+1

Sq

> 2

∥w(ts)∥2. (40) Consider an interval between two consecutive switching instants ts and ts+1, and denote the constant order on this interval by q. Inequality (39) can be written as ˙Vq(t) + aV q(t) ≤ 2 max  

> p∈Q

∥PpBp∥2∥w(t)∥2.

Multiplying both sides by the integrating factor exp (at ) gives ddt [exp (at )Vq(t)] ≤ 2 max  

> p∈Q

∥PpBp∥2 exp (at )∥w(t)∥2.

Integrating from ts to ts+1 yields exp (at s+1)Vq(t−

> s+1

) − exp (at s)Vq(t+ 

> s

)

≤ 2 max  

> p∈Q

∥PpBp∥2

Z ts+1

> ts

exp (aτ)∥w(τ)∥2dτ.

Dividing by exp (at s+1) gives 

Vq(t−

> s+1

) ≤ exp [−a(ts+1 − ts)] Vq(t+ 

> s

) + 2 max  

> p∈Q

∥PpBp∥2

×

Z ts+1

> ts

exp [−a(ts+1 − τ)] ∥w(τ)∥2dτ. (41) Now fix t > 0 and let N be the number of switches on (0,t].Since q(0) = 1 and every switch increases the order by one, 0 ≤ N ≤ qmax − 1.

We now iterate the continuous-evolution estimate (41) and the reset estimate (40). Suppose first that N ≥ 1, and denote the switching instants by 0 < t1 < · · · < tN ≤ t. When tracking only the contribution propagated from Vq(0)(0), each application of (40) contributes a factor μ, whereas each application of (41) contributes the exponential-decay factor for that interval. Repeated application of these two inequalities therefore gives 

μN exp [−a(t − tN )] exp [−a(tN − tN−1)] · · · × exp [−a(t2 − t1)] exp (−at 1)Vq(0)(0)= μN exp (−at )Vq(0)(0),

because the interval lengths sum to (t − tN ) + ( tN − tN−1) + 

· · · + ( t2 − t1) + t1 = t. When N = 0, there is no reset and the same expression reduces to exp (−at )Vq(0)(0). A continuous-input contribution generated at time τ is multiplied by the remaining exponential-decay factor exp [−a(t − τ)] and by at most N reset factors. Hence, the sum of all continuous-input contributions is bounded by 2μN max  

> p∈Q

∥PpBp∥2

Z t

> 0

exp [−a(t − τ)] ∥w(τ)∥2dτ

≤ 2μN

a max  

> p∈Q

∥PpBp∥2 sup 

> 0≤τ≤t

∥w(τ)∥2.

Finally, the input term introduced at the rth reset passes through N − r later resets. Dropping the subsequent exponential-decay factors, which are no greater than one, bounds the total reset contribution by 2 max 

> 1≤q<qmax

P1/2

> q+1

Sq 

> 2N−1

# ∑

> j=0

μ j sup 

> 0≤τ≤t

∥w(τ)∥2.

The sum is absent when N = 0. Adding the three contributions gives 

Vq(t)(t) ≤ μN exp (−at )Vq(0)(0)+ 2μN

a max  

> p∈Q

∥PpBp∥2 sup 

> 0≤τ≤t

∥w(τ)∥2

+ 2 max 

> 1≤q<qmax

P1/2

> q+1

Sq

> 2

×

> N−1

# ∑

> j=0

μ j sup 

> 0≤τ≤t

∥w(τ)∥2.

Applying N ≤ qmax − 1 and the definition of G reduces this to 

Vq(t)(t) ≤ μqmax −1 exp (−at )Vq(0)(0)+ G sup 

> 0≤τ≤t

∥w(τ)∥2.

Since 



min  

> p∈Q

λmin (Pp)



∥η(t)∥2 ≤ Vq(t) ≤



max  

> p∈Q

λmax (Pp)



∥η(t)∥2,

the estimate for Vq(t)(t) together with these eigenvalue bounds implies 

∥η(t)∥2 ≤ C2ISS exp (−at )∥η(0)∥2

+ γ2ISS sup 

> 0≤τ≤t

∥w(τ)∥2.

Taking square roots and using √x + y ≤ √x + √y proves (33). 

(iii) Because qk is integer-valued, nondecreasing, and bounded above by qmax , there is an integer K such that qk = q∞

for every k ≥ K. Suppose first that q∞ < qmax . No switch occurs after tK . Therefore, the first case of (21) must be false, and 

rk > εk, k ≥ K. (42) The tracking error is continuous at every decision instant, so 

J(t− 

> k

) = J(t+ 

> k

) = J(tk). If J(tk) > 0, substituting (19) into (42) gives 

J(tk+1) < (1 − εk)J(tk). (43) If J(tk) = 0, then J(tk+1) must also be zero; otherwise rk = −∞

and the switching law would increase the order. Thus, (43) holds with a non-strict inequality in all cases. Iterating this inequality and applying the lower estimate in Lemma 4 proves (34). Since 0 < εk < 1, log (1 − εk) ≤ − εk. Hence, 

> k−1

# ∏

> ℓ=K

(1 − εℓ) ≤ exp −

> k−1

# ∑

> ℓ=K

εℓ

!

.

The product therefore tends to zero whenever ∑k εk = ∞. For the schedule (20), this condition holds if ε∞ > 0. When ε∞ = 0, it reduces to the divergence of the p-series ∑k ε0/(k + 1)α ,which occurs exactly for 0 < α ≤ 1. This proves the endpoint convergence assertion. 11 

Now suppose that q∞ = qmax , and choose T after the last switch. Then q(t) = qmax for every t ≥ T , so (29) becomes the linear time-invariant system ˙η(t) = Aqmax η(t) + Bqmax x(qmax ) 

> 0

(t), t ≥ T.

Applying the variation-of-constants formula to this system with initial state η(T ) gives 

η(t) = exp (Aqmax (t − T )) η(T )+ 

> ZtT

exp (Aqmax (t − τ)) Bqmax x(qmax ) 

> 0

(τ)dτ.

We next convert the asymptotic magnitude of the input x(qmax )

> 0

into an asymptotic bound on the state. Define ¯d ≜ lim sup 

> t→∞

|x(qmax ) 

> 0

(t)|.

If ¯d = ∞, the claimed bound is immediate. Suppose instead that ¯d < ∞. By the definition of the limit superior, for every 

δ > 0 there exists Tδ ≥ T such that 

|x(qmax ) 

> 0

(τ)| ≤ ¯d + δ , τ ≥ Tδ .

Applying the variation-of-constants formula from Tδ yields 

η(t) = exp (Aqmax (t − Tδ )) η(Tδ )+ 

> ZtTδ

exp (Aqmax (t − τ)) Bqmax x(qmax ) 

> 0

(τ)dτ.

Therefore, for t ≥ Tδ ,

∥η(t)∥ ≤ ∥ exp (Aqmax (t − Tδ )) η(Tδ )∥

+ ( ¯d + δ ) 

> ZtTδ

∥ exp (Aqmax (t − τ)) Bqmax ∥dτ

= ∥ exp (Aqmax (t − Tδ )) η(Tδ )∥

+ ( ¯d + δ ) 

> Zt−Tδ
> 0

∥ exp (Aqmax s)Bqmax ∥ds

≤ ∥ exp (Aqmax (t − Tδ )) η(Tδ )∥

+ ( ¯d + δ ) 

> Z∞
> 0

∥ exp (Aqmax s)Bqmax ∥ds,

where the equality uses the change of variable s = t − τ.Because Aqmax is Hurwitz, the first term tends to zero and the improper integral is finite. Taking the limit superior gives lim sup 

> t→∞

∥η(t)∥ ≤ ( ¯d + δ ) 

> Z∞
> 0

∥ exp (Aqmax s)Bqmax ∥ds.

Letting δ ↓ 0, substituting the definition of ¯d, and using ∥e∥ ≤ ∥η∥ proves the saturated-order bound (35). Finally, the same argument applies after the order settles at any q∞ ∈ Q. In that case, the final subsystem is ˙η(t) = Aq∞ η(t) + Bq∞ x(q∞) 

> 0

(t).

Because Aq∞ is Hurwitz, the homogeneous response converges to zero. If x(q∞) 

> 0

(t) → 0, split the convolution at a sufficiently large time: the contribution over the fixed initial interval decays exponentially, while the remaining contribution is bounded by sup 

> τ≥Tδ

|x(q∞) 

> 0

(τ)| 

> Z∞
> 0

∥ exp (Aq∞ s)Bq∞ ∥ds,

which can be made arbitrarily small. Therefore, η(t) → 0 and hence e(t) → 0. 

Remark 4. Part (i) shows that every individual order produces a stable continuous subsystem. This fact alone would not generally guarantee stability under switching, because a reset may increase the Lyapunov function even when both the old and new subsystems are stable. Part (ii) accounts explicitly for this increase through the factor μ and the reset-input matrices 

Sq.

Remark 5. The vector w (t) contains all target derivatives required by the admissible controller orders. When order q is active, the continuous dynamics (29) are driven only by x(q) 

> 0

(t), whereas an order increase at t s introduces the reset term Sqw(ts) from Lemma 5. Accordingly, the second term in (33) bounds both the continuous forcing and the reset-induced state increments. If w (t) is bounded, this term remains finite, and hence η(t) remains uniformly bounded throughout all admissible order transitions. The first term represents the zero-input response and decays exponentially from the initial state. Because the input term uses the supremum over the entire past, however, (33) alone does not imply convergence when w (t) → 0. Instead, Part (iii) establishes convergence after the order settles: x (q∞) 

> 0

(t) → 0 implies e (t) → 0. Finally, the constants in (33) account for every admissible order and reset, so the estimate provides a uniform robustness bound. 

Remark 6. The condition q ∞ < qmax means that the active order repeatedly achieves the relative reduction required by the monitor, so no further integral state is activated. It does not, by itself, mean that the target has an exact internal model of order q∞. The monitor compares only the two endpoints of each decision interval. Accordingly, (34) guarantees contraction at the decision instants, but it does not exclude a larger error between those instants. 

Remark 7. The equality q ∞ = qmax indicates that the adap-tation has reached its prescribed complexity limit; it does not indicate loss of stability. Bound (35) shows that the remaining tracking error is governed by two factors: the asymptotic magnitude of the unmodeled derivative x (qmax ) 

> 0

and the induced gain of the final stable subsystem. In particular, a polynomial target of degree d is tracked asymptotically whenever q ∞ ≥ d + 1, since then x (q∞) 

> 0

= 0.

VII. D ETECTION OF INSUFFICIENT ORDERS 

The preceding convergence analysis identifies the admissi-ble final orders, but it does not yet explain how the switching mechanism recognizes an insufficient active order or how quickly it does so. For a polynomial target of degree d, the error behavior depends on the degree of under-modeling. If 

q < d, the unmatched polynomial forcing causes the tracking error to grow polynomially, whereas the critical order q = d

produces a nonzero steady residual. In either case, the relative improvement rk in (19) eventually falls below the threshold εk

in (20). Therefore, whenever q < qmax , the switching rule (21) rejects the active order after finitely many decision intervals. The critical case q = d is the most difficult to detect because its 12 

residual remains bounded rather than growing. We therefore also derive an explicit upper bound on its rejection time. The following theorem formalizes these claims. 

Theorem 3. Suppose that the hypotheses of Theorem 2 hold, the decision instants are uniformly spaced as t k = kT d , ε∞ > 0,and the target is a polynomial of exact degree d ≥ 1, i.e., x0(t) = ∑dr=0 artr with a d̸ = 0. Then the following statements hold. (i) Every active order q ≤ d is rejected after finitely many decision intervals unless q = qmax . Consequently, q∞ ≥ min {d + 1, qmax }. (44) 

(ii) Suppose that order q = d is activated, and let k d be the first decision index such that q kd = d. If d < qmax , then its rejection time satisfies the explicit bound trej ,d ≤ tkd +

 τd

Td



+ 1



Td ,

where τd is a constant defined in (60) . In particular, order d is rejected no later than one decision interval after the first decision instant not smaller than t kd + τd .Proof: (i) For z ∈ Rn, define 

Jm(z) ≜ max  

> i=1,..., n

ρi(z), (45) where ρi(z) ≜  12 ∑ j∈Ni (zi − z j)2 + ai0z2

> i

1/2. By definition, 

J(t) = Jm(e(t)) . The reverse triangle inequality applied to the local quantity ρi in (45) gives 

|ρi(z) − ρi(y)| ≤ ρi(z − y).

Using the standard inequality | max i ai − max i bi| ≤ max i |ai −

bi| and then taking the maximum of the local bounds over i

yields 

|Jm(z) − Jm(y)| ≤ max  

> i=1,..., n

|ρi(z) − ρi(y)|≤ max  

> i=1,..., n

ρi(z − y) = Jm(z − y). (46) Hence, 

J2m(z − y) ≤

> n

# ∑

> i=1

"

12 ∑

> j∈Ni

(zi − yi) − (z j − y j)2 + ai0(zi − yi)2

#

= ( z − y)T (L + A0)( z − y).

By Lemma 1, 

J2m(z − y) ≤ (z − y)T (L + A0)( z − y) ≤ λn∥z − y∥2,

which further gives 

Jm(z − y) ≤ pλn∥z − y∥.

Substituting this bound into (46) yields 

|Jm(z) − Jm(y)| ≤ pλn∥z − y∥. (47) We next characterize the response of an insufficient fixed order. Suppose that order q ≤ d is activated and never sub-sequently changed. Its error dynamics are given directly by (28). By Theorem 2, the homogeneous system associated with (28) is exponentially stable. Therefore, the error can be decomposed as 

e(t) = eh(t) + ep(t), (48) where eh and its derivatives decay exponentially and ep is a polynomial particular solution. Consider first q < d and set m ≜ d − q ≥ 1. Since the terms 

artr with r < q vanish after q differentiations, differentiating the target polynomial term by term gives 

x(q) 

> 0

(t) = 

> d

# ∑

> r=q

ar

r!

(r − q)!tr−q

= ad

d!

(d − q)!td−q +

> d−1

# ∑

> r=q

ar

r!

(r − q)!tr−q

= ad d!

m! tm + O(tm−1). (49) Since ep(t) ∈ Rn, write the polynomial particular solution as 

ep(t) = cmtm + cm−1tm−1 + · · · + c0, c0, . . . , cm ∈ Rn.

(50) After substituting ep(t) into (28), the term e(q) 

> p

(t) and the terms κl (L + A0)e(q−1−l) 

> p

(t) for l = 0, . . . , q − 2 contain at least one derivative of ep(t) and therefore have degree at most m − 1. The remaining term, corresponding to l = q − 1, contains no derivative. Consequently, its leading component 

κq−1(L + A0)cmtm is the only term of degree m on the left-hand side. By (49), the coefficient of tm on the right-hand side of (28) is −ad d!1/m!. Equality of the two polynomial expressions therefore requires their coefficients of tm to satisfy 

κq−1(L + A0)cm = − ad d!

m! 1. (51) Multiplying both sides of (51) by (L + A0)−1/κq−1 gives 

cm = vq ≜ − ad d!

m!κq−1

(L + A0)−11. (52) Since ad̸ = 0 and L + A0 is nonsingular, vq̸ = 0. Substituting (50) and cm = vq from (52) into the solution decomposi-tion (48) gives 

e(t) = eh(t) + vqtm + cm−1tm−1 +

> m−2

# ∑

> r=0

crtr.

Here, the last sum is interpreted as zero when m = 1 and is therefore O(tm−2) for every m ≥ 1. Moreover, exponential stability implies eh(t) = o(tm−2), because exponential decay dominates every polynomial power. Therefore, 

e(t) = vqtm + cm−1tm−1 + O(tm−2). (53) Equation (51) shows that [( L + A0)vq]i̸ = 0 for every i. Hence 

ρi(vq) > 0; otherwise, all the relative and target-access terms 13 

defining ρi(vq) would vanish, which implies [( L + A0)vq]i = 0. Define 

aq,i ≜ ρi(vq) > 0,

bq,i ≜ 1

aq,i

"

12 ∑

> j∈Ni

(vq,i − vq, j) (cm−1)i − (cm−1) j



+ ai0vq,i(cm−1)i

#

.

Substituting (53) into the definition of ρi in (45) and collecting equal powers of t gives 

ρ2 

> i

(e(t)) = a2

> q,i

t2m + 2aq,ibq,it2m−1 + O(t2m−2).

Since aq,i > 0, taking the square root and using (1 + s)1/2 =

1 + s/2 + O(s2) as s → 0 yields 

ρi(e(t)) = aq,itm



1 + 2bq,i

aq,it + O(t−2)

1/2

= aq,itm



1 + bq,i

aq,it + O(t−2)



= aq,itm + bq,itm−1 + O(tm−2).

Define the largest leading coefficient and the set of indices attaining it by 

αq ≜ max  

> i=1,..., n

aq,i = Jm(vq) > 0,

Iq ≜ {i ∈ { 1, . . . , n} : aq,i = αq}.

For every i /∈ Iq, the positive gap αq − aq,i multiplies the dominant term tm; hence such an index cannot maximize 

ρi(e(t)) for all sufficiently large t. Thus, for all sufficiently large t, only indices in Iq can attain the maximum. Defining 

bq ≜ max i∈Iq bq,i, and using the fact that Iq is finite, we obtain 

J(t) = max 

> i∈Iq

αqtm + bq,itm−1 + O(tm−2)

= αqtm + bqtm−1 + O(tm−2). (54) Factoring the leading term in (54) gives 

J(tk) = αqtmk



1 + bq

αqtk

+ O(t−2 

> k

)



,

J(tk + Td ) = αqtmk



1 + mT d + bq/αq

tk

+ O(t−2 

> k

)



.

Substituting these two expressions into the relative-reduction test (19) and using (1 + s)−1 = 1 − s + O(s2) as s → 0 yields 

rk = 1 − J(tk + Td )

J(tk)= 1 − 1 + ( mT d + bq/αq)/tk + O(t−2 

> k

)

1 + bq/(αqtk) + O(t−2 

> k

)= 1 −



1 + mT d + bq/αq

tk

+ O(t−2 

> k

)

  

1 − bq

αqtk

+ O(t−2 

> k

)



= − mT d

tk

+ O(t−2 

> k

). (55) Thus, rk < 0 for all sufficiently large k. Since εk > 0, an order 

q < d cannot remain active indefinitely unless it has reached the prescribed maximum order. Now consider the critical under-order q = d. In this case, 

x(d) 

> 0

(t) = ad d! is constant. Setting q = d in (28) gives 

e(d)(t) + 

> d−1

# ∑

> l=0

κl (L + A0)e(d−1−l)(t) = −ad d!1.

For a constant particular solution e(t) = e⋆ 

> d

, all positive-order derivatives of e⋆ 

> d

vanish. Hence every term in the sum with l <

d − 1 is zero, while the term with l = d − 1 contains e(0)(t) = 

e⋆ 

> d

. The remaining algebraic equation is therefore 

κd−1(L + A0)e⋆ 

> d

= −ad d!1,

and hence 

e⋆ 

> d

= − ad d!

κd−1

(L + A0)−11. (56) Since ad̸ = 0 and L + A0 is nonsingular, e⋆

> d

̸ = 0. Because Jm is a norm, the corresponding steady monitor value J⋆ 

> d

≜ Jm(e⋆ 

> d

) is strictly positive. If order d were to remain active indefinitely, the solution would be the sum of e⋆ 

> d

and an exponentially decaying homogeneous response. Consequently, 

e(t) → e⋆ 

> d

, J(t) → J⋆ 

> d

> 0. (57) Thus, the two endpoint values used to compute rk converge to the same positive limit, which implies rk → 0. Since εk ≥ ε∞ >

0, the inequality rk ≤ εk must hold after finitely many decision intervals. The switching rule then rejects order d whenever 

d < qmax , contradicting indefinite activation. Applying this conclusion together with the result for q < d successively to every active order q ≤ d proves (44). 

(ii) It remains to establish the stated quantitative rejection-time bound for the critical under-order. By definition, tkd is the time at which order d becomes active; in particular, kd = 0when d = 1. While order d is active, the derivative-state vector is 

η(t) = eT (t) [e(1)(t)] T · · · [e(d−1)(t)] T T .

At the constant particular solution e(t) = e⋆ 

> d

, all derivatives 

e(r)(t), r = 1, . . . , d − 1, are zero. The corresponding equilib-rium of the derivative-state dynamics is therefore 

η⋆ 

> d

≜ (e⋆ 

> d

)T 0T · · · 0T T .

Equation (56) ensures that Ad η⋆ 

> d

+Bd ad d! = 0, so η⋆ 

> d

is indeed an equilibrium of (29) for the constant input x(d) 

> 0

= ad d!. Choose Md ≥ 1 and βd > 0 such that 

∥ exp (Adt)∥ ≤ Md exp (−βdt), t ≥ 0,

which is possible because Ad is Hurwitz. Subtracting η⋆ 

> d

from the state equation gives the exact homogeneous relation 

η(t) − η⋆ 

> d

= exp [Ad (t − tkd )][ η(tkd ) − η⋆ 

> d

].

It follows from the definition of Md and βd that 

∥η(t) − η⋆ 

> d

∥ ≤ Md exp [−βd (t − tkd )] ∥η(tkd ) − η⋆ 

> d

∥. (58) Combining (58) with (47) gives 

|J(t) − J⋆ 

> d

| ≤ pλnMd exp [−βd (t − tkd )] ∥η(tkd ) − η⋆ 

> d

∥. (59) 14 

Define 

τd ≜ 1

βd

"

log (2 + ε∞)√λnMd ∥η(tkd ) − η⋆ 

> d

∥

ε∞J⋆

> d

#

> +

, (60) Define 

θd ≜ ε∞

2 + ε∞

.

For every t ≥ tkd + τd , we have 

pλnMd exp [−βd (t − tkd )] ∥η(tkd ) − η⋆ 

> d

∥≤ pλnMd exp (−βd τd )∥η(tkd ) − η⋆ 

> d

∥.

If pλnMd ∥η(tkd ) − η⋆ 

> d

∥ ≤ θd J⋆ 

> d

,

then the logarithm in (60) is nonpositive, so τd = 0 and 

pλnMd exp (−βd τd )∥η(tkd ) − η⋆ 

> d

∥

= pλnMd ∥η(tkd ) − η⋆ 

> d

∥ ≤ θd J⋆ 

> d

.

Otherwise, 

τd = 1

βd

log 

√λnMd ∥η(tkd ) − η⋆ 

> d

∥

θd J⋆

> d

,

and direct substitution gives 

pλnMd exp (−βd τd )∥η(tkd ) − η⋆ 

> d

∥ = θd J⋆ 

> d

.

Hence, in either case, the exponential bound is no greater than 

θd J⋆ 

> d

. Therefore, (59) yields 

|J(t) − J⋆ 

> d

| ≤ θd J⋆ 

> d

, t ≥ tkd + τd . (61) At t = tk, (61) gives 

J(tk) ≥ J⋆ 

> d

− | J(tk) − J⋆ 

> d

| ≥ (1 − θd )J⋆ 

> d

.

Applying the same estimate at t = tk+1 and using the triangle inequality yields 

J(tk) − J(tk+1) = J(tk) − J⋆ 

> d

+ J⋆ 

> d

− J(tk+1)

≤ | J(tk) − J⋆ 

> d

| + |J(tk+1) − J⋆ 

> d

|≤ 2θd J⋆ 

> d

.

Therefore, 

rk ≤ 2θd

1 − θd

= ε∞ ≤ εk,

and the switching rule rejects order d at tk+1. The first decision instant not smaller than tkd + τd is at most tkd + ⌈τd /Td ⌉Td .Therefore, the rejection time trej ,d satisfies 

trej ,d ≤ tkd +

 τd

Td



+ 1



Td . (62) Theorem 3 formalizes the distinction between OADIC and a fixed-order controller. A fixed order q ≤ d permanently accepts the residual generated by the unmodeled target derivative, whereas OADIC rejects that order in finite time. The critical order q = d is the slowest deficiency to detect: its error remains bounded and converges to the nonzero value (56). The bound (62) shows that weak connectivity, slow modal decay, a small threshold, or a large reset transient can all delay the detection.                                    

> TABLE I TRACKING –COMPLEXITY –STRUCTURE COMPARISON OVER T=60 S
> Controller Etail ¯c(T)Feedback FOIC, q=121 .0822 0Linear FOIC, q=21.0211 1Linear FOIC, q=30.0039 2Linear NFOIC, q=30.0029 2Signed power OADIC 0.0431 1.5333 Linear by order

VIII. S IMULATION 

In this section, we compare OADIC with both fixed-order linear controllers and a nonlinear controller. We consider ten agents connected by an undirected ring augmented with the links {1, 6} and {3, 8}; hence, 

E = {{ 1, 2}, {2, 3}, . . . , {9, 10 }, {10 , 1}, {1, 6}, {3, 8}} .

Agents 1, 5, and 8 have access to the target. We choose ¯di =

i − 5.5 for i = 1, . . . , 10 , and x0(t) = 0.12 t2 + 0.35 t. We then set ¯ζi j = ¯di − ¯d j on every communication edge and ¯ζi0 = ¯di for each target-informed agent. Thus, the desired displacements satisfy the consistency condition in Theorem 1. We set qmax =

3 and use the nested gain sequence κ0 = 8, κ1 = 3, κ2 =

0.25 . For this network, λ1 = 0.2455. Therefore, κ1 < λ1κ20 /4 =

3.9285 and κ2 < κ21 /(4κ0) = 0.2813, so the gain conditions in (24) hold. OADIC uses Td = 4, ε0 = 0.22, ε∞ = 0.03, and 

α = 0.6. As linear benchmarks, we use fixed-order integral control (FOIC) with q = 1, 2, 3 and the same gains as OADIC. We also define a nonlinear-enhanced FOIC (NFOIC) by augmenting the full order-three FOIC with the fixed-time-type signed-power feedback commonly used in nonlinear and distributed finite-time control [11, 12]: 

uNL  

> i

= −

> 2

# ∑

> l=0

κl σil − γ1 sig μ (σi0) − γ2 sig ν (σi0), (63) where sig p(z) ≜ |z|p sign (z), μ = 0.6, ν = 1.4, γ1 = 1.2, and 

γ2 = 0.12. Thus, NFOIC receives the complete order-three internal model from the outset and further strengthens its transient feedback through one sublinear and one superlinear power. This choice favors NFOIC in tracking performance and allows us to compare that performance with the structural simplicity and online state allocation of OADIC. We simulate every controller over T = 60 using a fixed-step fourth-order Runge–Kutta method with a step size of 0 .002. To quantify controller complexity, define the average num-ber of active integral states per agent over the simulation horizon [0, T ] as ¯c(T ) ≜ 1

T

Z T

> 0

[q(t) − 1]dt. (64) Thus, the fixed orders q = 1, 2, 3 have ¯ c = 0, 1, 2, respectively, and NFOIC has ¯ c = 2. We also calculate the tail root-mean-square error 

Etail ≜

 10.2T

Z T

> 0.8T

∥e(t)∥2dt

1/2

. (65) Figure 2(a) first confirms the role of controller order. The proportional controller cannot follow the accelerating 15  

> Fig. 2. Ten-agent comparison for a quadratic target: (a) global tracking errors; (b) order selected by OADIC; and (c) cumulative integral-state time per agent.

target, whereas the order-two controller removes the velocity-dependent component but retains an acceleration-induced residual. By contrast, both order-three controllers contain the required internal model and drive the error toward zero. The additional signed-power terms give NFOIC the smallest tail error, but they do not reduce its prescribed order or its number of dynamic states. As shown in Fig. 2(b), OADIC initially retains proportional feedback, activates the first integral state at t = 8, and activates the second at t = 20. Thereafter, it has the same order-three internal model as the full-order FOIC and NFOIC, and its error converges toward zero. Because OADIC delays both activations, its finite-horizon tail error is larger than those of the controllers that use the complete order-three structure from 

t = 0. Thus, OADIC trades some transient speed for online complexity allocation. The benefit of this tradeoff appears in Fig. 2(c) and Table I. OADIC uses no integral state over [0, 8), one per agent over 

[8, 20 ), and two thereafter. Consequently, ¯ c(60 ) = ( 0 × 8 + 1 ×

12 + 2 × 40 )/60 ≈ 1.5333. Relative to either full-order bench-mark, the corresponding reduction is (2−1.5333 )/2×100% =

23 .3%. Likewise, the tail RMS error reduction relative to the fixed order-two controller is (1.0211 − 0.0431 )/1.0211 ×

100% = 95 .8%. Therefore, nonlinear enhancement can im-prove the transient when the complete internal model is already available, but it does so while retaining all high-order states and introducing fractional-power computations. OADIC takes a different route: it keeps every active controller linear and approaches the required high-order accuracy while activating the corresponding states only when the observed tracking performance demands them. IX. C ONCLUSION 

We developed OADIC for distributed dynamic coordination when the required internal-model order is unknown. Instead of fixing a conservative high order or introducing nonlinear feedback, we start with proportional feedback and add integral states only when the observed tracking progress is insufficient. We thereby move among nested linear controllers according to actual need while keeping the control law purely linear at every active order. Moreover, we preserve the existing states and gains during each order transition and initialize the new state to maintain continuity of the control input. We also established the main guarantees behind this adapta-tion mechanism. First, we derived a cycle-consistency condi-tion that determines whether the prescribed relative displace-ments define a feasible coordination pattern. Next, we con-structed a nested gain condition that stabilizes every admissible fixed-order subsystem. We then established a uniform input-to-state stability bound that accounts for both target motion and order transitions. Furthermore, we proved that OADIC rejects every insufficient nonsaturated order after finitely many decision intervals, even when the corresponding fixed-order subsystem is stable but leaves a nonzero tracking error. Finally, we used numerical comparisons to illustrate the re-sulting accuracy–complexity tradeoff. Compared with the fixed order-three controller, we reduced integral-state time by 23 .3% while approaching its tracking accuracy. Compared with the fixed order-two controller, we reduced the tail RMS error by approximately 95 .8%. NFOIC achieved a smaller finite-horizon tail error, but it retained both integral states throughout the operation and introduced signed-power feedback at every agent. Therefore, these results do not suggest that OADIC uniformly outperforms full-order controllers in transient speed. Instead, they show that we can obtain the necessary tracking capability with purely linear active controllers and without carrying the full order throughout the entire operation. In future work, we will study robustness to noisy measurements and changing network topologies. REFERENCES 

[1] B. A. Francis and W. M. Wonham, “The internal model principle of control theory,” Automatica , vol. 12, no. 5, pp. 457–465, 1976. [2] P. Wieland, R. Sepulchre, and F. Allg¨ ower, “An internal model principle is necessary and sufficient for linear output synchronization,” Automatica , vol. 47, no. 5, pp. 1068–1074, 2011. [3] Y. Su and J. Huang, “Cooperative output regulation of linear multi-agent systems,” IEEE Transactions on Automatic Control , vol. 57, no. 4, pp. 1062–1066, 2012. [4] Z. Li, W. Ren, X. Liu, and M. Fu, “Consensus of multi-agent systems with general linear and lipschitz nonlinear dynamics using distributed adaptive protocols,” IEEE Transactions on Automatic Control , vol. 58, no. 7, pp. 1786–1791, 2013. [5] M. Andreasson, D. V. Dimarogonas, H. Sandberg, and K. H. Johansson, “Distributed control of networked dy-namical systems: Static feedback, integral action and 16 

consensus,” IEEE Transactions on Automatic Control ,vol. 59, no. 7, pp. 1750–1764, 2014. [6] R. A. Freeman, P. Yang, and K. M. Lynch, “Stability and convergence properties of dynamic average consensus estimators,” in Proceedings of the 45th IEEE Conference on Decision and Control , 2006, pp. 338–343. [7] F. Chen and W. Ren, Distributed Average Tracking in Multi-agent Systems . Cham, Switzerland: Springer, 2020. [8] L. Cheng, Y. Wang, W. Ren, Z.-G. Hou, and M. Tan, “Containment control of multi-agent systems with dy-namic leaders based on a PI n-type approach,” IEEE Transactions on Cybernetics , vol. 46, no. 12, pp. 3004– 3017, 2016. [9] J. Guo, Y. Zhang, and J.-F. Zhang, “Distributed output feedback indirect MRAC of continuous-time multiagent linear systems,” SIAM Journal on Control and Optimiza-tion , vol. 63, no. 3, pp. 1616–1640, 2025. [10] G. Fattore and M. E. Valcher, “Distributed output syn-chronization of discrete-time multi-agent systems: Adata-driven approach,” Automatica , vol. 185, p. 112789, 2026. [11] A. Polyakov, “Nonlinear feedback design for fixed-time stabilization of linear control systems,” IEEE Transac-tions on Automatic Control , vol. 57, no. 8, pp. 2106– 2110, 2012. [12] S. E. Parsegov, A. E. Polyakov, and P. S. Shcherbakov, “Fixed-time consensus algorithm for multi-agent systems with integrator dynamics,” in IFAC Proceedings Volumes ,vol. 46, no. 27, 2013, pp. 110–115. [13] F. Chen, W. Ren, W. Lan, and G. Chen, “Distributed average tracking for reference signals with bounded accelerations,” IEEE Transactions on Automatic Control ,vol. 60, no. 3, pp. 863–869, 2015. [14] S. Zhou, D. Sun, and G. Feng, “Fixed-time formation tracking for heterogeneous linear multiagent systems with a nonautonomous leader,” IEEE Transactions on Control of Network Systems , vol. 12, no. 2, pp. 1733– 1743, 2025. [15] I. Katsoukis and G. A. Rovithakis, “Output feedback prescribed performance state synchronization for leader-following high-order uncertain nonlinear multi-agent sys-tems,” Journal of Automation and Intelligence , vol. 5, no. 1, pp. 35–45, 2026. [16] F. R. Chung, Spectral graph theory . American Mathe-matical Soc., 1997, vol. 92. [17] D. C. Kurtz, “A sufficient condition for all the roots of a polynomial to be real,” The American Mathematical Monthly , vol. 99, no. 3, pp. 259–263, 1992. [18] R. Olfati-Saber and R. M. Murray, “Consensus prob-lems in networks of agents with switching topology and time-delays,” IEEE Transactions on Automatic Control ,vol. 49, no. 9, pp. 1520–1533, 2004. [19] Y. Hong, J. Hu, and L. Gao, “Tracking control for multi-agent consensus with an active leader and variable topology,” Automatica , vol. 42, no. 7, pp. 1177–1182, 2006. [20] A. Jadbabaie, J. Lin, and A. S. Morse, “Coordination of groups of mobile autonomous agents using nearest neighbor rules,” IEEE Transactions on Automatic Con-trol , vol. 48, no. 6, pp. 988–1001, 2003. [21] J. A. Fax and R. M. Murray, “Information flow and coop-erative control of vehicle formations,” IEEE Transactions on Automatic Control , vol. 49, no. 9, pp. 1465–1476, 2004. [22] F. Chen, W. Ren, and Y. Cao, “Surrounding control in cooperative agent networks,” Systems & Control Letters ,vol. 59, no. 11, pp. 704–712, 2010. [23] A. Furch` ı, M. Lippi, A. Marino, and A. Gasparri, “A finite-time distributed dynamic consensus protocol for tracking maximum (minimum) reference signals,” Auto-matica , vol. 179, p. 112390, 2025. [24] M. Franceschelli, A. Gasparri, A. Giua, and C. Seatzu, “Decentralized estimation of Laplacian eigenvalues in multi-agent systems,” Automatica , vol. 49, no. 4, pp. 1031–1036, 2013.