Title: Fully Distributed GNE Algorithms for Multi-Robot Placement without Consensus on Multipliers

URL Source: https://arxiv.org/pdf/2608.29388v1

Published Time: Tue, 01 Sep 2026 00:52:56 GMT

Number of Pages: 6

Markdown Content:
# Fully Distributed GNE Algorithms for Multi-Robot Placement without Consensus on Multipliers 

Shao-An Yin, Mingyi Hong, and Nicola Elia 

Abstract — Recent machine learning research has increasingly focused on equilibrium analysis in non-cooperative games rather than solely on optimal solutions. Many such problems in-volve shared constraints and can be formulated as Generalized Nash Equilibrium Problems (GNEPs). For strongly monotone games, existing methods compute consensus-based variational GNEs (v-GNEs) by exchanging Lagrange multipliers. We pro-pose a fully distributed continuous-time algorithm for shared linear equality constraints that converges without multiplier exchange and reaches any GNE, reducing communication overhead and improving privacy. Discrete-time schemes are also provided, and the method is validated on a multi-robot placement task. 

I. I NTRODUCTION 

Nash Equilibrium (NE) has been widely studied across diverse areas [1] and plays a central role in multi-agent and adversarial machine learning, including multi-agent re-inforcement learning [2], [3], [4] and generative modeling frameworks [5], [6]. In many settings, agents must compute equilibria under shared constraints [7], [8], [9], leading to the Generalized Nash Equilibrium Problem (GNEP). The concept of GNEP was introduced in [10], [11]. In this setting, each agent minimizes its local cost given others’ decisions, while its feasible set also depends on those decisions, often due to shared resources or safety constraints. A Generalized Nash Equilibrium (GNE) occurs when no agent can reduce its cost by unilaterally changing its decision within this feasible set. Early work focused on existence results [10], [11]. However, the existence of a solution does not imply that it can be easily computed, even with centralized coordination. Early centralized approaches, such as [12], solved GNEPs using augmented penalty methods that repeatedly compute Nash equilibria with increasing penalties on shared con-straints, later extended in [13], [14], [15]. However, the resulting nonsmooth inner problems make distributed imple-mentations difficult [15]. Moreover, centralized computation becomes impractical with many agents due to high computa-tional and communication costs, and agents may be unwilling to share their cost functions for privacy reasons. These challenges motivate distributed approaches where agents compute decisions with limited information exchange. Most distributed GNEP studies focus on variational GNEs (v-GNEs) for strongly monotone games with linear shared           

> * This work was supported in part by NSF ECPN award 2311007. All authors are with the Department of Electrical and Computer Engineering, University of Minnesota, USA.

yin00425@umn.edu, mhong@umn.edu, nelia@umn.edu .

constraints. For example, [16], [17], [18] use operator-splitting methods, while [19] adopts a continuous-time ap-proach. These methods rely on equilibrium uniqueness and enforce multiplier consensus, thus computing only v-GNEs. In a v-GNE, agents subject to the same shared constraints use identical multipliers, implicitly assuming a central authority that enforces feasibility and broadcasts a common shadow price. In contrast, general GNEs allow agent-specific multi-pliers, modeling personalized tariffs on shared resources. In this paper, we formulate the multi-robot placement problem as a distributed GNEP with individual convex con-straints and shared linear equality constraints. For this class of problems, we propose a fully distributed continuous-time primal–dual algorithm that eliminates multiplier exchange and converges to general GNEs rather than only v-GNEs. We also establish convergence for strongly monotone games with shared linear equality constraints. The main contributions of this work are: 1) To the best of our knowledge, this is the first distributed algorithm that avoids consensus on Lagrangian multipli-ers for general convex cost functions, thereby reducing per-iteration communication. 2) Existing distributed algorithms are typically restricted to v-GNEs. In contrast, depending on the initialization, our algorithm can converge to general GNEs. 3) We formulate the multi-robot placement problem with shared constraints as a GNEP and demonstrate the ef-fectiveness of our algorithm. For this class of problems, our method reduces information exchange to one-third per iteration compared to existing approaches. II. P RELIMINARIES 

A. Notations 

We denote Rm as the m-dimensional Euclidean space, 

R≥0 and R>0 as the nonnegative and positive reals, and 

[m] = {1, 2, . . . , m }. Vectors 1 and 0 denote all-ones and all-zeros of appropriate dimensions. For a ∈ Rm, (a)i is its 

i-th component, a⊤ its transpose, and a⊤b the inner product with b ∈ Rm; a ≤ b indicates component-wise inequality. For A ∈ Rp×q , (A)ij is the (i, j ) entry and A⊤ the transpose. 

G = ( V, E) denotes a graph with vertex set V and edge set E.Following [20], [21], we also define the projection operator: 

[a]+ 

> b

:= 

(

a, if b > 0

max {0, a } if b = 0 . (1) Lastly, the sublevel set of a function V : Rm → R with 

β > 0 is defined as V −1(≤ β) = {x ∈ Rm | V (x) ≤ β}.

> © 2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses. arXiv:2608.29388v1 [cs.LG] 29 Aug 2026

B. Generalized Nash Equilibrium Problems (GNEPs) 

Consider a GNEP with N agents. Each agent v controls decision variables zv ∈ Rnv and has a cost function 

fv (zv , zf 

> −v

), which depends on its own decision variables and those of other agents, where zf 

> −v

denotes the variables from agents that influence fv (·). This dependency induces a cost-function graph Gf = ( Vf , Ef ). Each agent is also subject to individual constraints hv (zv ) ≤ 0qv . Let z ∈ Rn,with n = PNv=1 nv , denote the joint decision vector of all agents, and let q = PNv=1 qv denote the total number of individual constraints. Additionally, the constraint set of each agent depends on the decisions of other agents. Specifically, zv must satisfy 

gv (zv , zg 

> −v

) = 0mv , where zg 

> −v

denotes the collection of decision variables from all agents other than v that in-fluence gv (·). This dependency induces a constraint graph 

Gg = ( Vg , Eg ). Formally, each agent v solves the following optimization problem: 

min zv fv (zv , zf 

> −v

)

subject to hv (zv ) ≤ 0qv

gv (zv , zg 

> −v

) = 0mv

. (2) To avoid abuse of notation, we use z−v to denote the col-lection of other agents’ decision variables when the context is clear. That is, we write fv (zv , z−v ) instead of fv (zv , zf 

> −v

)

and gv (zv , z−v ) instead of gv (zv , zg 

> −v

).Specifically, in the paper, we assume the presence of globally shared linear equality constraints. In other words, 

g(z) = A z − c, (3) where A ∈ Rp×n and c ∈ Rp represent the global shared constraints among agents. However, these global constraints are not directly observed by any agent; each agent only knows its local component gv (zv , z−v ). Thus, for each agent, 

gv (zv , zg 

> −v

) = A(mv , n v ) zv + X

> j∈G g

A(mv , n j ) zj − c(mv ),

where A(mv , n v ) represents the mv rows and nv columns of A associated with zv ∈ Rnv and c(mv ) represents the mv

rows of c associated with zv .The solution concept for the GNEP (2) is called General-ized Nash Equilibrium (GNE): 

Definition 1 (Generalized Nash Equilibrium (GNE)) . z⋆ is a Generalized Nash Equilibrium (GNE) if for all agent v,

fv (z⋆v , z⋆ 

> −v

) ≤ fv (zv , z⋆ 

> −v

),

∀ z⋆v , z v ∈ { zv | gv (zv , z⋆ 

> −v

) = 0mv , h v (zv ) ≤ 0qv }.

Assumption 1. For every player v, fv (zv , z−v ) and hv (zv )

are continuously differentiable, convex, and have locally Lipschitz continuous gradients with respect to zv .

Assumption 2. Let z⋆ 

> −v

be given, and suppose that z⋆v is a solution of the GNEP (2), then for all v, λ⋆v ∈ Rmv , μ⋆v ∈

Rqv exist such that 

Lv (z, λ v , μ v ) := fv (zv , z−v ) + g⊤ 

> v

(zv , z−v )λv + h⊤ 

> v

(zv )μv

∇zv Lv (z⋆, λ ⋆v , μ ⋆v ) = 0nv

gv (z⋆v , z⋆ 

> −v

) = 0mv

0qv ≤ μ⋆v ⊥ hv (z⋆v ) ≤ 0qv

,

(4) where the last line uses the standard compact notation in optimization theory to jointly express primal feasibility, dual feasibility, and complementary slackness. Assumption 2 implies that, for each agent v, a suitable constraint qualification holds. In addition, we assume the Linear Independence Constraint Qualification (LICQ). Under the convexity assumptions in Assumption 1, stacking the KKT conditions of all agents together with the corresponding constraint qualifications implies that z⋆ is a GNE if and only if it satisfies the KKT conditions (4) (see Section 4.2 of [22]). In addition, we define the pseudo-gradient F : Rn → Rn

as a point-to-point map, where the v-th block is the gradient of the cost function of agent v with respect to its own decision variables, given by 

F (z) := 



∇z1 f1(z1, z−1)

∇z2 f2(z2, z−2)

...

∇zN fN (zN , z−N )

 . (5) 

Definition 2 (Strongly Monotone Game) . We say the GNEP (2) is strong monotone if there exists a δ > 0 such that ∀ z,

z′ ∈ Rn,

(z − z′)⊤ (F (z) − F (z′)) ≥ δ ∥z − z′∥2.

C. Variational GNE (v-GNE) 

A Variational Inequality (VI) problem, V I (K, F ), is de-fined as: Find z⋆ ∈ K such that 

(z − z⋆)⊤F (z⋆) ≥ 0 ∀ z ∈ K, (6) where 

K := {z | A z − c = 0p, h v (zv ) ≤ 0qv , ∀ v} , (7) is the Cartesian product of the local feasible sets and the global shared constraints. In practice, this global set is unknown to all agents. Each agent only knows a portion of it, based on its own decision variables. In [23], [24], the connection between the GNEP (2) and the VI problem (6) was established for the case where all agents share global constraints as in (3). It was shown that solving the VI (6) yields a solution to the GNEP (2). Such solutions are called v-GNE. It is important to note that GNEs may not be unique, and every v-GNE is contained in the solution set of the GNEP (2). 

Assumption 3. The GNEP (2) is strongly monotone. More-over, F is locally Lipschitz in z, i.e., Lipschitz continuous on every compact set K′ ⊆ Rn.Assumption 3 guarantees a unique solution to V I (K, F )

and thus ensures the existence of a GNE [25], even though the game itself may admit multiple equilibria. This assump-tion is common in distributed algorithms and arises in many engineering applications requiring distributed solvers [26], [27], [16], [17], [18], [19]. Moreover, [24] shows that a GNE of (2) coincides with a solution of V I (K, F ) when all agents use identical multipliers for shared constraints, linking multiplier consensus to v-GNEs. A v-GNE imposes uniform shadow prices on the shared constraints, meaning the KKT multipliers are identical across all agents. This formulation implicitly assumes a coordinating authority that enforces the shared constraints and broadcasts a common shadow price. In contrast, a general (non-variational) GNE allows player-specific multipliers, so each agent faces personalized prices or tariffs on shared resources. This distinction is important in both modeling and practice, as the two formulations capture different operational trade-offs. Our algorithm targets the general GNE set, extending applicability beyond v-GNEs. While multiplier consensus can recover a v-GNE under centralized coordination, our main result shows that even without such consensus, the proposed distributed continuous-time algorithm converges to the KKT points of the GNEP (2), and hence to a GNE. To the best of our knowledge, this is the first distributed algorithm that achieves convergence to any GNE without requiring multiplier consensus. 

D. Projected Dynamical System (PDS) 

We briefly review the Projected Dynamical System, a key tool in our convergence analysis. 

Definition 3 (Projected Dynamical System) . [10]. Given x ∈

ˆK and v ∈ Rn, where ˆK ⊆ Rn a closed convex set, define the projection of the vector v at x with respect to ˆK by 

Π ˆK (x, v ) := lim 

> ϵ→0

P ˆK (x + ϵv ) − xϵ ,

where P ˆK (x) := arg min y∈ ˆK ∥x − y∥. Let ˆF : Rn → Rn be a given mapping. The projected dynamical system (PDS) is defined as 

˙x = Π ˆK (x, − ˆF (x)) . (8) Due to the discontinuous nature of projected dynamical systems [28], [20], the Lie derivative of a continuously differentiable function V : Rn → R with respect to the dynamics (8) at a point x is defined as 

L V := ( ∇xV )⊤Π ˆK (x, − ˆF (x)) . (9) This enables us to extend standard stability analysis and apply invariance principles. III. P ROPOSED FULLY DISTRIBUTED ALGORITHM 

From the formulation (2), the distributed setup is described by two graphs: Gf = (Vf , Ef ), capturing cost-function dependencies, and Gg = ( Vg , Eg ), capturing dependencies from shared linear constraints. Each agent knows only its own fv (·), hv (·), and gv (·). The algorithm’s objective is to converge to a game solution while limiting information exchange over these graphs and preserving data privacy. In the worst case, both graphs are complete, with fv (·) and 

gv (·) depending on all agents’ decision variables. Building on this setup, our main contribution is Algorithm 1. 

Algorithm 1 Continuous Time Dynamics for Each Agent v 

> 1:

Input: Initial zv (0) , λv (0) , μv (0) ≥ 0 

> 2:

˙zv = −∇ zv Lv (z, λ v , μ v ). 

> 3:

˙λv = gv (zv , z−v ). 

> 4:

˙μjv = [ hjv (zv )] +

> μjv

, for j ∈ [qv ].This fully distributed method preserves the privacy of each agent’s cost function, multipliers, and constraints: agents exchange only their decision variables at each iteration. Specifically, in line 2, decision variables are communicated or made known over the cost-function graph Gf , and in line 3 over the constraint graph Gg . Hence, the information exchanged at each step is limited to decision variables transmitted over Gf ∪ G g . In contrast, existing v-GNE al-gorithms (e.g., [16], [17], [18], [19]) require exchanging not only decision variables but also Lagrangian multipliers and auxiliary variables over Gg .To quantify the savings, suppose Gf = Gg and each edge transmits a vector of dimension d. In our algorithm, agents exchange only decision variables, whereas consensus-based methods require an additional 2|E g | d messages per fixed time for multipliers and auxiliary variables. In this special case, our approach reduces the communication cost to one-third compared to consensus-based alternatives. 

A. Convergence Analysis 

Lemma 1. Algorithm 1 is an implementation of the PDS (8) where ˆF is created by stacking the right-hand side of the dynamics without the projection operations, and ˆK =

{(z, μ v λv ) | z ∈ Rn, μ v ≥ 0qv , λ v ∈ Rmv for all v}.

Consider the set of λiv (t) evolving according to the shared constraint gi(z), which represents the i-th component of the common constraints g(z). We arbitrarily select one of these as a reference state, denoted by λi(t). Thus, there are p

reference states. For each λiu(t) associated with the shared constraint gi(z)

but not chosen as the reference state, we define its deviation from the reference λi as ∆iu := λiu − λi. The corresponding dynamics are given by 

˙∆iu = ˙λiu − ˙λi = gi(z) − gi(z) = 0 . (10) In other words, all the ∆iu(t) remain constant over time. If 

u is selected as the reference state, ∆iu(t) = 0 . Additionally, 

∆v (t) is a stack of all ∆iv (t), i ∈ mv with proper order. 

Lemma 2. For any given set of ∆v (0) for all v, there exists a unique equilibrium point. However, agents are not aware of this equilibrium because they do not know their respective 

∆v (0) . Moreover, every equilibrium point of the Algorithm 1 satisfies the KKT conditions (4) of the GNEP (2). Lemma 2 states that if a central authority has knowledge of all agents’ initial values, λv (0) , as well as their cost functions and constraints, it can predict the equilibrium point because there is only one possible equilibrium once ∆v (0) is set. However, individual agents cannot predict the equilibrium, as they have access to neither other agents’ λu nor their cost functions and constraints. Moreover, even though the central authority can predict where the equilibrium will be, this does not guarantee that the algorithm will actually converge to that point. Given that the entire algorithm operates as a PDS, we establish the subsequent Lemma 3. 

Lemma 3. Define λ(t), ∆v (t) as in (10). Consider a positive semi-definite function V defined on Rn × Rp × Rq

> ≥0

,

V (z, λ, μ ):= 12

" NX

> v=1

 ∥zv − z⋆v ∥2 + ∥μv − μ⋆v ∥2 + ∥λ − λ⋆∥2

#,

(11) 

where (z⋆, λ ⋆, μ ⋆v ) is an equilibrium point as in Lemma 2. Then the Lie derivative of the function (11) along the trajectory L V ≤ 0 for all z, λ, and μv .

Using Lemma 3 and following a similar argument in Lemma 4.3 of [20], along with Lemma 1, we derive Lemma 4, ensuring the uniqueness and continuity of the trajectory. With this result and the fact that the Lie derivative is non-positive in Lemma 3, we are prepared to establish the convergence of Algorithm 1 in Theorem 1. 

Lemma 4. Starting from any point (z, λ, μ ) ∈ Rn ×

Rp × Rq

> ≥0

, a unique trajectory t 7 → γ(t) to the Algorithm 1 exists and remains within the set 



Rn × Rp × Rq

> ≥0



∩

V −1(≤ V (z, λ, μ )) . Moreover, if a sequence of points 

{(zk, λ k, μ k)}∞ 

> k=1

⊂ Rn ×Rp ×Rq 

> ≥0

converge to (z, λ, μ ) as 

k → ∞ , then the sequence of trajectories {t 7 → γk(t)}∞

> k=1

starting at these points converge uniformly to the trajectory 

t 7 → γ(t) on every compact set of [0 , ∞).

Theorem 1. The set of equilibrium points of Algorithm 1 is globally stable, and every trajectory converges to a GNE. Remark 1. If all agents initialize with identical multipliers (e.g., set to zero by default or centrally), they can converge to the same v-GNE without exchanging multiplier information. We see that our approach can recover the v-GNE as existing schemes, as a special case. 

B. Discretization for Discrete-Time Algorithms 

Although the continuous-time algorithm has elegant theo-retical properties, it must be discretized for implementation. Discretization, however, introduces approximation errors and often requires additional conditions to preserve the behavior of the continuous dynamics. Accordingly, we develop two discretization schemes for Algorithm 1, each with its own assumptions determined by the chosen technique. In addition to the standard Assumptions 1–3, each scheme requires at least one additional assumption to guarantee convergence. 

Assumption 4 (Additional Assumptions) . The discretized algorithms require one or more of the following assumptions: 1) The pseudo-gradient in (5) is globally Lipschitz with constant Lf .2) For every agent v and any zv , z ′ 

> v

∈ Rnv ,

∥hv (zv ) − hv (z′ 

> v

)∥2 ≤ Lv ∥zv − z′ 

> v

∥2.

3) The individual constraint set Zv := {zv | hv (zv ) ≤ 0}

is compact for all agents. Based on [21], Algorithm 2 provides a discrete version of the continuous algorithm using a standard diminishing step size. Specifically, we choose a step size αk, where k denotes the iteration index, satisfying: 

> ∞

X

> k=1

αk = ∞,

> ∞

X

> k=1

(αk)2 = Γ < ∞. (12) It is important to note that this step size is independent of agents’ private data and can therefore be chosen without accessing such information. To establish convergence, we require a deterministic version of the almost supermartingale convergence lemma from [29]. 

Algorithm 2 Forward Discretization for Each Agent v 

> 1:

Input: Initial z0 

> v

, λ0 

> v

, μ0 

> v

≥ 0, αk. 

> 2:

for k = 1 , 2, . . . , K do  

> 3:

Receive the decision variables z−v from other agents through the cost-function graph Gf and the shared constraint graph Gg . 

> 4:

z+ 

> v

← zv − αk · ∇ zv Lv (z, λ v , μ v ). 

> 5:

λv ← λv + αk · gv (zv , z−v ). 

> 6:

μjv ← P≥0

μjv + αk · hjv (zv ), for j ∈ [qv ]. 

> 7:

zv ← z+ 

> v

. 

> 8:

end for Theorem 2. With a diminishing step size as in (12), and under the additional Assumptions 4 (1) and (2), the sequence 

zk generated by Algorithm 2 converges to a fixed point z⋆,which is a GNE. 

Theorem 2 shows that a diminishing step size yields a valid discretization of Algorithm 1, ensuring convergence under additional assumptions on the GNEP. This also sug-gests that other discretizations, such as Euler with a constant step size, may be suitable for GNEPs with different proper-ties. Beyond forward discretization, backward (resolvent) or hybrid schemes can also be used. Algorithm 3 illustrates a forward–backward hybrid scheme (similar in spirit to Runge–Kutta methods), in which the multiplier step size α

must be chosen globally, i.e., all agents share the same α

even if their initial multipliers differ. 

Theorem 3. Define the inverse learning-rate matrices as 

τ := diag ([ τ −11 , . . . , τ −1 

> N

]) , σ := α−1Ip×p,

where Ip×p is the identity matrix of size p. If 

min {λmin (τ ), λ min (σ)} > Lf 

> 2δ

+ ∥A∥2,where λmin (·) denotes the smallest eigenvalue, and Assump-tions 4 (1) and (3) hold, then Algorithm 3 converges to a fixed point corresponding to a GNE of the GNEP (2) .

Algorithm 3 Forward-Backward for Each Agent v 

> 1:

Input: Initial z0 

> v

, λ0 

> v

, τv , σ. 

> 2:

for k = 1 , 2, . . . , K do  

> 3:

Receive the decision variables z−v from other agents through the cost-function graph Gf and the shared constraint graph Gg .

> 4:

z+ 

> v

← PZv

n

zv − τv · ∇zv fv (zv , z −v )+ λ⊤ 

> v

∇zv gv (zv , z−v )o

. 

> 5:

Receive the updated decision variables z+ 

> −v

from other agents through the shared-constraint graph Gg .

> 6:

λv ← λv + α · 2 · gv (z+ 

> v

, z+ 

> −v

) − gv (zv , z−v )+ 2 · c(mv ). 

> 7:

zv ← z+ 

> v

. 

> 8:

end for 

IV. M ULTI -ROBOTS PLACEMENT PROBLEM 

We apply our fully distributed algorithm to a multi-robot placement problem. Multiple agents (robots) act as simple integrators and collaborate to complete shared tasks. Each task j has a center location cj ∈ R2 and an associated set of robots Nj with size nj . A robot v may be assigned to multiple tasks, each with a different center. This setup induces global shared constraints: for each task 

j, the positions of all assigned robots must collectively center at cj . Mathematically, this is represented as gjv (zv , z−v ) = P 

> v∈Nj

zv − nj · cj = 0, where zv = (xv , y v ) denotes the placement of robot v. Let Mv be the set of tasks involving robot v. Then gv (zv , z−v ) is the collection of all constraints gjv (zv , z−v ) for j ∈ Mv . Importantly, while a global constraint set g(z) exists, each robot only has access to its local constraints gv (zv , z−v ), not the full global set. In addition to task locations, each robot v has an anchor point Av , representing its charging or control station. Each robot must remain within a range centered at Av , where both the radius and location depend on the robot’s capabilities. Formally, this local constraint is ∥zv − Av ∥ ≤ rv , which depends only on robot v and is independent of other robots’ positions. For the objective function, each robot balances two goals: staying close to its anchor point and staying close to other robots u ∈ Nj for tasks j ∈ Mv to enable coordination. To capture this trade-off, each agent’s cost function includes a private penalty parameter ρv . Specifically, the cost function is defined as: 

fv (zv , z−v ) = ∥zv −Av ∥2 +ρv

X 

> u∈Nj,j ∈Mv

∥zv −zu∥2. (13) This leads to the following GNEP with convex individual constraints and linear shared constraints: 

min zv fv (zv , z−v )

subject to ∥zv − Av ∥ ≤ rv

gv (zv , z−v ) = 0 

.

Figure 1 illustrates a setup with 12 robots and 4 tasks. Our algorithm is fully distributed: each robot keeps its constraint and cost information locally and does not exchange multi-pliers. Since GNEs depend on initialization, Figure 2 shows different equilibria reached by the continuous algorithm 1 under varying initial conditions. This non-uniqueness high-lights both the difficulty of computing such equilibria, even with centralized solvers, and the contribution of our method. Figure 3 compares the convergence of our forward– backward discretization algorithm 3 with a consensus-based method adapted from [16]. Both show similar convergence experimentally, but our method requires less communication per iteration: since Gf coincides with Gg , information ex-change is reduced by two-thirds per iteration. 123456 78910 11 12 C1 C2 C3 C4               

> Fig. 1. Multi-robot placement with 12 robots and 4 tasks. Circles denote robots v, rectangles their anchor points Av, and dotted boxes local constraint regions. Yellow stars cjmark task locations. Arrows indicate assignments (v∈Nj,j∈Mv), forming a bipartite robot–task structure.
> Fig. 2. Converging trajectories from six random initializations (seeds 0–5). “x” marks the corresponding convergence points, all of which are GNEs. Step Norm ( log  𝐳 ! − 𝐳 ⋆ )Iteration
> Consensus-Based Algorithm 3

Fig. 3. Comparison of Algorithm 3 (forward–backward discretization) with consensus-based approaches from [16]. Both are initialized at zero with learning rate 0.01 , converging to a v-GNE. 

V. C ONCLUSION 

We proposed a fully distributed continuous-time algorithm for solving GNEPs without multiplier consensus. By elim-inating multiplier exchange, the method reduces commu-nication overhead while converging to the broader set of GNEs; v-GNEs can still be recovered by identical multiplier initialization. Discrete-time variants were also introduced and validated on a multi-robot placement problem with shared constraints. Future work includes extending the analysis to GNEPs with global inequality constraints, where formal convergence guarantees remain open, and studying how sparsity and graph structure affect communication efficiency, in particular whether Gg can be chosen sparser than Gf and what the sparsest feasible communication graph is, especially in dynamic networks where agents may join or leave. REFERENCES [1] L. Marris, I. Gemp, T. Anthony, A. Tacchetti, S. Liu, and K. Tuyls, “Turbocharging solution concepts: Solving nes, ces and cces with neu-ral equilibrium solvers,” in Advances in Neural Information Processing Systems , S. Koyejo, S. Mohamed, A. Agarwal, D. Belgrave, K. Cho, and A. Oh, Eds., vol. 35. Curran Associates, Inc., 2022, pp. 5586– 5600. [2] J. Hu and M. P. Wellman, “Nash q-learning for general-sum stochastic games,” J. Mach. Learn. Res. , vol. 4, no. null, p. 1039–1069, dec 2003. [3] M. L. Littman, “Markov games as a framework for multi-agent rein-forcement learning,” Machine Learning Proceedings 1994 , p. 157–163, 1994. [4] Y. Yang, R. Luo, M. Li, M. Zhou, W. Zhang, and J. Wang, “Mean field multi-agent reinforcement learning,” in Proceedings of the 35th International Conference on Machine Learning , ser. Proceedings of Machine Learning Research, J. Dy and A. Krause, Eds., vol. 80. PMLR, 10–15 Jul 2018, pp. 5571–5580. [5] I. Goodfellow, J. Pouget-Abadie, M. Mirza, B. Xu, D. Warde-Farley, S. Ozair, A. Courville, and Y. Bengio, “Generative adversarial net-works,” Commun. ACM , vol. 63, no. 11, p. 139–144, oct 2020. [6] C. Jin, P. Netrapalli, and M. Jordan, “What is local optimality in nonconvex-nonconcave minimax optimization?” in Proceedings of the 37th International Conference on Machine Learning , ser. Proceedings of Machine Learning Research, H. D. III and A. Singh, Eds., vol. 119. PMLR, 13–18 Jul 2020, pp. 4880–4889. [7] D. Goktas and A. Greenwald, “Convex-concave min-max stackelberg games,” in Advances in Neural Information Processing Systems ,M. Ranzato, A. Beygelzimer, Y. Dauphin, P. Liang, and J. W. Vaughan, Eds., vol. 34. Curran Associates, Inc., 2021, pp. 2991–3003. [8] I. Tsaknakis, M. Hong, and S. Zhang, “Minimax Problems with Coupled Linear Constraints: Computational Complexity and Duality,” 

SIAM Journal on Optimization , vol. 33, no. 4, pp. 2675–2702, Dec. 2023. [9] F. Pedregosa, “Hyperparameter optimization with approximate gradi-ent,” in Proceedings of The 33rd International Conference on Machine Learning , ser. Proceedings of Machine Learning Research, M. F. Balcan and K. Q. Weinberger, Eds., vol. 48. New York, New York, USA: PMLR, 20–22 Jun 2016, pp. 737–746. [10] G. Debreu, “A Social Equilibrium Existence Theorem*,” Proceedings of the National Academy of Sciences , vol. 38, no. 10, pp. 886–893, Oct. 1952. [11] J. B. Rosen, “Existence and Uniqueness of Equilibrium Points for Concave N-Person Games,” Econometrica , vol. 33, no. 3, p. 520, July 1965. [12] M. Fukushima, “Restricted generalized Nash equilibria and controlled penalty algorithm,” Computational Management Science , vol. 8, no. 3, pp. 201–218, Aug. 2011. [13] F. Facchinei and C. Kanzow, “Penalty Methods for the Solution of Generalized Nash Equilibrium Problems,” SIAM Journal on Optimiza-tion , vol. 20, no. 5, pp. 2228–2253, Jan. 2010. [14] C. Kanzow and D. Steck, “Augmented Lagrangian Methods for the Solution of Generalized Nash Equilibrium Problems,” SIAM Journal on Optimization , vol. 26, no. 4, pp. 2034–2058, Jan. 2016. [15] A. Dreves and C. Kanzow, “Nonsmooth optimization reformulations characterizing all solutions of jointly convex generalized Nash equi-librium problems,” Computational Optimization and Applications ,vol. 50, no. 1, pp. 23–48, Sept. 2011. [16] P. Yi and L. Pavel, “An operator splitting approach for distributed generalized Nash equilibria computation,” Automatica , vol. 102, pp. 111–121, Apr. 2019. [17] C. Cenedese, G. Belgioioso, S. Grammatico, and M. Cao, “An asynchronous distributed and scalable generalized Nash equilibrium seeking algorithm for strongly monotone games,” European Journal of Control , vol. 58, pp. 143–151, Mar. 2021. [18] Y. Huang and J. Hu, “Distributed Solution of GNEP over Networks via the Douglas-Rachford Splitting Method,” in 2021 60th IEEE Conference on Decision and Control (CDC) . Austin, TX, USA: IEEE, Dec. 2021, pp. 3110–3116. [19] M. Bianchi and S. Grammatico, “Continuous-time fully distributed generalized Nash equilibrium seeking for multi-integrator agents,” 

Automatica , vol. 129, p. 109660, July 2021. [20] A. Cherukuri, E. Mallada, and J. Cort´ es, “Asymptotic convergence of constrained primal–dual dynamics,” Systems & Control Letters ,vol. 87, pp. 10–15, Jan. 2016. [21] K. Ebrahimi, U. Vaidya, and N. Elia, “Robust Optimization via Discrete-Time Saddle Point Algorithm,” in 2019 IEEE 58th Confer-ence on Decision and Control (CDC) . Nice, France: IEEE, Dec. 2019, pp. 2473–2478. [22] F. Facchinei and C. Kanzow, “Generalized Nash Equilibrium Prob-lems,” Annals of Operations Research , vol. 175, no. 1, pp. 177–211, Mar. 2010. [23] P. T. Harker, “Generalized Nash games and quasi-variational inequal-ities,” European Journal of Operational Research , vol. 54, no. 1, pp. 81–94, Sept. 1991. [24] F. Facchinei, A. Fischer, and V. Piccialli, “On generalized Nash games and variational inequalities,” Operations Research Letters , vol. 35, no. 2, pp. 159–164, Mar. 2007. [25] F. Facchinei and J.-S. Pang, Finite-Dimensional Variational Inequali-ties and Complementarity Problems , ser. Springer Series in Operations Research and Financial Engineering. New York, NY: Springer-Verlag New York, Inc, 2004. [26] J. Zhou, W. H. Lam, and B. G. Heydecker, “The generalized Nash equilibrium model for oligopolistic transit market with elastic de-mand,” Transportation Research Part B: Methodological , vol. 39, no. 6, pp. 519–544, July 2005. [27] A. Maiorano, Y. Song, and M. Trovato, “Dynamics of non-collusive oligopolistic electricity markets,” in 2000 IEEE Power Engineering So-ciety Winter Meeting. Conference Proceedings (Cat. No.00CH37077) ,vol. 2. Singapore: IEEE, 2000, pp. 838–844. [28] A. Bacciotti and F. Ceragioli, “Nonpathological Lyapunov functions and discontinuous Carath´ eodory systems,” Automatica , vol. 42, no. 3, pp. 453–458, Mar. 2006. [29] H. Robbins and D. Siegmund, “A convergence theorem for non-negative almost supermartingales and some applications,” in Optimiz-ing Methods in Statistics , J. S. Rustagi, Ed. Academic Press, 1971, pp. 233–257.