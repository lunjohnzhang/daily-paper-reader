---
title: Bi-Layer Ant Colony Optimization for Multi-Robot Task Allocation and Routing in Delivery Applications
title_zh: 双层蚁群优化算法在配送应用中的多机器人任务分配与路径规划
authors: "Le Na Nguyen, Thanh Long Nguyen, Thanh Thao Ton Nu, Quan Le, Manh Duong Phung"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17416v1"
tags: ["query:multi-robot"]
score: 9.0
evidence: 蚁群优化求解多机器人任务分配与路径规划，直接对应进化优化用于多机器人任务分配
tldr: "针对配送场景中的多机器人任务分配与路径规划问题，提出将两者统一建模为联合优化问题，并设计双层蚁群算法，在同一种群内交织决策实现同步求解。实验表明，相比MILP和PSO，该方法在所有任务规模下总行驶距离最短、完成时间最快，距离最多降低17.7%，时间缩短近20%，验证了其高效性与可扩展性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17416v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 717, \"height\": 1034, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17416v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1834, \"height\": 585, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17416v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 734, \"height\": 454, \"label\": \"Table\"}]"
motivation: 现有方法割裂任务分配与路径规划，导致整体效率下降，需要统一优化框架。
method: 定义新成本函数将MRTA转化为统一优化问题，提出单种群内双层决策的蚁群算法协同求解。
result: "相比MILP和PSO，总行驶距离最多降低17.7%，完成时间缩短近20%，且保持最优。"
conclusion: 双层ACO高效、可扩展且可靠，适用于多机器人配送任务的实际部署。
---

## 摘要
本文针对多机器人任务分配（MRTA）问题展开研究，该问题对于配送和物流应用至关重要。我们的方法首先定义了一个新的成本函数，将MRTA转化为一个统一的优化问题，同时涵盖任务分配与路径规划。随后，提出了一种双层蚁群优化（ACO）算法，在单一蚁群过程中整合两个相互依赖的决策层以求解该问题。这一分层框架使得多机器人间的任务分配与路径规划能够同步优化。与混合整数线性规划（MILP）和粒子群优化（PSO）的对比实验表明，所提出的双层ACO在各种任务规模下均能实现最短的总行驶距离和最快的完成时间。具体而言，与基线方法相比，总行驶距离最多可缩短17.7%，完成时间最多可缩短近20%。这些结果证实了所提出的双层ACO在多机器人配送任务中的高效性、可扩展性和可靠性。

## Abstract
This paper addresses the multi-robot task allocation (MRTA) problem, which is essential for delivery and logistics applications. Our approach first defines a new cost function that transforms the MRTA into a unified optimization problem capturing both task assignment and routing. A bi-layer ant colony optimization (ACO) algorithm is then introduced, integrating two interdependent decision layers within a single colony process to solve the problem. This hierarchical framework enables simultaneous optimization of task allocation and route planning across multiple robots. Comparative experiments with mixed-integer linear programming (MILP) and particle swarm optimization (PSO) demonstrate that the proposed bi-layer ACO achieves the shortest total travel distance and fastest completion time across all task sizes. Specifically, it reduces total travel distance by up to 17.7% and completion time by nearly 20% compared with baseline methods. These results confirm the efficiency, scalability, and reliability of the proposed bi-layer ACO for multi-robot delivery tasks.

---

## 论文详细总结（自动生成）

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明即可）

### 2.1 核心思想

- **统一建模**：将 MRTA 表述为同时优化任务分配和任务排序的联合优化问题，克服传统方法将二者分离导致的次优问题。
- **双层决策**：在单一蚁群中维护两套信息素矩阵，分别驱动“任务-机器人分配”和“任务间转移排序”，两类决策在蚁群迭代中相互耦合、同步优化。
- **单一种群协同**：每只蚂蚁在构造解时依次完成分配层和排序层，并通过共享的成本函数同时影响两层的信息素更新，形成联合寻优闭环。

### 2.2 关键技术细节

- **目标函数**（式4-5）：

  \[
  \min_{x, \pi} \sum_{i \in R} D_i(x, \pi_i)
  \]

  其中 \(D_i\) 包含三段距离：起点到第一个取货点、每个任务取货点到送货点、上一任务送货点到下一任务取货点。

- **约束条件**（式6-7）：

  - 每个任务必须且只能分配给一个机器人；
  - 每个机器人的任务序列必须是其被分配任务集合的一个有效排列。

- **双层信息素结构与启发式**（式11）：

  - 分配层：\(\tau^{\text{assign}}_{i,j}\) 表示任务 \(j\) 分配给机器人 \(i\) 的期望度；启发式 \(\eta^{\text{assign}}_{i,j} = 1/(d(s_i, p_j) + \epsilon)\) 反映机器人起点到任务取货点的距离。
  - 排序层：\(\tau^{\text{seq}}_{j,k}\) 表示在同一个机器人序列中任务 \(j\) 后衔接任务 \(k\) 的期望度；启发式 \(\eta^{\text{seq}}_{j,k} = 1/(d(d_j, p_k) + \epsilon)\) 反映前一任务送货点到后一任务取货点的距离。

- **构建解的过程**：

  - **分配层**：每个任务按概率式（12）选择机器人，信息素浓度高且距离近的机器人更易被选中；
  - **排序层**：每个机器人先从自身未完成任务中选取取货点距离起点最近的任务作为首任务（式13），之后按式（14）的概率依次选择后续任务，直到该机器人任务集为空。

- **信息素更新**（式15-16）：

  - 分配层更新：蒸发项 + 所有蚂蚁中“任务 \(j\) 分配给机器人 \(i\)”的强化项，权重为 \(Q/C(a)\)；
  - 排序层更新：蒸发项 + 所有蚂蚁中“任务 \(j\) 后紧跟任务 \(k\)”的强化项，遍历每只蚂蚁的每个机器人序列中的连续任务对。

- **算法流程**（Algorithm 1）：

  1. 初始化两套信息素矩阵和启发式矩阵；
  2. 对每次迭代：
      - 每只蚂蚁依次执行分配层（逐个任务选择机器人）和排序层（逐个机器人构造任务序列）；
      - 计算总成本并更新全局最优解；
      - 更新两层信息素矩阵；
  3. 返回最优分配矩阵、最优任务序列和最小总成本。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

### 3.1 场景设置

- **仿真平台**：ROS 2 Humble + Gazebo。
- **环境规模**：10 m × 15 m，含多个静态障碍物（图1a）；图1b为机器人1的 LiDAR 构建的代价地图。
- **机器人平台**：3台同构 TurtleBot3，配备2D LiDAR。
- **初始位置**：固定为 (0, 0)、(5, 0)、(0, 4)。
- **任务规模**：5、10、20 个任务，每个任务的取货点和送货点随机生成。
- **导航执行**：将算法输出的有序航点列表（取货/送货点）交由修改后的 Nav2 ROS 2 包执行自主导航。

### 3.2 Benchmark 对比方法

- **MILP**（混合整数线性规划）[3, 5]：精确优化基准。
- **PSO**（粒子群优化）[1, 7]：元启发式基准。
- **Baseline 选择理由**：分别代表精确方法和经典群智能方法，覆盖不同优化范式，便于评估双层ACO的相对优势。

### 3.3 评价指标

- 总行驶距离（米）
- 总完成时间（秒）
- 每次实验重复10次，记录均值与标准差。

## 4. 资源与算力

- **文中没有提及** 所使用的 GPU、CPU 型号或数量、训练/迭代时长等硬件细节。
- 仅能确认实验在 **仿真环境**（ROS 2/Gazebo）中完成，属于轻量级导航与任务分配任务，未涉及深度学习训练。因此，算力需求很低，但论文未说明具体硬件规格和单次运行耗时。

## 5. 实验数量与充分性

### 5.1 已进行的实验

- **3种任务规模**（5、10、20），**每种规模下对比3种算法**（ACO、PSO、MILP），每种算法各跑 **10次**，共 9 组实验组，含均值与标准差。
- 提供了总行驶距离的箱线图（图2）和表1的数据。
- 未提供消融实验（如去掉双层机制、单独使用分配层或排序层的对比）、大规模任务（>20）、异构机器人、动态障碍物等实验。

### 5.2 充分性评估

- **优点**：在给定任务规模下重复10次并报告标准差，具有一定统计可信度；三种对比方法具有代表性。
- **不足**：实验规模较小（最多20任务），未涉及真实场景或更复杂的室外环境；缺乏消融实验来验证双层设计的单独贡献；未检验算法在任务距离分布不均匀、时间窗约束、机器人故障等情况下的鲁棒性。总体而言，作为概念验证充分，但作为全面性能评估略显单薄。

## 6. 论文的主要结论与发现

- 在5任务场景下，ACO 相比 MILP 总行驶距离降低约 **17.7%**，相比 PSO 降低约 **9.8%**；完成时间最多缩短近 **20%**。
- 在10和20任务场景下，ACO 仍保持最低的平均距离和时间，且标准差相似或更低，说明其收敛稳定性和可扩展性优于基线。
- 双层ACO 将任务分配与路径规划统一建模，在单一蚁群过程中同步优化，避免了分阶段优化的割裂问题，有效减少冗余行驶。
- 作者总结认为，该方案在仿真环境中验证了高效性、可扩展性和可靠性，适合多机器人配送任务。

## 7. 优点

- **问题建模新颖且统一**：将任务分配与路径规划耦合为单一目标函数，避免了传统两阶段方法的次优性。
- **算法设计精巧**：双层信息素矩阵与双层启发式在同一种群内交互，结构清晰，易于实现和扩展。
- **实验对比合理**：同时包含精确方法（MILP）和经典元启发式（PSO），并用均值、标准差和箱线图给出统计信息。
- **结果一致性强**：在所有任务规模下均稳定优于基线，且方差并未显著增大，说明算法鲁棒性较好。
- **应用落地性强**：在 ROS 2/Gazebo 仿真环境中使用真实机器人模型（TurtleBot3）和标准导航栈（Nav2），贴近实际部署流程。

## 8. 不足与局限

- **实验规模受限**：最多仅20个任务、3台机器人，未测试更大规模场景，无法充分展示算法在大规模 MRTA 中的扩展性。
- **缺乏消融实验**：没有单独验证分配层和排序层各自的贡献，以及双层协同增益的来源。
- **环境单一**：只有一种静态障碍物布局，未考虑动态障碍物、多楼层、室内外混合等真实配送环境。
- **任务模型简化**：假设所有机器人同构、任务无时间窗、无优先级、无电量约束，实际配送场景中这些因素常不可忽略。
- **对异构性和动态性处理不足**：文章结果部分仅报告静态环境下的性能，未涉及任务动态到达、机器人故障重分配等灵活性问题。
- **计算效率未量化**：没有报告算法运行时间或收敛迭代次数，无法判断其在线应用的实时性。
- **基线的公平性存疑**：MILP 作为精确方法通常用于小规模问题，在20任务时可能因求解时间限制而使用次优解；论文未说明 MILP 的求解时限或最优性间隙，可能影响对比公平性。

（完）
