---
title: "MixedComplementarityProblems.jl: A Fast, Batched, Open-Source Interior Point Solver for Mixed Complementarity Problems"
title_zh: MixedComplementarityProblems.jl：用于混合互补问题的快速、批处理、开源内点法求解器
authors: David Fridovich-Keil
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.00959v1"
tags: ["query:multi-robot"]
score: 7.0
evidence: 开源混合互补问题求解器，支撑机器人多智能体轨迹优化
tldr: 混合互补问题广泛出现于机器人多智能体轨迹优化中，现有主流求解器PATH虽性能强但闭源。本文提出开源Julia求解器MixedComplementarityProblems.jl，采用内点法，支持CPU多线程与GPU批处理及自动微分。在标准基准上可靠性媲美PATH；多智能体换道博弈中，CPU批处理比PATH顺序快约100倍，GPU在大规模KKT系统中表现出优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.00959v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00959v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 913, \"height\": 351, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00959v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 789, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00959v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1554, \"height\": 632, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.00959v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00959v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 937, \"height\": 311, \"label\": \"Table\"}]"
motivation: PATH求解器在机器人领域表现优异但闭源，且不支持批处理和参数微分，缺乏开源高效替代方案。
method: 设计参数化MCP的内点法求解器，通过抽象线性代数后端，同一实现可运行于稠密、批稀疏和单大规模后端，支持批并行与自动微分。
result: 在标准基准上可靠性等同PATH；换道博弈中CPU批处理比PATH顺序调用快约100倍，GPU在KKT系统规模大时更优。
conclusion: 提供开源、快速、可批处理的MCP求解器，支持自动微分，为机器人规划和博弈问题提供高效工具。
---

## 摘要
混合互补问题（MCPs）作为非线性规划和非合作博弈的一阶最优性条件出现，并为机器人学中出现的多智能体轨迹优化问题提供了自然的表述形式。此类问题的主要求解器是PATH，它在机器人问题上表现出色，但仍然是闭源的。我们提出了MixedComplementarityProblems.jl，一个开源的、纯Julia实现的参数化MCPs内点法，它：(i) 在标准基准测试上与PATH的可靠性相匹配，(ii) 原生支持对许多参数实例进行批处理、并行处理，无论是在CPU线程上还是在NVIDIA GPU上，(iii) 支持对问题参数进行解的高效自动微分。在一个代表机器人规划问题的多智能体变道轨迹博弈中，我们的CPU多线程批处理求解器处理完一批参数实例比顺序调用PATH快约100倍。GPU后端，运行相同的求解器实现而无需修改，也比PATH更快地处理完这些批次，但在此问题上并未超过多线程CPU；只有当每个实例的KKT系统变大时，GPU才会领先，我们描述了这种性能表现的机制依赖。我们描述了求解器的内点法表述，以及一种抽象，使单个求解器实现能够在密集、批量稀疏和单一大线性代数后端之间无需修改地运行，并报告了在随机生成的二次规划和轨迹博弈上对PATH的基准测试结果。

## Abstract
Mixed complementarity problems (MCPs) arise as the first-order optimality conditions of nonlinear programs and noncooperative games, and provide a natural formulation for multi-agent trajectory optimization problems that appear throughout robotics. The dominant solver for problems of this form is PATH, which offers strong performance on robotics problems but remains closed-source. We present MixedComplementarityProblems.jl, an open-source, pure Julia implementation of an interior point method for parametric MCPs that: (i) matches PATH's reliability on standard benchmarks, (ii) natively supports batched, parallel processing of many parameter instances, either across CPU threads or on an NVIDIA GPU, and (iii) supports efficient automatic differentiation of solutions with respect to problem parameters. On a multi-agent lane-change trajectory game representative of robotics planning problems, our CPU-multithreaded batched solver clears a batch of parametric instances ~100x faster than sequential calls to PATH. A GPU backend, running the same solver implementation unmodified, also clears these batches far faster than PATH, but does not outperform the multithreaded CPU on this problem; the GPU pulls ahead only once each per-instance KKT system grows large, and we characterize this regime dependence. We describe the solver's interior point formulation, the abstraction that lets a single solver implementation run unmodified across dense, batched-sparse, and single-large linear-algebra backends, and report benchmarks against PATH on both randomly generated quadratic programs and trajectory games.