Title: RIT*: Riemannian Informed Trees for Cost-Adaptive Optimal Motion Planning

URL Source: https://arxiv.org/pdf/2608.00822v1

Published Time: Tue, 04 Aug 2026 01:00:52 GMT

Number of Pages: 8

Markdown Content:
> IEEE ROBOTICS AND AUTOMATION LETTERS 1

# RIT*: Riemannian Informed Trees for Cost-Adaptive Optimal Motion Planning 

Muhayy Ud Din 1, Ahmed Nadar 1, Jan Rosell 2, and Irfan Hussain 1∗

Abstract —We present Riemannian Informed Trees (RIT*), a planning framework that replaces Euclidean primitives in batch-informed search with their Riemannian counterparts. RIT* constructs a tighter, cost-consistent informed set, performs a nearest-neighbour search under an anisotropic distance metric, and evaluates edge costs efficiently via a cascading scheme. We further introduce a Collision-Adaptive Metric Refinement (CARM), which learns an obstacle-proximity cost field online from collision feedback, reducing the reliance on prior metric de-sign in practical settings. Experiments across environments from 2-D to 14-D show that RIT* is competitive in low-dimensional and spatially constant-metric settings and produces substantially lower-cost solutions when the metric varies spatially in high-dimensional configuration spaces. Performance gains scale with anisotropy and dimension, reaching up to 13.0% improvement in median initial cost over BIT* in the 3-D anisotropic benchmark, up to 9.0% in median final cost over BIT* in 6-DOF manipula-tion, and 24.8-63.5% in a 14-DOF bimanual planning problem, where Euclidean-informed baselines degrade. Videos and code can be found here: https://muhayyuddin.github.io/ritstar/. 

Index Terms —Motion planning, Riemannian geometry, asymp-totic optimality, informed sampling, online metric learning. 

I. I NTRODUCTION 

Sampling-based motion planning enables collision-free path computation in high-dimensional configuration spaces [1]. Asymptotically optimal (AO) planners such as RRT* [2] and PRM* converge to an optimal path, and later work improved convergence via informed sampling : once an initial solution is found, samples are drawn solely from the informed set ,a prolate hyperspheroid in Euclidean space [3]. This idea underpins Informed RRT* [3], BIT* [4], AIT*, EIT* [5], and APT* [6]. These planners, however, select this set using Euclidean distance, which does not reflect the true cost structure in anisotropic environments. In addition, they typically rely on a predetermined cost structure rather than learning it from planning experience, reducing their effectiveness when the cost landscape is unknown or environment-dependent. In this work, we address both limitations with RIT*. We replace Euclidean components with a Riemannian formu-lation, where a spatially varying metric tensor G(x) cap-tures the underlying cost structure. We further introduce a 

This research was supported by the Center for Autonomous Robotic Systems, Khalifa University (KU-CARS), through the project “T2FS by Silal”, under Project ID: KU-EXT-SILAL-2025-8475000023 and un-der Project PID2024-157729OB-I00 funded by MICIU/AEI/10.13039/ 501100011033/FEDER, UE. 

> 1

Muhayy Ud Din, Ahmed Nadar, and Irfan Hussain are with Center for Autonomous Robotic Systems, Khalifa University (KU-CARS). 

> 2

Jan Rosell is with the Institute of Industrial and Control Engineering, Universitat Politècnica de Catalunya, Barcelona, Spain.  

> ∗

e-mail: irfan.hussain@ku.ac.ae 

Fig. 1: Overview of RIT*. (a) BIT* samples from the Eu-clidean informed set IE , ignoring anisotropic cost structure. 

(b) RIT* uses a cost-aligned Riemannian informed set; CARM (red shading) learns a cost field from collision feedback to guide paths away from obstacles. Collision-Adaptive Metric Refinement (CARM), which learns a collision-induced conformal cost field while planning from collision feedback and adapts the planner accordingly (Fig. 1). 

Contributions: The key contributions are: 1) Riemannian informed planning: RIT* replaces Eu-clidean components with Riemannian ones: the informed set aligns with the underlying cost structure, nearest neighbours use an anisotropic metric, and edge costs are computed via a cascading scheme, focusing sampling on relevant regions and improving efficiency. 2) Online proximity-aware metric refinement via CARM: 

CARM learns an obstacle-proximity cost field from collision feedback and applies it as a conformal scaling of a base metric, requiring no prior obstacle knowledge: given a base metric (e.g., joint-space inertia) it adds proximity-aware structure; without one, it defaults to Euclidean and learns from scratch. 3) Experimental validation: We evaluate RIT* from 2-D/3-D to high-dimensional tasks (6-DOF, 14-DOF), including a real UR10e arm, with a comprehensive benchmark against asymptotically optimal planners. II. R ELATED WORK 

We review prior work on informed sampling-based plan-ning, the use of Riemannian geometry in motion planning, and obstacle-aware cost design. 

Euclidean informed sampling planners: Sampling-based planners have been improved by restricting search to an informed subset of the space. Gammell et al. [3] introduced direct sampling of a prolate hyperspheroid to accelerate RRT*. BIT* [4] combines batch sampling with informed sets and lazy edge evaluation, while AIT* and EIT* [5] extend this with asymmetric bidirectional search. APT* [6] shapes nearest-neighbour search using virtual forces and adapts batch size, arXiv:2608.00822v1 [cs.RO] 1 Aug 2026 IEEE ROBOTICS AND AUTOMATION LETTERS 2

and FIT* [7] improves early convergence through batch adap-tation. Despite these advances, all define the informed set, nearest-neighbour search, and edge cost in Euclidean distance. 

Riemannian geometry in motion planning. Riemannian ge-ometry naturally models anisotropic costs, but its use is largely limited to local or optimisation methods. CHOMP [8] and STOMP [9] optimise trajectories under such costs, but are not sampling-based and lack AO guarantees. In sampling-based planning, Kim et al. [10] use Riemannian metrics for local steering, and Kyaw and Kelly [11] propose a Riemannian RRT* using retractions and natural gradients. However, these methods are incremental, do not construct informed sets, and do not address global sampling. Bi-AM-RRT* [12] similarly biases nearest-neighbour selection via a diffusion-based as-sisting metric, but does not integrate informed sampling or batch processing, its metric serves as a search heuristic for fast (re)planning in dynamic 2-D navigation rather than as the cost functional being optimised. The cost-space RRT lit-erature, notably T-RRT on configuration-space costmaps [13], biases exploration through stochastic transition tests over a scalar cost field. These planners handle general costmaps but retain Euclidean distances, are not informed, and provide no asymptotic-optimality guarantees.. 

Obstacle-aware Riemannian metrics. Several works incor-porate obstacle information into metric design but rely on prior knowledge or demonstrations: Klein et al. [14] design barrier-based metrics requiring full obstacle geometry, RMPflow [15] composes task-space metrics for reactive control, and Beik-Mohammadi et al. [16] learn Riemannian manifolds from demonstrations. None is integrated with sampling-based plan-ning, and all require information beyond collision checking. 

Key distinction: Prior Riemannian approaches use the metric for local operations (steering, trajectory optimisation, reac-tive control). RIT* instead integrates Riemannian geometry into the global informed sampling pipeline, replacing the informed set, nearest-neighbour search, edge cost evaluation, and rewiring of a batch-informed planner with Riemannian counterparts, and incorporates CARM, which learns a con-formal scaling online from collision feedback, enabling cost-aware planning without prior environment knowledge. III. P ROBLEM FORMULATION 

We consider optimal motion planning where the cost of motion is anisotropic and may vary across the space; such costs are naturally described by a Riemannian metric assigning a direction-dependent traversal cost at each configuration. Let 

C ⊆ Rd denote a d-dimensional configuration space, and let Cfree ⊆ C be the subset of collision-free configurations. The space is equipped with a smooth, positive-definite metric tensor field G : C → Sd

> ++

, where Sd 

> ++

denotes the set of symmetric positive-definite matrices. For any configuration 

x ∈ C , the matrix G(x) defines the local cost of moving through x, so that different directions can have different traversal costs. Under this metric, the cost of a path σ : [0 , 1] → C is its Riemannian arc length, 

c(σ) = 

Z 10

q

˙σ(t)⊤G σ(t) ˙σ(t) dt, (1) where ˙σ(t) is the tangent vector of the path. The resulting 

geodesic distance dR(x, y ) is defined as the minimal cost of any path between the two configurations x and y, i.e., 

dR(x, y ) = inf σ c(σ) taken over all paths σ that connect x

and y. In the special case where G(x) = Id, this corresponds to the usual Euclidean distance. Given a start state xs ∈ C free and a goal state xg ∈ C free ,our objective is to find a collision-free path of minimum cost: 

σ∗ = arg min    

> σ:xs→xg, σ ⊂C free

c(σ), (2) A planner is asymptotically optimal (AO) if the cost cn of its returned solution converges almost surely to the optimal cost 

c∗ as the number of samples n → ∞ .After a feasible solution with cost cbest is found, the search can be restricted to configurations that may still generate a better path. In Euclidean informed planning, this region is the 

informed set 

IE = x ∈ C : ∥xs − x∥2 + ∥x − xg ∥2 ≤ cbest , (3) For anisotropic costs, however, Euclidean distance does not accurately reflect the true cost-to-come and cost-to-go. We therefore define the Riemannian informed set as 

IR = x ∈ C : dR(xs, x ) + dR(x, x g ) ≤ cbest . (4) This set contains configurations that can still improve the current solution under the Riemannian cost. When G ⪰ Id,

dR(x, y ) ≥ ∥ x − y∥2 and hence IR ⊆ I E ; more generally, under the bi-Lipschitz bounds of Sec. IV-F the two sets are equivalent up to constant-factor distortion, with IR aligned to the cost structure. Our goal is an AO informed planner operating natively under the Riemannian cost: sampling from IR, selecting neigh-bours and evaluating edges via dR, and, when the available metric is absent or does not encode obstacle proximity, refining it online from collision feedback. 

Remark 1. Numerical geodesic cost and admissible heuristic: Since dR has no closed form for general G, RIT* evaluates the arc-length of a straight-line edge γ(t) = u + t∆, ∆ = v − u,via n-point Gauss-Legendre (GL-n) quadrature, 

cR(u, v ) ≈

> n

X

> k=1

wk

q

∆⊤G(u + tk∆) ∆ , (5) 

where (tk, w k)nk=1 are GL nodes and weights on [0 , 1] (default 

n = 10 , configurable). For spatially constant metrics (e.g. G =diag( w)), eq. (5) is exact. The planning heuristic is ˆhR(v) = √∆⊤G ∆ for constant G, and ˆhR(v) = √λmin ∥v − xg ∥2

for spatially varying G, where λmin = min x λmin (G(x)) ;admissibility follows from dR(x, y ) ≥ √λmin ∥x − y∥2. We write cR for the value (5) computes (the superscript c(n) 

> R

is retained only in Lemma 1); since edges are straight lines, the tree-accumulated P cR converges to dR under densifica-tion, so the implementation rewiring and pruning criteria are asymptotically equivalent to their dR ideal forms. IEEE ROBOTICS AND AUTOMATION LETTERS 3

Algorithm 1: RIT* planner                                                                                             

> Input: xs, x g,Cfree , G (·), B, T, K c, σ, α
> Output: Optimal path σ∗
> 1V ← { xs};E ← ∅ ;cbest ← ∞ ;Ccol ← ∅ ;
> 2for t= 1 , . . . , T do
> 3Xnew ←RIEMANNIAN INF SAMPLE (B, c best , G )
> 4V ← V ∪ Xnew
> 5PRUNE NODES (V, c best , G )
> 6Compute rRnvia (6)
> 7foreach v∈ V in order of f(v)do
> 8N(v)←neighbours of vwithin rRn
> 9foreach u∈N(v)do
> 10 (cuv , p col )←CASCADING EDGE (u, v, G )
> 11 UPDATE AND REWIRE (V,E, u, v, c uv , r Rn, G )
> 12 if v=xgand g(v)< c best then
> 13 cbest ←g(v)
> 14 end
> 15 if pcol ̸=∅then
> 16 Ccol ← C col ∪ { pcol }
> 17 end
> 18 end
> 19 end
> 20 if tmod Kc= 0 and |C col |>0then
> 21 G←CARM_U PDATE (Ccol , G, σ, α )
> 22 end
> 23 end
> 24 return σ∗

IV. M ETHOD 

Batch Informed Trees [4] is an asymptotically optimal plan-ner that incrementally builds a tree from batches of samples drawn from an informed set; within each batch, vertices and edges are ordered by cost-to-come and heuristic estimates and evaluated lazily, balancing global exploration with goal-directed search. RIT* extends this framework with three modifications and a learning component: i) the informed set is defined by a Rie-mannian metric, producing a cost-aligned sampling region; ii) nearest-neighbour search uses Riemannian distance, enabling connections along low-cost directions; iii) edge costs are approximated via a cascading quadrature scheme; and CARM learns a conformal scaling online from collision feedback. Algorithm 1 summarises the procedure. The planner takes as input a start-goal pair (xs, x g ), free space Cfree , a metric tensor G (or Id if unknown), batch size B, iteration budget T ,and CARM parameters: update interval Kc, bandwidth σ, and inflation strength α. A metric cache M is precomputed on a grid for O(1) metric lookups. Within each batch, vertices are processed in order of f (v) = g(v) + ˆhR(v) (line 7), where 

g(v) is the Riemannian cost-to-come and ˆhR(v) = ¯ c(v, x g )

is a cached Riemannian distance heuristic (Remark 1). Com-ponents Riemannian sampling (line 3), cascading evaluation (line 10), rewiring, pruning (lines 5, 10), and CARM updates (line 21) are detailed later. 

A. Riemannian Informed Sampling 

RIT* draws samples from the Riemannian informed set IR

(line 3 of algorithm 1), defined in (4). When G ⪰ Id, the Riemannian distance dominates the Euclidean one, dR(x, y ) ≥∥x − y∥2, so IR ⊆ IE .To draw samples from IR efficiently, we apply a metric whitening transform. Let ¯G be the mean of G at Nseg = 10 

uniformly spaced points along the start-goal segment and 

L = chol( ¯G); both are computed once per planner invocation (or per CARM update) and reused, so the per-sample cost 

Fig. 2: Riemannian NN selection and cascading edge evalua-tion in RIT*. (a) The Riemannian ball expands along low-cost directions vs. the Euclidean ball. (b-d) Cascading filter: most edges are rejected early, only survivors receive full-precision evaluation. is a single L−⊤ multiplication. The change of coordinates 

˜x = L⊤x maps IR to an approximate prolate hyperspheroid, where the direct sampling of [3] applies. Samples are mapped back via x = L−⊤ ˜x. This transformation approximates the Riemannian informed set, with accuracy governed by the smoothness of G. Empirically, the median relative volume mismatch |vol( L−⊤ E) − vol( Itrue  

> R

)|/vol( Itrue  

> R

) is below 1% on our 3-D, 6-D, and 14-D benchmarks and below 3% under mild spatial metric variation; in a controlled sweep with 

G(x) = diag(1 , 3, 6)(1 + β∥x − xc∥2), β ∈ { 0, 0.5, 1, 2},median mismatch grows smoothly from < 0.5% (β = 0 ) to 

2.7% (β = 1 ) and 6.1% (β = 2 ), i.e. the whitening degrades gracefully with ∥∇ G∥.

B. Riemannian Nearest-Neighbour Search 

Let rRn be the neighbour connection radius for n samples; RIT* uses the Riemannian ball {u : dR(u, v ) ≤ rRn }, an ellipsoid aligned with the local metric that expands along low-cost directions and contracts along high-cost ones, matching the traversal cost structure. The connection radius is 

rRn = γR

 log nn

1/d 

, γR = 2 



1 + 1

d

1/d  μR(IR)

ζd

1/d 

,

(6) where μR(IR) is the Riemannian volume of IR and ζd is the volume of the unit d-ball. Because μR(IR) < μ(IE ), this radius is typically smaller than its Euclidean counterpart, re-ducing the number of candidate edges. In practice we evaluate 

μR(IR) ≈ det( ¯G)1/2 μ(IE ), the Jacobian change-of-variables under whitening (Sec. IV-A); this is exact for spatially constant 

G and incurs an error of O(∥∇ G∥∞ diam( IR)) otherwise, quantified empirically in Sec. V-A. 

C. Cascading Edge Evaluation 

Evaluating the Riemannian edge cost requires numerical in-tegration of (1) with multiple metric-tensor evaluations. Since most candidate edges cannot improve the current solution, RIT* screens them through a three-level cascade of increasing accuracy and cost, rejecting unpromising edges early (line 10 of algorithm 1). For a candidate edge from parent u to vertex v, let 

∆ = v − u and m = ( u+v)/2. An edge is discarded whenever 

g(u)+ˆ c(u, v ) > c best , where g(u) is the cost-to-come from xs

to u along the current tree. IEEE ROBOTICS AND AUTOMATION LETTERS 4

L1: Midpoint check : A single cached metric lookup at the midpoint m = ( u + v)/2 gives the fast estimate 

ˆc1 = ¯ c(u, v ) = 

q

∆⊤G(m) ∆ . (7) This L1 estimate serves only as a rapid first-pass filter; the exact arc-length is computed at L3 via the Gauss-Legendre quadrature (Remark 1). L1 rejects the majority of candidates across our 2-D to 14-D benchmarks (78-85%; 80% in the 6-D UR10 environment). 

L2: Simpson estimate : Surviving edges are re-evaluated with Simpson’s rule: 

ˆc2 = ∥∆∥

6

q 

∆⊤G(u) ∆+4 

q

∆⊤G(m) ∆+ 

q

∆⊤G(v) ∆ 



,

(8) which captures metric variation along the edge more accu-rately. This level rejects approximately 75% of the edges that passed L1. 

L3: Full quadrature and collision check : Only the roughly 5% of edges surviving both levels receive 10-point Gauss-Legendre integration followed by collision checking. Fig. 2 illustrates the cascade: of seven candidate neighbours, two are rejected at L1, one at L2, and one at L3 due to collision, leaving three accepted edges. Unlike the L3 quadrature (Lemma 1), the L1/L2 esti-mates (7)-(8) are not lower bounds on the true edge cost and could, in principle, discard an improving edge when G

varies sharply along it. The cascade thresholds are therefore deflated: an edge is rejected at L1/L2 only if g(u) + η−1 

> e

ˆc > cbest , where ηe = pλe

> max

/λ e 

> min

is computed from cached metric eigenvalue bounds over the cells traversed by the edge. Since every integrand sample of (1), and hence ˆc1,

ˆc2, and cR(u, v ), lies in [pλe

> min

∥∆∥, pλe

> max

∥∆∥], we have 

η−1 

> e

ˆc ≤ pλe

> min

∥∆∥ ≤ cR(u, v ): thus, no improving edge is ever falsely rejected. With the bounded edge lengths of Table I and smooth G, ηe → 1 as ∥∆∥ → 0, so the deflation is mild in practice and vanishes under densification; the quoted rejection rates are reported with this safeguard in place. 

D. Tree Rewiring and Pruning Rewiring (line 11 of algorithm 1) : When a new vertex v is added to the tree, it may provide a cheaper path to existing nearby vertices. For each neighbour u within the Riemannian radius rRn , RIT* checks whether the path through v is cheaper than the current parent of u:

g(v) + cR(v, u ) < g (u), (9) where g(·) is the Riemannian cost-to-come from xs along the current tree and cR(v, u ) is the Riemannian edge cost (1). If the condition holds, u is reconnected as a child of v and the cost-to-come of all descendants is updated. Because the comparison uses cR rather than Euclidean distance, rewiring respects the anisotropic cost structure and discovers connec-tions that Euclidean planners would overlook. 

Pruning (line 5 of algorithm 1) : After each batch, RIT* removes vertices that can no longer contribute to an improved solution. A vertex x is pruned if its best possible cost-to-come plus cost-to-go exceeds the current best solution cost: 

dR(xs, x ) + dR(x, x g ) > c best . (10) 

Fig. 3: Tree rewiring and pruning in RIT*. (a) Rewiring switches a vertex’s parent when a lower-cost Riemannian path is found. (b) Pruning removes vertices outside the Riemannian informed set. In practice, dR in (10) is evaluated from the accumulated L3 edge costs cR already computed during tree growth; by Remark 1 and Lemma 1, cR → dR under densification, pre-serving asymptotic optimality (Theorem 3). Pruning reduces the tree size and nearest-neighbour search cost, and when CARM updates the metric (line 21 of algorithm 1), it forces re-evaluation of vertex membership under the tightened IR,ensuring the tree adapts to the evolving cost landscape (Fig. 3). 

E. Collision-Adaptive Riemannian Metric (CARM) 

CARM is an online refinement of an initial base metric: the planner starts from a base metric G, which defaults to the identity Id when no prior metric is available, and CARM refines it from collision feedback. The metric G appearing as an input to algorithm 2 is thus the current (possibly default) metric being refined, not a prerequisite. CARM takes four inputs: a set of collision points Ccol = {ci}Ni=1 collected from failed edge checks, the current metric G, a kernel bandwidth σ

that controls spatial resolution, and an inflation strength α

that controls how aggressively traversal cost increases near obstacles. Failed edge checks naturally reveal points near obstacle boundaries; as they accumulate, they form a sparse map of regions where traversal should be expensive. CARM converts this map into a density field via a Gaussian kernel density estimate (KDE), 

ˆf (x) = 1

N

> N

X

> i=1

exp 



− ∥x − ci∥2

2σ2



, (11) and applies it as a conformal scaling of the base metric (lines 1-3 of algorithm 2): 

GCARM (x) = s(x) G(x), s(x) = 1 + α ˆf (x). (12) Because s(x) is scalar, GCARM is a conformal refinement: CARM learns an obstacle-proximity inflation field, not an anisotropic tensor structure. Directional cost variation, when present, must come from the base metric G(x); this is ap-propriate for collision-based learning because collisions carry positional information but not directional information. Once returned, GCARM overwrites G in the main loop (line 21 of algorithm 1), so all subsequent sampling, edge evaluation and pruning use the refined metric. Since GCARM (x) ⪰ G(x),traversal cost increases and the informed region contracts, preserving completeness and asymptotic optimality (Corol-lary 4). After each update the algorithm recomputes the metric cache, shrinks IR, and prunes vertices outside the tightened IEEE ROBOTICS AND AUTOMATION LETTERS 5

Algorithm 2: CARM_U PDATE (Ccol , G, σ, α )                                      

> Input: Collision points Ccol ={ci}Ni=1 , current metric G, bandwidth σ,strength α
> Output: Updated metric GCARM
> 1Build KDE: ˆf(x)←N−1PNi=1 exp  −∥ x−ci∥2/(2 σ2)
> 2Compute scale field: s(x)←1 + αˆf(x)
> 3Update metric: GCARM (x)←s(x)G(x)
> 4Recompute metric cache at grid points
> 5Recompute IRwith GCARM
> 6return GCARM

TABLE I: Experimental environment summary. d: dimension; 

κ: metric condition number; ℓmax : max edge length; nc:collision checks per edge.                          

> Environment dκℓmax nc
> 2-D Random World 210.05 10 3-D Diagonal 3≈60.05 10 6-D UR10 6≈12 0.10 rad 10 14-D Tiago Bimanual 14 ≈18 0.10 rad 10

informed set (lines 4-6 of algorithm 2), creating a feedback loop: collisions refine the metric, the refined metric focuses sampling, and focused sampling generates more informative collisions. CARM is triggered every Kc iterations (line 21 of algo-rithm 1) when the collision buffer is non-empty. As fig. 4 shows, the KDE field stabilises after ∼20 iterations ( ∼400 

collision points in 2-D). We use σ = 0.1, α = 10 , and 

Kc = 15 throughout; Sec. V-E reports sensitivity to these choices. 

F. Theoretical Analysis 

RIT* preserves the asymptotic guarantees of BIT* [4]; we sketch the modifications needed under a Riemannian metric and CARM. 

Assumption 1. G satisfies λmin Id ⪯ G(x) ⪯ λmax Id on Cfree 

with 0 < λ min ≤ λmax < ∞ and ∥∇ G∥∞ < ∞; the optimal path σ∗ has cost c∗ and strong δ-clearance. For inertia metrics 

G(q) = M (q) this holds when the configuration bounds exclude kinematic singularities; reported κ = λmax /λ min is the maximum over 10 4 grid samples. 

Bounding (1) by the extreme eigenvalues gives the bi-Lipschitz equivalence. 

pλmin ∥x − y∥2 ≤ dR(x, y ) ≤ pλmax ∥x − y∥2, (13) 

Lemma 1 (Convergence of GL-n arc-length quadrature) . For any u, v ∈ C free and smooth metric G, the n-point Gauss-Legendre estimate c(n) 

> R

(u, v ) defined in (5) satisfies 

|c(n) 

> R

(u, v ) − cR(u, v )| = O(∥v − u∥2n+1 ), (14) 

where cR(u, v ) is the true arc-length of the straight-line edge and the O(·) constant depends on the (2 n)-th derivative of 

t 7 → p∆⊤G(u + t∆) ∆ along the edge. The GL-n rule is exact for polynomials of degree ≤ 2n − 1; for default n = 10 

this yields a 20th-order method. For spatially constant metrics, the integrand is constant and c(n) 

> R

= cR exactly for any n ≥ 1.Hence c(n) 

> R

→ cR as ∥v − u∥ → 0 at rate O(∥v − u∥2n+1 ),so the L3-evaluated pruning and rewiring are asymptotically exact, preserving Theorem 3. 

Fig. 4: CARM metric evolution: as collision points accumu-late, the learned cost field refines, tightening the informed set and improving sample allocation. 

Theorem 2 (Probabilistic completeness) . RIT* finds a feasible solution with probability 1 as n → ∞ .Proof. Discretize σ∗ into waypoints w0, . . . , w K with ∥wk −

wk+1 ∥2 < δ/ 2; each is in the fixed set I+ 

> R

= {x :

dR(xs, x )+ dR(x, x g ) ≤ c∗ +ε}, which has a positive measure and is contained in IR. Uniform sampling on IR almost surely hits every δ/ 4-ball; by (13) the nearest samples satisfy 

dR(sk, s k+1 ) < √λmax δ, which falls below the connection ra-dius rRn = Θ((log n/n )1/d ) for sufficiently large n, producing a collision-free chain s0 → · · · → sK almost surely. 

Theorem 3 (Asymptotic optimality) . P lim n→∞ cRIT ∗ 

> n

=

c∗ = 1 .Proof. We verify the Karaman-Frazzoli conditions [2]. By (13), a Riemannian ball of radius rRn contains a Euclidean ball of radius rRn /√λmax , so RIT*’s edge set is a superset of BIT*’s at ¯rn = rRn /√λmax and inherits its neighbour-graph properties. Using (6), μR(IR) ≥ λd/ 2min μ(IR) > 0, so the connection-radius constant γR ≥ λ1/2min γKF (where γKF 

is the Karaman-Frazzoli lower bound on γ in (6)) exceeds the Karaman-Frazzoli threshold. The remaining steps (dense approximation of σ∗, cost convergence, optimal rewiring) follow [4] with dR replacing ∥ · ∥ 2 and (13) together with the GL-n convergence bound of Lemma 1 bounding the approximation error. 

Corollary 4 (CARM preserves guarantees) . With CARM, 1 ≤

s(x) ≤ 1 + α, so the scaled metric satisfies Assumption 1 with (λmin , (1 + α)λmax ). Updates terminate at iteration t0

when ∥ ˆft − ˆft−1∥∞ < ε 0 (ε0 = 10 −3); since at most B

collision points enter per Kc-window, the per-update change is O(B/N t) → 0, so t0 < ∞ almost surely. For t ≥ t0, the metric is frozen at G∞ and Theorems 2 and 3 apply with its constants. Since G∞ ⪰ G implies dG∞ 

> R

≥ dGR and hence 

IG∞ 

> R

⊆ IGR , the pruning rule (10) applied under G∞ targets the G∞-optimal solution, which is the planner’s objective after CARM refinement. 

V. E XPERIMENTS 

A. Experimental Setup 

We evaluated RIT* in environments that span 2-D, 3-D, 6-D and 14-D configuration spaces (Table I, Fig. 5), designed to assess both the Riemannian informed-set construction and the CARM adaptation mechanism 1.In low-dimensional spaces ( R2 and R3), we consider two geometric benchmarks. The Random World (2-D) consists  

> 1Code and scripts to reproduce all reported results are provided as supple-mentary material and will be released publicly. IEEE ROBOTICS AND AUTOMATION LETTERS 6

TABLE II: Benchmark Evaluations. Per environment: tmin init , tmed init (min/median time to first solution, s), cmed init (median initial cost), and cmed fin (median final cost). Bold : best per column.                                                                                                                            

> 2-D Random World ( R2)3-D Diagonal ( R3)6-D UR10 ( R6)14-D Tiago Biman. ( R14 )Succ. Planner tmin tmed cmed init cmed fin tmin tmed cmed init cmed fin tmin tmed cmed init cmed fin tmin tmed cmed init cmed fin (%) RIT* 0.013 0.020 0.9578 0.9135 0.021 0.040 2.4923 2.1608 0.0601 0.0915 1.9143 1.4143 0.8135 3.4255 16.5747 8.5778 100 BIT* 0.0183 0.0308 0.9698 0.9108 0.0476 0.0698 2.8167 2.2027 0.0651 0.0820 1.9415 1.5415 0.7657 0.8834 18.5069 10.7092 100 Inf. RRT* 0.0129 0.0289 0.9946 0.9165 0.0312 0.0464 2.8263 2.2278 0.1390 0.2114 1.9541 1.6975 1.5915 2.2198 19.9509 13.5773 100 AIT* 0.0111 0.0217 1.0475 0.9116 0.0236 0.0433 2.9950 2.2563 0.0697 0.1159 2.7794 1.4588 0.8070 1.4469 18.8514 11.0986 100 EIT* 0.0106 0.0162 1.0285 0.9124 0.0247 0.0413 2.8736 2.2449 0.0705 0.1397 2.7929 1.4530 0.8095 1.4452 19.8802 11.4415 100 APT* 0.0127 0.0189 1.0946 0.9225 0.0312 0.0465 2.7233 2.4436 0.1391 0.2126 2.9256 1.7536 1.6003 2.2284 19.8939 14.0232 100

Fig. 5: Benchmark environments from 2-D to 14-D, covering geometric setups and manipulation tasks for RIT* evaluation. of a square populated with 30 obstacles under a Euclidean metric ( κ = 1 ), providing an isotropic baseline. The Diagonal 

environment (3-D) is a unit cube with obstacles aligned with the axis that form a narrow passage, equipped with a diagonal-anisotropic metric G = diag(1 , 3, 6) (κ ≈ 6) to evaluate direction-dependent costs. In higher-dimensional manipulation tasks ( R6, R14 ), we evaluate scalability under articulated robot dynamics. In R6, a UR10e arm with a Robotiq 85 gripper in PyBullet [17] uses a joint-space inertia metric G(q) = M (q) (κ ≈ 12 ); the task (UR10 Drill ) retrieves a drill from a cluttered shelf. In R14 ,a Tiago Pro Bimanual task with two 7-DOF arms performs a coordinated pre-grasp motion toward a tall box with obstacle pillars blocking direct paths, under a bimanual inertia metric (κ ≈ 18 ), our most challenging setting in dimensionality and anisotropy. We compare RIT* with five asymptotically optimal anytime planners: Informed RRT* [3], BIT* [4], AIT*, EIT* [5], and APT* [6]. All planners are implemented in a common Python framework with shared collision checks, random seeds, and environment interfaces; each is provided with the same metric tensor G and evaluates edge costs using the same Riemannian integral (1), ensuring a common objective, and the cascading evaluation of Sec. IV-C is applied to all planners, so time differences are attributable to informed-set geometry and connectivity rather than integration cost. Baselines retain their original Euclidean informed sets and nearest-neighbour radii, isolating the effect of the Riemannian replacements. All planners use batch size B = 100 , maximum iterations 

T = 200 , a per-trial timeout of 200 s, and base seed 60; edge parameters follow Table I. All path costs in Tables II-IV are evaluated a posteriori under the fixed base metric G of each environment (the oracle metric in 6-D/14-D), never under 

GCARM : CARM only shapes where the planner samples, con-nects, and prunes, so metric inflation cannot bias the reported costs. The oracle-vs-CARM comparison in Table IV therefore directly measures the base-metric suboptimality introduced by CARM’s conservativeness, including any bias from spurious collision clusters, bounding it at ≤ 1.0% ; moreover, since the KDE (11) is normalised by N , isolated spurious clusters re-ceive vanishing weight as collision evidence accumulates. We do not compare against the metric-adaptive Bi-AM-RRT* [12]: its diffusion-based assisting metric accelerates search and replanning in dynamic 2-D navigation but is decoupled from the optimisation objective, so under the cost (1) it optimises a different objective, making a path-cost comparison inappro-priate. An efficiency-focused comparison in dynamic settings, where Bi-AM-RRT* is designed to excel, is left to future work. Runtime profiling on the 14-D benchmark shows collision checking dominates ( ∼61% of wall-clock time), followed by nearest-neighbour search (21%), metric evaluation/cache lookup (11%), and CARM updates (3%); the cascade reduces full quadrature and collision checks to ∼5% of candidates. For RIT*, we use a diagonal approximation of the metric, 

G(q) = diag( M (q)) , evaluated via trilinear interpolation from a precomputed cache on a res d grid with O(1) lookup ( RES =32 in 2-D/3-D, RES = 3 in 6-D/14-D). This trade-off preserves the dominant per-joint inertia scaling that drives the reported anisotropy ( κ ≈ 12 in 6-D, κ ≈ 18 in 14-D) while avoiding the O(d2) cache footprint and full M (q) evaluations at every L1 lookup. The coarse 6-D/14-D resolution is justified by the smooth variation of M (q) within the planning bounds (relative interpolation error < 4% at 10 4 random configurations) and because the cache feeds only the L1 filter; L3 re-samples G(q)

on the fly. Each experiment uses N = 50 trials with a 200 s timeout. We report wall-clock time and Riemannian path cost; init /final 

denote the first and terminal solutions, and bold marks the best per column. The volume approximation of Sec. IV-B incurs 

< 1% error on our benchmarks and 2.7% at β = 1 in the controlled sweep of Sec. IV-A, consistent with the bound. 

B. Low-Dimensional Benchmarks (2-D, 3-D) 

In isotropic 2-D ( κ = 1 ), RIT* attains the lowest initial cost (0.958), with gaps to baselines of 1.3-14.3%; Euclidean and Riemannian informed sets coincide here, so the small lead reflects only the tighter NN ellipsoid. At convergence all planners cluster within 1.3% (cmed fin ∈ [0 .911 , 0.923] , BIT* marginally lowest at 0.911 vs. RIT* 0.914 ), confirming the predicted equivalence under isotropy. In 3-D ( κ ≈ 6), RIT* attains the lowest cost (2.49) and is fastest in median time to a first solution (0.040 s, narrowly ahead of EIT* at 0.041 s), outperforming APT* (2.72, +9.3%), BIT* (2.82, +13.0%), IRRT* (2.83, +13.4%), EIT* (2.87, +15.3%), and AIT* (2.99, +20.1%). The 3-D Diagonal metric is spatially constant, so the mean-metric whitening of Sec. IV-A is exact and IR

is a uniformly rescaled IE ; the gain over BIT* here stems from the tighter nearest-neighbour ellipsoid, with informed-set tightening becoming dominant when the metric varies spatially (Sec. V-C). The lead persists at convergence: RIT* attains IEEE ROBOTICS AND AUTOMATION LETTERS 7

Fig. 6: Success rate (top) and Riemannian path cost (bottom) vs. time across all benchmarks; median over trials with shaded spread. RIT* converges faster and to lower cost, with the largest gains at 6-D/14-D. 

Fig. 7: UR10e real-robot execution of RIT*-planned trajecto-ries. The planner produces smooth, collision-free motions that transfer directly from simulation to hardware. the lowest cmed fin = 2 .16 , with gaps of +1 .9-13 .1% across baselines. 

C. High-Dimensional Benchmarks (6-D, 14-D) 

In the 6-D UR10 task ( κ ≈ 12 ), RIT* achieves the lowest cost across all baselines (1.91) while maintaining complete success (100%); RIT* improves on BIT* (1.94, +1.4%) and IRRT* (1.95, +2.1%); AIT* (2.78, +45.2%), EIT* (2.79, +45.9%), and APT* (2.93, +52.8%) trail further. At con-vergence RIT* retains the lead ( cmed fin : RIT* 1.414 , EIT* 

1.453 , AIT* 1.459 ; spread 3.2% ), with BIT* (+ 9.0% ), IRRT* (+ 20 .0% ), and APT* (+ 24 .0% ) trailing; the Riemannian in-formed set tightens both early progress and final convergence in this regime. In the 14-D Tiago task ( κ ≈ 18 ), RIT* attains the lowest cost (8.58), outperforming BIT* (10.71, +24.8%), AIT* (11.10, +29.4%), and EIT* (11.44, +33.4%), with IRRT* (13.58, +58 .3% ) and APT* (14.02, +63 .5% ) trailing further. The same ordering holds for cmed fin , where RIT*’s lead remains the largest across all environments, indicating that informed-set tightening, not just early connectivity, drives the gain at high dimension and anisotropy. Performance gains grow with both anisotropy and dimensionality: up to +14.3% (2-D), +20.1% (3-D), up to +24.0% in 6-D, and +24.8–63.5% (14-D, across all five baselines). RIT*’s median time to a 

first solution in 14-D (3.43 s) is about four times that of BIT* (0.88 s). This overhead has two sources: the smaller Riemannian connection radius (6), since μR(IR) < μ(IE )

results in fewer candidate edges per batch and consequently delays first connectivity in 14 dimensions, and the additional cost of per-edge metric evaluations. This initial overhead is offset after the first solution: RIT* converges to a 24.8% lower final cost than BIT* (Fig. 6). Where first-solution latency is critical, a hybrid schedule that uses the Euclidean connection radius until an initial solution is found could reduce this overhead without affecting the asymptotic guarantees. TABLE III: Ablation study: mean path cost ( ± std) over 50 trials. Bold : best practical variant with complete success (oracle excluded). “w/o CARM” uses the oracle metric.                                                     

> Variant 2-D 3-D 6-D 14-D
> RIT* (full) 0.921 ±0.007 2.196 ±0.027 1.489 ±0.082 8.638 ±0.144 w/o Riem. samp. 0.916 ±0.002 2.388 ±0.182 1.459 ±0.075 9.226 ±0.935 w/o cascading 0.920 ±0.008 2.195 ±0.017 1.489 ±0.082 8.638 ±0.144 w/o CARM 0.913 ±0.003 2.174 ±0.033 1.484 ±0.086 8.586 ±0.012

Real robot execution: We additionally demonstrate exe-cution of RIT*-planned trajectories on a UR10e platform (Fig. 7); a full hardware comparison is left to future work. The trajectories are smooth, collision-free, and preferentially use lower-inertia joints (wrist over shoulder/elbow), consistent with simulation. Across 20 hardware runs of three planned trajectories, all were collision-free, with mean joint tracking error 0.018 ±0.004 rad and end-effector deviation 4.2±1.1 mm. 

D. Ablation Study 

We isolate the contribution of each component across repre-sentative 2-D, 3-D, 6-D, and 14-D environments (Table III): (i) 

RIT* (full) , (ii) w/o Riemannian sampling (Euclidean informed set), (iii) w/o cascading (full edge evaluation), and (iv) w/o CARM (oracle metric). The oracle variant uses the ground-truth metric and serves as an upper bound rather than a directly comparable method. 

Observations: Low-D: Riemannian sampling provides the dominant benefit under anisotropy ( +8 .7% in 3-D without it; negligible in isotropic 2-D, where removing it is marginally lower, 0.916 vs. 0.921 – expected, since with G = Id the whitening reduces to the identity and only adds numeri-cal overhead; the 0.5% gap lies within trial-to-trial spread 

±0.007 ). CARM closely tracks the oracle in every environ-ment ( < 1.1% overhead), with the largest residuals in 2-D and 3-D ( +0 .9% , +1 .0% ), where the cost field is learned from G = Id. High-D: gains are smaller, and performance is primarily governed by informed-set geometry; CARM pro-vides incremental refinement over the base metric. Cascading 

has < 0.3% impact on cost, confirming it is primarily a computational optimisation. These trends are consistent with the analysis in Sec. V-E. 

E. CARM Analysis 

We evaluate how well CARM approximates the oracle (ground-truth) metric by comparing RIT* under learned versus oracle metrics (Table IV). In 6-D/14-D the oracle is the inertia IEEE ROBOTICS AND AUTOMATION LETTERS 8

TABLE IV: CARM quality analysis: mean path cost (oracle vs. learned metric). Overhead denotes relative cost increase w.r.t. the oracle; lower is better.                                

> Env Oracle CARM Overhead
> 2-D 0.913 ±0.003 0.921 ±0.007 +0 .9%
> 3-D 2.174 ±0.033 2.196 ±0.027 +1 .0%
> 6-D 1.484 ±0.086 1.489 ±0.082 +0 .3%
> 14-D 8.586 ±0.012 8.638 ±0.144 +0 .6%

matrix M (q) and CARM applies a learned proximity scaling to this same M (q), so the small overhead ( < 1% ) partly reflects the strength of the prior. The low-dimensional case is therefore most informative: starting from G = Id, CARM must reconstruct the obstacle-proximity cost entirely from collision feedback, yet attains overheads of only +0 .9% (2-D) and +1 .0% (3-D). Across all four settings CARM stays within ∼ 1% of the oracle, indicating that the learned scalar field recovers nearly all the obstacle-proximity information available from collisions, an appropriate scope since collisions carry positional but not directional information. Two additional checks support these conclusions: (a) a 

3 × 3 × 3 sweep over σ ∈ { 0.05 , 0.1, 0.2}, α ∈ { 5, 10 , 20 },

Kc ∈ { 10 , 15 , 30 } on the 3-D Diagonal benchmark yields median costs in [2 .18 , 2.24] across the 27 configurations, a worst-case variation of ±1.4% around our default (0 .1, 10 , 15) ,which is also the best cell, indicating low hyperparameter sensitivity; (b) re-running 6-D UR10 with G = Id (no inertia prior), RIT*+CARM attains median cost 1.451 vs. 1.414 for the oracle ( +2 .6% ) and 1.520 without CARM ( +7 .5% ), so CARM recovers ∼ 2/3 of the oracle gap from collisions alone. This last study also indicates that CARM is not tied to the Riemannian machinery of RIT*: since it simply rescales the cost field used for edge evaluation, it can be attached to any planner that evaluates edge costs. With an isotropic base G0 = Id, it effectively turns a Euclidean planner into a cost-space variant, and the G = Id result above suggests that similar gains may transfer. VI. D ISCUSSION AND LIMITATIONS 

Discussion: The benefit of RIT* increases with both anisotropy ( κ) and dimensionality. For isotropic costs ( κ ≈ 1), 

IR ≈ IE and the gap to informed-set baselines is small (within 7.4%), although weaker informed-sampling strategies (APT*) trail by up to 14.3%. As anisotropy grows, IR aligns progressively better with the cost structure: +13.0% over BIT* in 3-D ( κ ≈ 6), and in 14-D ( κ ≈ 18 ) the advantage is largest at +24 .8-63 .5% over all five baselines. Overall, the gain scales with the tightening of the informed set under the cost geometry, while RIT* maintains competitive convergence speed (Fig. 6) despite the additional metric computations. 

Limitations: Some limitations suggest directions for im-provement. (i) The metric cache scales as res d, which can become prohibitive in high dimensions; sparse, adaptive, or learnt representations could reduce this. (ii) CARM employs isotropic Gaussian kernels, which may blur directional struc-ture near obstacles; contact-normal directions returned by the collision checker could provide the missing directional information, enabling anisotropic kernels or a tensor-valued (rather than conformal) refinement aligned with local obstacle geometry. (iii) CARM relies on collision feedback, and its effectiveness depends on the informativeness of sampled col-lisions; in simpler settings recovery may be slower, motivating exploration that actively probes uncertain regions. (iv) The computational overhead of metric evaluation and caching may impact scalability in very high dimensions. VII. C ONCLUSIONS 

We introduced RIT*, a Riemannian batch-informed planner that replaces Euclidean primitives with cost-consistent coun-terparts and learns a collision-adaptive cost refinement online via CARM. From 2-D to 14-D, RIT* produces lower-cost so-lutions under anisotropic metrics while remaining competitive in isotropic settings, with gains scaling with anisotropy and dimension ( +13 .0% over BIT* in 3-D, up to +24 .0% in 6-D, and +24 .8-63 .5% in 14-D bimanual planning), and CARM recovers most of the oracle-metric advantage from collision feedback alone. REFERENCES [1] S. M. LaValle, Planning Algorithms . Cambridge Univ. Press, 2006. [2] S. Karaman and E. Frazzoli, “Sampling-based algorithms for optimal motion planning,” Int. J. Robot. Res. , vol. 30, no. 7, pp. 846–894, 2011. [3] J. D. Gammell, T. D. Barfoot, and S. S. Srinivasa, “Informed sampling for asymptotically optimal path planning,” IEEE Trans. Robot. , vol. 34, no. 4, pp. 966–984, 2018. [4] ——, “Batch Informed Trees (BIT*): Informed asymptotically optimal anytime search,” Int. J. Robot. Res. , vol. 39, no. 5, pp. 543–567, 2020. [5] M. P. Strub and J. D. Gammell, “Adaptively Informed Trees (AIT*) and Effort Informed Trees (EIT*): Asymmetric bidirectional sampling-based path planning,” Int. J. Robot. Res. , vol. 41, no. 4, pp. 390–417, 2022. [6] L. Zhang, S. Wang, K. Cai, Z. Bing, F. Wu, C. Wang, S. Haddadin, and A. Knoll, “APT*: Asymptotically optimal motion planning via adaptively prolated elliptical r-nearest neighbors,” IEEE Robot. Autom. Lett. , vol. 10, no. 10, pp. 10 242–10 249, 2025. [7] L. Zhang, Z. Bing, K. Chen, L. Chen, K. Cai, Y. Zhang, F. Wu, P. Krumbholz, Z. Yuan, S. Haddadin, and A. Knoll, “Flexible Informed Trees (FIT*): Adaptive batch-size approach in informed sampling-based path planning,” in Proc. IEEE/RSJ Int. Conf. Intell. Robot. Syst. (IROS) ,2024, pp. 3146–3152. [8] N. Ratliff, M. Zucker, J. A. Bagnell, and S. Srinivasa, “CHOMP: Gradient optimization techniques for efficient motion planning,” in Proc. IEEE Int. Conf. Robot. Autom. (ICRA) , 2009, pp. 489–494. [9] M. Kalakrishnan, S. Chitta, E. Theodorou, P. Pastor, and S. Schaal, “STOMP: Stochastic trajectory optimization for motion planning,” in 

Proc. IEEE Int. Conf. Robot. Autom. (ICRA) , 2011, pp. 4569–4574. [10] B. Kim, T. T. Um, C. Suh, and F. C. Park, “Tangent bundle RRT: A randomized algorithm for constrained motion planning,” Robotica ,vol. 34, no. 1, pp. 202–225, 2016. [11] P. T. Kyaw and J. Kelly, “Geometry-aware sampling-based motion plan-ning on Riemannian manifolds,” 2026, arXiv preprint arXiv:2602.00992. [12] Y. Zhang, H. Wang, M. Yin, J. Wang, and C. Hua, “Bi-AM-RRT*: A fast and efficient sampling-based motion planning algorithm in dynamic environments,” IEEE Trans. Intell. Veh. , vol. 9, no. 1, pp. 1282–1293, 2024. [13] L. Jaillet, J. Cortés, and T. Siméon, “Sampling-based path planning on configuration-space costmaps,” IEEE Trans. Robot. , vol. 26, no. 4, pp. 635–646, 2010. [14] H. Klein, N. Jaquier, A. Meixner, and T. Asfour, “On the design of region-avoiding metrics for collision-safe motion generation on Rieman-nian manifolds,” in Proc. IEEE/RSJ Int. Conf. Intell. Robot. Syst. (IROS) ,2023, pp. 2346–2353. [15] C.-A. Cheng, M. Mukadam, J. Issac, S. Birchfield, D. Fox, B. Boots, and N. Ratliff, “RMPflow: A computational graph for automatic motion policy generation,” in Proc. Worksh. Algorithmic Found. Robot. (WAFR) ,ser. Springer Proc. Adv. Robot., vol. 14. Springer, 2018, pp. 441–457. [16] H. Beik-Mohammadi, S. Hauberg, G. Arvanitidis, G. Neumann, and L. Rozo, “Reactive motion generation on learned Riemannian mani-folds,” Int. J. Robot. Res. , vol. 42, no. 10, pp. 729–754, 2023. [17] E. Coumans and Y. Bai, “PyBullet, a Python module for physics simulation for games, robotics and machine learning,” http://pybullet.org, 2016–2021.