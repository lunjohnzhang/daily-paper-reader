---
title: "Tensor-Coord: Algebraic Decomposition of Joint Plan Tensors for Conflict-Free Multi-Agent LLM Planning"
title_zh: "Tensor-Coord: 面向无冲突多智能体大模型规划的联合计划张量代数分解"
authors: Mudit Rastogi
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16478v1"
tags: ["query:multi-robot"]
score: 9.0
evidence: 通过张量分解实现多智能体无冲突规划
tldr: "多智能体LLM规划中独立生成计划易导致碰撞、资源竞争等协调失败。Tensor-Coord将N个智能体的联合计划表示为三阶张量，通过CP和Tucker分解提取潜在协调结构，基于最小ε-近似CP秩定义协调复杂度，并利用残差张量定位冲突。在机器人递送任务中，2智能体100%收敛到无冲突计划（平均1.4轮），3智能体80%（3.2轮），4智能体60%（4.0轮）。该方法无需领域规则即可定位失败并提供可解释约束用于LLM重规划，CP秩线性增长表明其可预测协调复杂度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1686, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 1455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1565, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1702, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16478v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1755, \"height\": 318, \"label\": \"Table\"}]"
motivation: 多智能体LLM规划中独立生成计划导致协调失败，现有方法依赖领域规则或难以扩展，需要可计算的协调复杂度度量和不依赖规则冲突定位方法。
method: 将联合计划表示为三阶张量，应用CP和Tucker分解提取协调结构；基于最小ε-近似CP秩定义协调复杂度；利用残差张量计算冲突分数定位失败；Tucker因子转化为自然语言约束，迭代指导LLM重规划。
result: "在2-4智能体网格递送任务中，2智能体100%无冲突收敛（平均1.4轮），3智能体80%（3.2轮），4智能体60%（4.0轮），CP秩随智能体数量线性增长。"
conclusion: Tensor-Coord为多智能体LLM规划提供了可计算的协调复杂度度量和无规则冲突定位方法，实验验证其有效性，CP秩可作为协调复杂度的预测指标。
---

## 摘要
大语言模型在多智能体规划方面仍然受限，因为独立生成的计划会导致协调失败，例如空间碰撞、资源争用和时间死锁。我们提出Tensor-Coord，一个多重线性代数框架，将N个智能体的联合计划表示为关于智能体、时间步和动作的三阶张量\(T \in R^{N \times H \times A}\)。使用典型多面体分解和Tucker分解来识别潜在协调结构。最小ε近似CP秩R*定义了可计算的协调复杂度度量，即\(CC(Pi)=(R*-N)/N\)。我们证明R*=N是计划独立的充要条件。残差\(E=T-T_{R*}\)定义了智能体对、时间步和动作上的冲突分数，无需领域特定规则即可定位失败。Tucker因子提供了可解释的智能体角色、时间阶段和动作聚类，这些被转化为自然语言约束，用于迭代的大模型重规划。在简单（2个智能体，5x5网格）、中等（3个智能体，5x5网格）和困难（4个智能体，5x5网格）设置下的多机器人配送任务实验表明，在2个智能体情况下平均1.4次迭代内100%收敛到无冲突计划，3个智能体情况下平均3.2次迭代内80%收敛，4个智能体情况下平均4.0次迭代内60%收敛。CP秩近似线性增长为\(R*(N) = 3.9N + 0.5\)，支持其作为协调复杂度的预测指标。

## Abstract
Large language models (LLMs) remain limited in multi-agent planning because independently generated plans can create coordination failures such as spatial collisions, resource contention, and temporal deadlocks. We introduce Tensor-Coord, a multilinear algebra framework that represents the joint plan of N agents as a third-order tensor \(T \in R^{N \times H \times A}\) over agents, timesteps, and actions. Canonical Polyadic (CP) and Tucker decompositions are used to identify latent coordination structure. The minimal epsilon-approximate CP rank R* defines a computable coordination complexity measure, with \(CC(Pi)=(R*-N)/N\). We prove that R*=N is necessary and sufficient for plan independence. The residual \(E=T-T_{R*}\) defines a conflict score over agent pairs, timesteps, and actions, localizing failures without domain-specific rules. Tucker factors provide interpretable agent roles, temporal phases, and action clusters that are converted into natural language constraints for iterative LLM replanning. Experiments on multi-robot delivery tasks across Easy (2 agents, 5x5 grid), Medium (3 agents, 5x5 grid), and Hard (4 agents, 5x5 grid) settings show convergence to conflict-free plans in 100% of 2-agent cases within 1.4 iterations on average, 80% of 3-agent cases within 3.2 iterations, and 60% of 4-agent cases within 4.0 iterations. CP rank scaled approximately linearly as \(R*(N) = 3.9N + 0.5\), supporting its use as a predictor of coordination complexity.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）在多智能体规划中存在根本性局限——当多个智能体独立生成计划时，容易产生空间碰撞、资源争用和时间死锁等协调失败。现有方法（联合提示、顺序规划、经典多智能体规划如CBS）要么因联合动作空间指数增长而超出上下文窗口，要么引入顺序偏差，要么需要领域形式化规范，无法兼容LLM的自然语言优势。
- **整体含义**：作者提出一种多重线性代数框架（Tensor-Coord），将联合计划表示为三阶张量，利用张量分解自动发现协调结构，并定义可计算的协调复杂度度量，从而在不依赖领域规则的前提下代数化定位协调失败，生成自然语言约束引导LLM重规划，实现无冲突的联合计划。

## 2. 论文提出的方法论

- **核心思想**：将N个智能体的联合计划编码为软张量\(\mathcal{T} \in \mathbb{R}^{N \times T \times K}\)（代理×时间步×动作）；通过规范多面体（CP）分解获得最小ε-近似秩R*，并定义协调复杂度\(CC(\Pi) = (R^*-N)/N\)；利用CP残差张量计算冲突分数\(CS(i,j,t) = \rho(i,t) \cdot \rho(j,t)\)（其中\(\rho(i,t) = \|E(i,t,:)\|_2\)），定位冲突；通过Tucker分解获取可解释的因子矩阵（代理角色、时间阶段、动作类型），将其转化为自然语言约束，迭代指导LLM重规划。
- **关键技术细节**：
  - **软张量编码**：\(\mathcal{T}(i,t,k) = \gamma \cdot \mathbf{1}[a_i^t = a_k] + \frac{1-\gamma}{K-1} \cdot \mathbf{1}[a_i^t \neq a_k]\)，其中\(\gamma=0.8\)，确保全秩且稳定分解。
  - **CP分解**：\(\mathcal{T} \approx \sum_{r=1}^{R} \lambda_r \cdot u_r \otimes v_r \otimes w_r\)，采用交替最小二乘法（ALS）更新因子，从R=N开始递增直至重建误差≤ε=0.1。
  - **Tucker分解**：\(\mathcal{T} \approx \mathcal{G} \times_1 U \times_2 V \times_3 W\)，通过HOSVD计算，U、V、W分别对应代理角色、时间阶段、动作类型。
  - **迭代算法**（Algorithm 1）：每轮由LLM生成各代理计划→构造软张量→计算CP秩和残差→检测冲突→若冲突为零则收敛；否则根据冲突和Tucker因子生成三类自然语言约束（空间约束、阶段边界约束、角色约束），附加到下一轮LLM提示中，重复最多K次。

## 3. 实验设计

- **数据集/场景**：多机器人配送任务，网格世界。设置5种配置：
  - Easy: 2智能体, 5×5网格, 2包裹, 时间步T=10
  - Medium: 3智能体, 5×5网格, 3包裹, T=15
  - Hard: 4智能体, 5×5网格, 4包裹, T=20
  - Very Hard: 5智能体, 6×6网格, 5包裹, T=30
  - Complex: 3智能体, 6×6网格, 6包裹, T=35
- **Benchmark**：未使用公开的规划基准（如PlanBench），而是自建场景。对比了三种基线方法：
  - B1: 独立规划（无协调）
  - B2: 轮询规划（顺序生成，后代理可见前代理计划）
  - B3: 单LLM联合规划（一次提示生成所有代理计划）
- **LLM后端**：Azure OpenAI GPT-5，temperature=0.2，max tokens=500。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或训练时长。仅提及使用Azure OpenAI GPT-5作为LLM后端进行推理，未报告微调或大规模训练算力需求。实验规模较小（最多5智能体，30个时间步），推理成本较低。

## 5. 实验数量与充分性

- **实验数量**：每种配置使用5个随机种子，最多5次迭代。共5种配置×5种子=25组运行。此外，针对基线方法，在相同配置上进行了比较（图中显示5种配置×3基线，但未说明种子数）。总体实验数量适中。
- **充分性与公平性**：
  - 优：覆盖了不同智能体数量、网格大小、包裹数量，且提供了收敛迭代次数、冲突数、成功率的量化表。
  - 不足：①种子数较少（5个），统计显著性不足；②未报告方差或置信区间；③基线方法仅与一轮生成的Tensor-Coord对比，未考虑迭代轮次对齐（基线仅一次执行，而Tensor-Coord允许多次迭代），比较的公平性有限；④未在标准多智能体规划基准（如CBS、M*等）上对比，仅与LLM baseline比较；⑤未进行消融实验（如去掉Tucker约束、只用CP等）。

## 6. 论文的主要结论与发现

- **主要结论**：Tensor-Coord实现了基于张量秩的协调复杂度度量，证明R*=N是计划独立的充要条件；残差冲突检测能无需领域规则定位冲突；Tucker分解提供可解释的协调结构。
- **关键发现**：
  - 2智能体100%收敛（平均1.4轮），3智能体80%收敛（3.2轮），4智能体60%收敛（4.0轮）；4智能体（Hard）表现优于3智能体（Medium）（80% vs 60%），归因于网格更大。
  - CP秩随智能体数量线性增长：\(R^*(N) \approx 3.9N + 0.5\)（R²=0.94）。
  - 所有配置中Tensor-Coord的冲突减少率均高于全部基线，且是唯一能通过迭代达到零冲突的方法。
  - 发现“LLM回退悖论”：当LLM无法满足约束时返回空计划（等待动作），反而降低了张量残差复杂度，加速收敛。

## 7. 优点

- **创新性**：首次将张量分解应用于多智能体LLM规划协调，提出可计算的协调复杂度度量（CP秩）和代数化冲突定位方法（残差乘积）。
- **可解释性**：Tucker因子直接映射为代理角色、时间阶段和动作类型，可生成针对性的自然语言约束，避免模糊的“避免碰撞”指令。
- **无需领域规则**：冲突检测完全基于张量残差，无需手动定义碰撞规则，泛化性好。
- **迭代闭环**：张量分析结果反馈给LLM重规划，形成“生成→分析→约束→重生成”的闭环，提升成功率和收敛性。
- **理论支撑**：给出了定理1（独立性刻画）和命题2（残差检测冲突的合理性证明），有理论分析。

## 8. 不足与局限

- **实验规模有限**：最大仅5智能体、6×6网格，未测试更大规模（如10+智能体、更复杂环境），结论泛化性存疑。
- **收敛率非100%**：对于4智能体以上，收敛率低于80%，存在三种失败模式（网格拥堵、LLM回退空计划、约束非单调交互），解决方法不够健壮。
- **缺乏标准基准对比**：未与经典多智能体规划算法（CBS、M*）比较，也未使用PlanBench等标准LLM规划基准。
- **统计报告不充分**：仅报告平均值，未提供标准差、置信区间等，实验结论的可重复性和可靠性待验证。
- **潜在偏差**：LLM后端为单一模型GPT-5，结果可能受LLM版本影响；实验场景为特定配送任务，其他领域（如仓储、交通）未验证。
- **计算复杂度**：ALS求解CP秩需多次迭代（R从N递增），且文中未报告完全收敛所需平均迭代次数上限（最大设5次），对于更高维张量可能计算代价上升。
- **约束生成策略**：三类约束的生成规则未详细阐述可自动化程度，依赖人工设计的模板，可能仍是半自动。

（完）
