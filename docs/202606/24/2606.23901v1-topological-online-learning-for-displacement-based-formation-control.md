---
title: Topological Online Learning for Displacement-based Formation Control
title_zh: 基于位移的编队控制的拓扑在线学习
authors: "Saksham Sharma, Shubhankar Gupta, Sumant A Gunagi, Suresh Sundaram"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23901v1"
tags: ["query:multi-robot"]
score: 8.0
evidence: 多机器人编队控制的在线拓扑学习
tldr: "针对位移编队控制中固定拓扑易受干扰导致形变的问题，提出拓扑在线学习（TOLD）框架，通过实时更新边权重最小化编队形变。两种策略：无约束权重的在线梯度流（OGF）和凸权重的指数梯度流（OExpGF）。仿真表明TOLD增强节点级控制器后，累积形变误差中位数降低1.2%-33.14%；硬件实验显示中位形变降低62%（OGF）和31.4%（OExpGF）。该工作首次将拓扑学习引入编队控制，有效提升鲁棒性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23901v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1828, \"height\": 848, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23901v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 537, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23901v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1833, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23901v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 925, \"height\": 789, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23901v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 885, \"height\": 443, \"label\": \"Figure\"}]"
motivation: 传统编队控制依赖固定拓扑权重，易受干扰导致形变大；缺乏在线自适应机制，难以实时优化编队结构。
method: 提出TOLD框架，在位移编队控制中在线调整交互拓扑权重，具体设计OGF（无约束）和OExpGF（非负凸权重）两种学习策略。
result: "仿真12机器人加入TOLD后累积形变误差中位数降1.2%-33.14%；四旋翼实验形变中位数降62%（OGF）和31.4%（OExpGF）。"
conclusion: TOLD通过在线拓扑学习显著降低编队形变，可无缝增强现有节点级控制器，为鲁棒编队控制提供新范式。
---

## 摘要
本文通过引入基于位移的拓扑在线学习（TOLD）编队控制，一种实时边级自适应框架，解决了鲁棒编队控制问题。与传统的节点级鲁棒控制器不同，后者在不修改交互拓扑的情况下调节单个机器人输入，TOLD在线更新交互拓扑权重，直接最小化编队畸变。在TOLD编队控制框架下提出了两种策略：无约束权重的在线梯度流（OGF）和非负凸权重的在线指数梯度流（OExpGF）。理论分析表明，对于有向图上的单积分器智能体，OExpGF保证渐近一致性，而OGF确保有界编队畸变。在间歇性干扰下的十二个机器人仿真中，将TOLD与节点级控制器相结合时，中位累积均方根畸变误差降低了1.2%-33.14%。使用Crazyflie 2.0四旋翼飞行器的硬件实验表明，与固定权重一致性相比，中位编队畸变降低了超过62%（OGF）和31.4%（OExpGF）。

## Abstract
This paper addresses the problem of robust formation control by introducing Topological Online Learning for Displacement-based (TOLD) formation control, a real-time edge-level adaptation framework. Unlike conventional node-level robust controllers that regulate individual robot inputs without modifying the interaction topology, TOLD updates the interaction topology weights online to directly minimize formation distortion. Two strategies are proposed under the TOLD formation control framework: Online Gradient Flow (OGF) with unconstrained weights and Online Exponential Gradient Flow (OExpGF) with non-negative convex weights. Theoretical analysis establishes that, for single-integrator agents over directed graphs, OExpGF guarantees asymptotic consensus, while OGF ensures bounded formation distortion. Simulations with twelve robots under intermittent disturbances show 1.2%-33.14% median cumulative Root Mean Distortion Error reduction when augmenting TOLD with node-level controllers. Hardware experiments with Crazyflie 2.0 quadrotors demonstrate over 62% (OGF) and 31.4% (OExpGF) reduction in median formation distortion compared to fixed-weight consensus.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在多机器人位移编队控制中，传统方法通常采用固定拓扑权重（如共识算法），但面对环境干扰（如风力、传感器噪声）时，编队易产生较大畸变。现有节点级鲁棒控制器（如自适应增益、衰减增益、扰动观测器）虽能调节单个机器人输入，但未改变交互拓扑本身，因此难以从拓扑层面主动补偿畸变。
- **研究动机**：作者希望在不依赖大规模离线训练或复杂学习模型的情况下，实现实时的拓扑级自适应，从而在保持轻量级和可解释性的同时，显著提升编队鲁棒性。此外，鉴于现有基于图神经网络（GNN）或边级协议的方法需要离线训练、存在仿真到现实差距且缺乏稳定性保证，作者提出在线学习方案。
- **整体含义**：该论文首次将在线梯度下降的思想引入编队控制的拓扑权值更新，提出拓扑在线学习（TOLD）框架，作为可叠加在任意现有节点级控制器上方的“拓扑路由覆盖层”，以直接最小化编队畸变。实验证明该方法能有效提升编队精度并降低畸变。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式说明

- **核心思想**：TOLD框架在位移编队控制中，利用机器人之间相对位置传感器的实时观测，在线更新交互拓扑的边权重（即邻居之间的影响系数），使得共识误差（编队畸变）随时间递减。它不依赖离线训练，完全在线运行。
- **两种具体策略**：
  1. **Online Gradient Flow (OGF)**：权重为实值（无符号约束），通过梯度下降更新。公式如：
     - 权重组合：\( w_{ij}(t) = (1-\epsilon) a_{ij} \hat{w}_{ij}(t) + \epsilon a_{ij} \)
     - 更新律：\( \dot{\hat{w}}_{ij}(t) = -\eta \frac{\partial e_i(t)}{\partial \hat{w}_{ij}} \)
     - 其中 \( e_i \) 为机器人 i 的邻域畸变误差，梯度通过链式法则计算（位置对权重的灵敏度积分得到）。
  2. **Online Exponential Gradient Flow (OExpGF)**：权重通过指数化映射保证非负且行和为1（凸组合）。公式如：
     - 权重：\( w_{ij}(t) = (1-\epsilon) \frac{a_{ij} \hat{w}_{ij}(t)}{\sum_{j'} a_{ij'} \hat{w}_{ij'}(t)} + \epsilon a_{ij} \)
     - 辅助变量：\( \hat{w}_{ij}(t)= \exp(-\eta_w l_{ij}(t)) \)，其中 \( l_{ij} \) 为折扣累积梯度。
     - OExpGF在更新中加入了折扣因子（γ）和归一化处理，确保权重始终为正且有界。
- **理论保证**：
  - OExpGF保证在满足有向生成树条件下渐近达成共识（直径收缩）。
  - OGF保证畸变误差不增长（时间导数非正）。
- **离散实现**：提供了算法1（OGF）和算法2（OExpGF），使用固定步长ΔT，机器人仅需本地相对位置和自身权重信息。

### 3. 实验设计：数据集/场景、基准、对比方法

- **仿真实验**：
  - 场景：2D环境中12个机器人，组成无领导者编队，目标为半径10m的十二边形。交互拓扑为有向循环图C12(1,2)，每个机器人观测两个单向邻居。
  - 干扰：6个机器人在t=1,2,4,6秒受间歇性扰动（均匀+高斯噪声），其余机器人仅有过程噪声；传感器噪声为低幅高斯。
  - 基准对比方法：
    - **固定权重共识（FixedWts）**
    - **三种节点级鲁棒控制器**：自适应增益（Adaptive Gain）、衰减增益（Decay Gain）、抗饱和扰动观测器（Anti-Windup DOB）
    - **纯拓扑方法**：单独OGF、单独OExpGF
    - **组合方法**：OGF/OExpGF + 每种节点级控制器（共6种组合）
  - 总共12种算法配置，每个配置运行1000次独立仿真。

- **硬件实验**：
  - 设备：Qualisys运动捕捉系统 + Crazyflie 2.0四旋翼（3机主实验，4机补充验证）。
  - 场景：领导者（Drone0）沿螺旋轨迹手动飞行，跟随者基于相对位置执行编队控制。仅与固定权重基线对比（未叠加节点级控制器，为隔离考察拓扑贡献）。
  - 指标：轨迹跟踪误差、编队畸变（分量+幅值）、权重矩阵Frobenius范数演变。

### 4. 资源与算力

- **文中未明确提及**使用的GPU型号、数量或训练时长。所有仿真在MATLAB中运行（使用ode45求解器），硬件实验基于运动捕捉系统及Crazyflie的机载运算。作者未透露仿真机器的具体配置或单次运行时间，也未说明是否使用GPU加速。因此无法评估训练或推理的计算开销。

### 5. 实验数量与充分性

- **实验数量**：
  - 仿真：12种方法 × 1000次重复，总仿真次数≥12,000次（含随机种子变化）。
  - 硬件：3机主实验（对比3种拓扑方法）+ 4机补充实验（验证可扩展性）；跟踪实验中还包含参数研究（不同学习率、折扣因子、窗口大小等）。
- **充分性**：
  - 仿真中考虑了多种典型干扰类型（均匀、高斯、间歇），且与多种节点级控制器正交组合，形成全面对照。
  - 硬件实验虽未组合节点级控制器，但作者明确说明这是为了隔离拓扑层的贡献，且后续讨论指出组合可进一步提升。
  - 参数敏感性分析（6.4节）覆盖了OGF的η和OExpGF的η_w、ρ及窗口大小，为实际部署提供了指导。
  - 不足：仿真中假设单积分器模型，未验证高阶动力学（如四旋翼完整模型）；硬件实验仅使用3-4架无人机，规模较小；未对比其他学习型拓扑方法（如GNN-based），仅与固定权重及其变体对比；未测试不同拓扑结构（如随机图）的影响。

### 6. 论文的主要结论与发现

- TOLD框架（OGF/OExpGF）能在实时中显著降低编队畸变，且可无缝叠加至已有节点级鲁棒控制器上。
- **仿真结果**：组合方法相对于纯节点级控制器，累积RMDE中位数降低1.2%–33.14%（其中OGF+Decay Gain降幅最大）。
- **硬件结果**：纯拓扑方法（无节点级控制器）相比于固定权重共识，中位畸变降低：OGF >62%，OExpGF >31.4%。OGF在精度上最优，但权重可无界增长；OExpGF权重有界，更易长期部署。
- 揭示了精度与实现可行性之间的权衡：OGF适合对精度要求高且可接受权重增长的应用；OExpGF更适合需要稳定、可预测行为的场景。

### 7. 优点

- **创新性**：首次将在线梯度下降/指数梯度流直接用于编队控制拓扑权重的实时调整，无需离线训练，与现有方法形成鲜明互补。
- **完整性**：从问题建模、算法设计、理论分析（渐近共识、误差有界）到仿真和硬件实验，逻辑链条完整。
- **实验设计严谨**：仿真中随机种子重复1000次并采用箱线图展示分布，硬件实验包含参数研究和多机验证，结果可信度高。
- **实用导向**：算法仅需本地相对位置测量，通信负载低；提供了离散时间实现算法，便于实际部署。
- **可解释性强**：理论保证（OExpGF收敛到共识，OGF误差非增）增强了方法的可靠性和工程采纳潜力。

### 8. 不足与局限

- **实验规模有限**：硬件实验仅3-4架无人机，未在更大编队（如10+）中验证；仿真虽然用了12机，但仍未测试20+或100+的规模，其可扩展性尚未充分证明。
- **动力学简化**：仿真和理论均基于单积分器模型，而实际四旋翼是二阶非线性系统。硬件实验中虽通过速度指令近似，但未分析模型失配导致的稳定性风险。
- **对比方法缺陷**：仅与固定权重共识及传统节点级控制器对比，未与近年来基于GNN的离线学习方法（如文献[19,20]）进行直接对比，可能无法全面体现TOLD相对于最新学习方法的优势。
- **未考虑通信延迟与丢包**：文中假定完美相对位置观测（通过外感受传感器），但实际传感器存在延迟或遮挡，该因素未被考虑。
- **权重无界风险**：OGF的权重可能增长至很大（Frobenius范数从1.2上升到7.0），长时间运行可能导致数值溢出或干扰其他控制目标。作者虽指出此问题但未提出明确解决方案（如裁剪、正则化）。
- **参数调节依赖经验**：OExpGF的折扣因子γ和学习率η_w需根据手动调节，缺乏自适应调参机制；仿真中的参数按“基于仿真的参数研究”选择，但未在硬件实验中进行系统调优。

（完）
