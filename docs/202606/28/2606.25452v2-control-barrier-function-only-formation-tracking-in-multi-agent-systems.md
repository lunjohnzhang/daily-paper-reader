---
title: Control Barrier Function only Formation Tracking in Multi-Agent Systems
title_zh: 多智能体系统中仅基于控制屏障函数的编队跟踪
authors: "S. Saharsh, Pushpak Jagtap"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25452v2"
tags: ["query:multi-robot"]
score: 8.0
evidence: 异构多智能体系统编队跟踪
tldr: 针对异构多智能体系统非线性动力学下的编队跟踪问题，本文提出一种基于控制障碍函数(CBF)的实时控制框架。该方法将编队跟踪任务转化为单个CBF约束，嵌入二次优化中求解，仅利用邻居相对信息而无需手动调参或标称控制器。仿真表明领导者-跟随者编队能稳定跟踪移动参考。此框架简化了设计复杂度，适用于分布式实时控制场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25452v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25452v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 851, \"height\": 798, \"label\": \"Figure\"}]"
motivation: 现有编队控制方法依赖精确模型或手动调参，难以处理异构非线性多智能体系统，需设计无需标称控制器的实时框架。
method: 将编队跟踪要求建模为单一控制障碍函数约束，融入二次规划优化，利用邻居相对信息求解分布式控制输入。
result: 领导者-跟随者框架仿真验证了移动编队跟踪的有效性，无需手动参数调整或额外控制器。
conclusion: 提出的CBF-only编队跟踪方法简化了控制器设计，提升了实时性与分布式适用性。
---

## 摘要
本文提出了一种用于具有非线性动力学的异构多智能体系统编队跟踪的实时控制框架。所提方法在解决编队跟踪的二次优化设置中，制定了一个类似控制屏障函数的单一约束。该控制器依赖于相邻智能体的相对信息，设计为无需手动参数调整或单独的名义编队控制器即可运行。通过移动编队仿真验证了领导者-跟随者框架。

## Abstract
This paper presents a real-time control framework for formation tracking of heterogeneous multi-agent systems with non-linear dynamics. The proposed method formulates a single Control Barrier Function-like constraint within a quadratic optimization setting that addresses formation tracking. Relying on the relative information of neighboring agents, the controller is designed to operate without the need for manual parameter tuning or a separate nominal formation controller. The leader-follower framework is validated through simulations of moving formations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有编队控制方法（如基于Lyapunov或标称控制器+QP）在处理异构非线性多智能体系统时，通常需要手动调参、依赖精确模型、或存在标称控制器与实时约束冲突导致不可行的问题。亟需一种无需标称控制器、仅利用邻居相对信息、且能在实时QP中统一处理编队跟踪的分布式控制框架。
- **整体含义**：提出了一种**仅基于控制屏障函数（CBF）的编队跟踪方法**，将编队跟踪任务编码为单个CBF约束，嵌入二次规划中求解，同时保证安全集的前向不变性和目标集的有限时间可达性。该方法适用于领导者-跟随者架构下的异构非线性多智能体系统。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：定义一种**可达性控制屏障函数（reach-CBF）**，它不仅保证安全集的正不变性，还能在有限时间内驱动系统进入靶集。将编队跟踪的期望位置偏差转化为一个关于速度方向与期望方向之间夹角的reach-CBF约束，并结合速度跟踪的标称控制器，构成单个CBF-QP优化问题。
- **关键技术细节**：
    - 系统模型：每个智能体为平面机器人，动力学为 `˙p_i = v_i, ˙v_i = f_{vi} + h_{vi} u_i`。
    - 通信图：无向图，领导者-跟随者架构，假设图是**无穷小轴承刚性的**（`det(B_{ff}) ≠ 0`），保证编队唯一可解。
    - 子问题分解：每个跟随者利用至少两个参考节点（邻居）的方位信息，构建**轴承引导目标点 `p_{iT}`**，即期望位置与当前位置的差向量。
    - Reach-CBF定义：`b_i(ˆv_i, ˆp_{iT}) = ⟨ˆv_i, ˆp_{iT}⟩ - cos(θ_i°)`，安全集 `X_i = {b_i ≥ 0}`，靶集 `R_i = {b_i ≥ μ_i}`。
    - 控制律：每个跟随者求解QP `min ½∥q - u_{iv}∥²`，约束为 `K_f + K_h^T q ≥ μ_i/τ_i`，其中 `u_{iv}` 是速度跟踪的标称控制器（基于Lyapunov设计），确保速度大小匹配领导者速度。
    - 定理3证明：结合CBF约束确保方向对齐，标称控制器确保位置误差渐近稳定，最终实现编队跟踪。
- **算法流程**：
    1. 每个跟随者接收邻居的相对位置和速度，计算轴承约束和目标点。
    2. 构建reach-CBF约束（线性不等式）。
    3. 求解最小二乘QP，得到控制输入 `u_i`。
    4. 执行控制，重复步骤1-3。

### 3. 实验设计

- **场景**：2D平面，**2个领导者 + 6个跟随者**，共8个智能体。领导者沿预定直线轨迹移动（速度 `v_l = [1,1]^T`），跟随者需保持一个由12条边定义的期望轴承构型。
- **智能体异构性**：跟随者3、6、8为**双积分器**（`f=0, h=I₂`）；跟随者4、5、7为**差速驱动**（`h=[cosδ -β sinδ; sinδ β cosδ]`，`β = ∥v∥`）。
- **Benchmark与对比方法**：**无**。论文仅展示单一案例结果，未与其他方法（如CLF-CBF、基于Lyapunov的方法）进行对比。
- **初始条件**：给出各智能体的初始位置、速度、朝向，参数 `γ_i = 1°`，时间步长未明确。
- **实验结果**：运行30秒后，轴承误差 `ϵ_i` 约为 `-4.6e-5` 到 `1.87e-4`（余弦相似度和），图2展示了轨迹和最终编队形状。

### 4. 资源与算力

- **文中未提及**任何GPU型号、数量、训练时长、计算平台等信息。实验仅通过仿真完成，未提供运行时间或硬件规格。

### 5. 实验数量与充分性

- **实验数量**：仅**1组仿真**，未设置不同编队尺寸、不同拓扑、不同噪声水平、不同领导者速度等变量。
- **消融实验**：**无**。未分析各组件（如CBF约束 vs. 标称控制器单独作用）的贡献。
- **公平性与客观性**：由于缺乏对比方法，无法评估相对性能提升。结果仅展示误差小，但未说明在接近奇点（如反平行）或较大初始误差时的鲁棒性。实验**不够充分**，难以支撑结论的普适性。

### 6. 论文的主要结论与发现

- 所提出的**单一CBF约束**框架能够有效实现异构多智能体系统的编队跟踪，无需单独的名义编队控制器或手动参数调整。
- 领导者-跟随者架构下的仿真验证了方法能处理双积分器和差速驱动等异构动力学，最终轴承误差在 `10^{-4}` 量级，编队收敛。

### 7. 优点

- **设计简洁**：用一个CBF约束统一了编队跟踪和安全（方向一致）目标，避免了传统方法中多目标优化的复杂度。
- **分布式与低计算量**：每个智能体仅使用本地邻居的相对信息解算一个小型QP（2维输入），适合实时嵌入式系统。
- **无参数调优需求**：除预设的误差容限 `γ_i` 和时间参数 `τ_i` 外，无需手动调整标称控制器增益（文中提供了增益条件）。
- **适用于GPS拒止环境**：仅依赖相对轴承和位置，可从摄像头、LiDAR等本地传感器获取。

### 8. 不足与局限

- **实验验证极其薄弱**：仅有一个仿真场景，未与任何现有方法（如CLF-CBF、基于轴承拉普拉斯矩阵的方法）对比，也未进行统计性分析（多次随机初始条件）。
- **假设较强**：需要图是无穷小轴承刚性（至少2个非平行参考节点），且假设轴承引导目标不反平行于速度（测度零的情况被排除），实际中可能因扰动而违反。
- **未考虑碰撞避免**：CBF只保证方向一致（任务安全），未纳入障碍物或智能体间的碰撞约束。
- **标称控制器增益依赖手动选择**：虽然给出条件（`κ_{i1}>1, κ_{i2}>(1+κ_{i1})/2`），但数值需人为设定，未说明自适应策略。
- **收敛性证明仅对局部渐近稳定**：Lyapunov分析基于理想情况（未考虑CBF约束可能带来额外偏差），实际稳定性需进一步严格证明。
- **可扩展性未知**：未测试大规模系统（>100个智能体）或更复杂的通信拓扑。

（完）
