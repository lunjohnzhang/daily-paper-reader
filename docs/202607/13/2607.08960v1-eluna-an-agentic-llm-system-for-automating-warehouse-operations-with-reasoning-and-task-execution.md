---
title: "Eluna: An Agentic LLM System for Automating Warehouse Operations with Reasoning and Task Execution"
title_zh: Eluna：一种用于自动化仓库操作的智能LLM系统，具备推理与任务执行能力
authors: "Ning Liu, Kalle Kujanpää, Zhaoxuan Zhu, P Aditya Sreekar, Kaiwen Liu, Chuanneng Sun, Jorge Marchena Menendez, Matthew Bales, Tianyu Yang, Shahnawaz Alam, Rose Yu, Baoyuan Liu, Kristina Klinkner, Shervin Malmasi"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08960v1"
tags: ["query:multi-robot"]
score: 8.0
evidence: Eluna是一个用于自动化仓储操作的多智能体LLM系统，基于标准操作程序引导
tldr: "仓库操作标准流程（SOP）复杂且需严格合规，但LLM代理在上下文过载时难以可靠执行。Eluna是一个生产部署的图引导多代理系统，将SOP编码为有向无环图并渐进式展示，并行子代理独立执行。采用非对称情节蒸馏，通过教师模型积累错误记忆后蒸馏至小模型，内化修正而不增加推理开销。在13任务基准和两个实际应用中，微调模型超越大型基线，票据处理专家一致性达94%，验证了方法的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08960v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1630, \"height\": 970, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08960v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 628, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08960v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 810, \"height\": 215, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08960v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 807, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08960v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 415, \"label\": \"Table\"}]"
motivation: 现有LLM代理缺乏强制执行SOP程序合规的机制，且完整SOP规范导致上下文过载，无法满足仓库操作的可靠性与延迟要求。
method: 提出Eluna：图引导多代理框架，将SOP编码为有向无环图并渐进披露，委派任务给并行子代理；使用非对称情节蒸馏，通过教师错误记忆优化小模型，去除推理时开销。
result: "在13任务基准和两个生产应用中，微调模型匹配或超越教师，超越所有更大基线，票据处理达到94%专家一致性。"
conclusion: Eluna通过图引导与蒸馏方法实现生产级可靠SOP执行，平衡了精确性与低延迟，适用于复杂自主决策场景。
---

## 摘要
仓库操作由标准操作程序（SOP）管理，这些程序编码了复杂的多系统决策逻辑，必须在严格的时间限制下可靠执行，然而LLM代理缺乏强制执行程序合规性的机制，并且在完整SOP规范带来的上下文过载下性能下降。我们提出了Eluna，一个已部署到生产环境的代理系统，用于可靠执行SOP。Eluna是一个图引导的多代理框架，将SOP编码为具有渐进式揭示的有向无环图，并将独立任务委派给并行子代理，每个子代理具有持久代码执行和实时数据访问。为了满足生产延迟和准确性需求，我们使用非对称情景式蒸馏，其中强教师通过情景错误记忆进行改进，然后在去除记忆的纠正轨迹上微调较小的学生，内化纠正而不增加推理时间开销。在13项任务基准和两个生产应用中，我们的微调模型匹配或超过其教师，击败所有更大的现成基线，并在工单处理应用中达到94%的专家一致性。

## Abstract
Warehouse operations are governed by Standard Operating Procedures (SOPs) that encode complex, multi-system decision logic, which must be executed reliably under strict time constraints, yet LLM agents lack mechanisms to enforce procedural compliance and degrade under the context overload full SOP specifications introduce. We present Eluna, a production-deployed agentic system for reliable SOP execution. Eluna is a graph-guided, multi-agent framework that encodes SOPs as directed acyclic graphs with progressive disclosure and delegates independent tasks to parallel sub-agents, each with persistent code execution and live data access. To meet production latency and accuracy needs, we use asymmetric episodic distillation where a strong teacher is improved through episodic error memories, then a smaller student is fine-tuned on the corrected trajectories with memory stripped, internalizing corrections without inference-time overhead. On a 13-task benchmark and two production applications, our fine-tuned models match or exceed their teacher, beat all larger off-the-shelf baselines, and reach 94% expert agreement on the ticket processing application.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：仓库操作依赖复杂多系统的标准操作程序（SOP），需要精确、可靠、低延迟地执行，但现有LLM代理在完整SOP规范下会因上下文过载而退化，且缺乏强制执行程序合规性的机制。例如，手动诊断速度慢、易出错，直接降低吞吐量。
- **整体含义**：Eluna旨在通过图引导多代理框架将SOP编码为有向无环图（DAG），并结合非对称蒸馏训练，实现生产环境下的可靠、低延迟SOP自动化执行，解决精确性与效率之间的矛盾。

---

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将SOP转化为结构化DAG，通过渐进披露减少上下文过载；使用层次化多代理架构（主代理+并行子代理）实现并行执行与上下文隔离；通过非对称情景式蒸馏将教师模型的情景记忆内化到小模型中，消除推理时记忆依赖。
- **关键技术细节**：
  - **图结构化SOP表示**：DAG包含5类节点（观察、根因、计算、约束、动作），边编码依赖关系（单节点边/多节点边）。渐进披露：只返回可达子图，节点规格按需获取。
  - **层次化多代理**：主代理持有子图并协调遍历，子代理在独立上下文（仅节点规格+工具）中并行评估，只返回结构化结论，减少主代理上下文增长；子代理不能被递归调用。
  - **工具生态**：持久代码解释器（CodeAct）、MCP协议实时数据访问、待办列表（TodoList）、代理RAG、持久化工作记忆（去重）。
  - **技能封装**：每个用例打包为独立Skill（DAG、工具、节点规格、CodeAct函数、执行指令），运行时加载，保持框架通用。
  - **非对称情景蒸馏**：
    1. 教师（GLM-4.7 355B）在框架内生成轨迹，通过情景学习（Episodic Learning）迭代修正错误（不更新权重），积累情景记忆。
    2. 对正确轨迹（经拒绝采样+LLM评判）进行逐轮分解（每步作为训练样本，去除先前推理仅保留历史+工具输出）。
    3. 小模型学生（Qwen3-32B / GLM-4.5-Air）在去除情景记忆的轨迹上微调（LoRA），内化修正，推理时无记忆开销。
  - **无公式/算法流程**：文中未给出显式公式，流程为：教师生成轨迹 → 情景记忆修正 → 拒绝采样筛选 → 轨迹分解 → SFT微调学生。

---

### 3. 实验设计：使用的数据集/场景、基准、对比方法

- **数据集与场景**：
  1. **13任务操作推理基准**（Operational Benchmark）：覆盖仓库操作所需能力（指标读取、状态分类、因果链分析、查询理解等），共1400个评估样本。
  2. **机器人搬运应用**（Robotic Conveyance）：63节点SOP，端到端执行，1500个真实场景，评价指标为二进制正确性（每节点）和精确匹配（所有节点）。
  3. **工单处理应用**（Ticket Processing）：46节点库存合并SOP，410个真实工单，专家审查评估正确率。
- **基准（Baseline）**：GLM-4.5-Air（106B，满足生产延迟SLA）作为参考基准，报告百分比改进。
- **对比方法**：
  - 现成模型：GLM-4.7（355B，教师）、Qwen3-235B-A22B-Thinking、Qwen3-32B。
  - 微调模型：Eluna-Q（Qwen3-32B）、Eluna-G（GLM-4.5-Air）、Eluna-G-XL（GLM-4.7 355B，仅用于工单处理）。

---

### 4. 资源与算力

- **训练资源**（来自附录D）：
  - Qwen3-32B微调：LoRA rank/alpha=256、lr 2e-4、64块H100 GPU、batch size 64、2 epochs。
  - GLM-4.5-Air微调：LoRA rank/alpha=32、lr 1e-4、128块H100 GPU、batch size 64、2 epochs。
  - Eluna-G-XL（355B）：LoRA rank/alpha=64、lr 5e-5、128块H100 GPU、batch size 64、1 epoch。
- **推理资源**：
  - Eluna-G/Q用vLLM部署在8块H100 GPU。
  - Eluna-G-XL用SGLang部署在8块H200 GPU。
- **训练数据**：8525个训练样本（由轨迹逐轮分解扩展得到）。

---

### 5. 实验数量与充分性

- **实验数量**：涵盖三大类（基准+两个生产应用），其中基准包含13个子任务；机器人搬运应用报告4次运行平均结果；工单处理基于410个真实工单；消融实验对比4种情景学习变体（Tasks 5&10）。
- **充分性**：
  - **正面**：评估维度多（二进制正确性、精确匹配、延迟、专家一致性）；进行了消融（情景学习贡献分析）；使用真实数据并有时空分割；自动评判（Claude Sonnet 4）和专家审查双重验证。
  - **局限性**：仅两个生产场景，泛化性证据不足；基准中部分任务提升较小（如Task 12 Eluna-Q为-3.4%）；消融仅针对两个任务，可能不全面；未评估不同微调规模或蒸馏策略影响。

---

### 6. 论文的主要结论与发现

- **主要结论**：
  1. 图引导框架+非对称蒸馏能够使小模型（32B）在SOP执行上匹配甚至超越大教师（355B），并胜过所有现成模型。
  2. 情景记忆对训练有效，但仅用于教师，学生需内化到权重中，否则推理时移除记忆会导致灾难性退化（-52.7%/-40.0%）。
  3. 生产部署中，机器人搬运应用减少54%延迟并提升SOP一致性；工单处理达到94.4%估计正确率，合并决策F1=98.0%。
  4. 残差错误主要源于SOP规范缺陷和数据质量问题，而非代理推理。
- **发现**：蒸馏胜过规模，非对称设计消除推理时记忆依赖是低延迟的关键。

---

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 渐进披露+子代理隔离有效解决上下文过载。
  - 非对称情景蒸馏创新：教师使用记忆改进，学生通过微调内化修正，无推理时开销。
  - 技能封装使框架通用，新用例只需编写Skill，无需修改执行引擎。
  - 使用真实生产系统（亚马逊）部署，提供实际业务影响数据。
- **实验亮点**：
  - 同时覆盖孤立任务基准和端到端生产应用，评价指标全面（正确性、精确匹配、延迟、专家一致性）。
  - 消融实验清晰证明情景记忆的正确使用方式（仅教师，不对称）。
  - 与多个现成大模型对比，且微调后在延迟约束下达到更好性能。

---

### 8. 不足与局限

- **应用限制**：仅部署在机器人搬运和工单处理两个领域，泛化到其他操作环境需验证；假设有强大教师和充足计算资源，不利于资源受限场景。
- **数据依赖**：情景学习和微调均依赖地面实况标签，人工标注昂贵耗时，减少依赖是未来方向。
- **实验覆盖**：消融仅两个任务（Tasks 5&10），未在全基准上验证；未对比不同蒸馏策略（如离线蒸馏 vs 在线蒸馏）；未报告统计显著性（p值或置信区间）。
- **评估偏差**：自动评判使用单一LLM（Claude Sonnet 4）可能存在偏好；工单处理专家评估样本量（410）中等；机器人搬运应用仅4次运行平均，波动未报告。
- **技术局限**：框架目前依赖预定义的DAG，无法动态发现新流程；情景记忆的维护和合并策略未充分讨论。

（完）
