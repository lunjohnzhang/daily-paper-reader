---
title: Finite-Sample Conformal Coverage Recovery via Fusion under Degraded Local Guarantees in Occupancy Map Estimation
title_zh: 占用地图估计中局部保证退化下的有限样本共形覆盖恢复通过融合
authors: "Ritvik Mahajan, Aneesh Raghavan, Karl Henrik Johansson"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14906v1"
tags: ["query:multi-robot"]
score: 8.0
evidence: 多机器人环境表示中的占用图估计
tldr: 针对多机器人占用图估计中局部保形预测的覆盖保证因轨迹非交换性和观测不均匀而退化的问题，提出分布式融合算法恢复有限样本覆盖保证。机器人仅交换标量e值，利用每邻域误覆盖预算和不确定性衰减融合算子，证明融合地图可达用户指定覆盖水平，不受通信拓扑和噪声分布影响。模拟显示融合预测器满足理论界，且密集拓扑提升效率，但证据不足时地图返回模糊分类。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14906v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 590, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 300, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 858, \"height\": 543, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 586, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 791, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1823, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 873, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 885, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14906v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 684, \"height\": 397, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14906v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 823, \"height\": 303, \"label\": \"Table\"}]"
motivation: 每个机器人局部保形预测的覆盖保证因轨迹相关性和非均匀观测而退化，缺乏有限样本可靠性。
method: 提出分布式融合算法，机器人交换标量e值，接收者基于每邻域误覆盖预算和不确定性衰减融合算子融合，恢复团队覆盖。
result: 融合集合值地图理论上恢复指定覆盖水平，仿真验证满足覆盖界，密集通信拓扑可缩小未分类区域。
conclusion: 该方法在局部保证退化时仍能恢复团队覆盖，但证据不足时地图返回模糊分类，需进一步优化。
---

## 摘要
准确可靠的环境映射是多机器人自主性的基本要求。虽然如高斯过程占用映射（GPOM）等连续映射技术提供了丰富的空间相关性和不确定性估计，但它们缺乏对其预测可靠性的形式上有限样本保证。共形预测可以为每个机器人的局部地图提供无分布覆盖保证，但这种局部保证在实践中会退化：沿机器人轨迹的时间相关性破坏了共形校准所依赖的可交换性，且每个机器人仅观察到环境中空间受限、非均匀的部分。鉴于这些退化的单智能体保证，我们开发了一种分布式融合算法，以恢复整个团队所需的覆盖。机器人仅与邻居交换轻量级标量e值，接收者使用每邻域误覆盖率预算和不确定性衰减的融合算子来融合它们。我们证明了无论通信图拓扑或底层传感器噪声分布如何，融合的集合值地图都能恢复用户指定的目标覆盖水平。然而，一个缺点是，在融合证据不足的地方，地图拒绝做出承诺并返回两个标签（空闲和占用），导致域中很大一部分未被分类，而不是阈值化为单一决策。模拟的多智能体映射实验表明，融合预测器可靠地满足其理论覆盖界限，并说明更密集的通信拓扑通过缩小未分类部分显著提高了地图效率。

## Abstract
Accurate and reliable environmental mapping is a fundamental requirement for multi-robot autonomy. While continuous mapping techniques like Gaussian Process Occupancy Mapping (GPOM) provide rich spatial correlation and uncertainty estimates, they lack formal, finite-sample guarantees on their predictive reliability. Conformal prediction can equip each robot's local map with a distribution-free coverage guarantee, but this local guarantee degrades in practice: temporal correlation along a robot's trajectory breaks the exchangeability on which conformal calibration relies, and each robot observes only a spatially limited, non-uniform portion of the environment. Taking these degraded per-agent guarantees as given, we develop a distributed fusion algorithm that recovers the desired coverage across the team. Robots exchange only lightweight scalar e-values with their neighbors, and a receiver fuses them using a per-neighborhood miscoverage budget and an uncertainty-attenuated fusion operator. We prove that the fused set-valued map recovers the target user-specified coverage level regardless of the communication graph topology or the underlying sensor noise distribution. However, a drawback is that wherever the fused evidence is insufficient, the map declines to commit and returns both labels (free and occupied), leaving a significant fraction of the domain unclassified rather than thresholded into a single decision. Simulated multi-agent mapping experiments demonstrate that the fused predictor reliably meets its theoretical coverage bounds, and illustrate that denser communication topologies significantly enhance map efficiency by shrinking this unclassified fraction.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：多机器人自主导航依赖准确的环境占用地图（Occupancy Map）。现有连续映射方法（如高斯过程占用映射 GPOM）能提供空间相关性和不确定性估计，但缺乏**形式上有限样本的可靠性保证**。  
- **关键挑战**：共形预测（Conformal Prediction）可为每个机器人的局部地图提供无分布覆盖保证，但该保证在实际中严重退化：  
  - 机器人沿轨迹采样存在**时间相关性**，破坏了共形预测对数据可交换性的要求；  
  - 每个机器人仅观测**空间受限且非均匀**的环境区域，局部覆盖可能远低于目标水平。  
- **核心目标**：在单个机器人局部保证退化的前提下，通过分布式融合恢复整个团队的目标覆盖水平（例如 1−α），且**不交换原始传感器数据**。

## 2. 方法论
### 核心思想
- 将多机器人占用映射建模为一个**覆盖恢复问题（Problem 1）**：每个机器人基于本地数据训练 GP、计算共形 p 值，并通过 e 值框架将其转化为具有有限样本保证的局部预测集。由于局部保证退化（误覆盖率可能超过目标 α），利用**轻量级 e 值广播和加权平均融合**，在接收者处恢复全局覆盖。
- **关键技术细节**：
  1. **本地校准与重新校准**（Algorithm 1 Phase 1）：  
     - 每个机器人 i 对时间块 k 内的数据拟合 GP（Matérn-32 核），计算标准化残差 nonconformity 分数。  
     - 根据每邻域预算 β_i = α/d（d 为邻居数+自身），在保留集上重新校准内层阈值 a_ik，使得本地误覆盖率 ϵ_ik ≤ β_i。
  2. **本地 e 值计算与广播**（Phase 2）：  
     - 对于共享查询集 Q_k 上的每个点，机器人 i 计算 e 值：  
       e_{ik} = g_{ik} · 1{p_{ik} ≤ a_ik}/ϵ_ik · 1{距离门控}  
       其中 g_{ik} = exp(−λ σ̂_{ik}) 为不确定性衰减因子（λ≥0），距离门控确保机器人只在其观测区域内贡献。  
     - 只广播标量 e 值（每个查询点两个标签各一个），不共享原始数据、GP 模型或校准分数。
  3. **融合**（Phase 3）：  
     - 接收者将邻居和自己的 e 值按均匀权重平均得到 fused e 值；  
     - 转换为 fused p 值 p_fused = min(1, 1/e_fused)，并将 p_fused > α 的标签保留在预测集中。
- **理论保证**（Theorem 4.1）：在局部覆盖假设（Assumption 3）下，融合预测集对任意通信图拓扑和传感器噪声分布均满足 P(Y_{⋆}∉Γ_fused) ≤ α。  
- **不确定性衰减恢复**（Proposition 2）：当局部保证不严格成立时，通过设置 λ ≥ μ_j（μ_j 是条件误覆盖率的增长指数），引入衰减因子可恢复 e 值的期望 ≤1，进而恢复覆盖保证。

## 3. 实验设计
- **数据集/场景**：  
  - 2D 矩形工作空间 [0,30]m × [0,20]m，内有走廊和障碍物。  
  - 5 个地面机器人，各配备 180° 平面激光雷达（量程 5m），传感器和位姿噪声设定具体（σ_r=2e-2m, σ_φ=3e-3rad 等）。  
  - 机器人轨迹如图 4 所示，部分重叠覆盖工作空间的不同子区域（每机器人覆盖约 52-62%）。  
- **基准（Benchmark）**：  
  - 将本文的融合算法应用于**环状拓扑**（每机器人度 di=3）和**网状拓扑**（完全图，di=5）。  
  - 对比指标：覆盖率（1 - miscoverage）、分类分数（singleton set 比例）、未分类分数（不确定或未观测）、精度等。  
  - 无外部 baseline（如 OGM、IGPOM）的直接对比，而是通过理论界验证和拓扑间对比展示方法有效性。
- **对比方法**：本文主要对比不同通信拓扑下的融合性能，未与其他多机器人地图融合方法（如分布式 GP、GMM 等）进行定量比较。

## 4. 资源与算力
- **文中未明确说明使用的 GPU 型号、数量或训练时长**。  
- 仅提到实现基于 Python、GPyTorch（用于 GP 训练）和 NumPy，每个机器人每时间块训练一次 ExactGP（最多 1.2×10^4 个点），在校准和 e 值计算中向量化。  
- 从计算量看属于轻量级，但未提供具体训练时间或 GPU 资源细节。

## 5. 实验数量与充分性
- **实验组数**：  
  - 固定 α=0.2 下比较环状和网状拓扑（每个拓扑报告 5 个机器人的统计和均值）。  
  - 扫过 α ∈ [0.05, 0.10, …, 0.40] 观察覆盖率随名义水平的变化。  
  - 扫过衰减因子 λ（0 到 4）展示有效性-效率权衡。  
  - 无消融实验（如不同门控半径、不同 GP 核、不同预算安全因子 s 的敏感性）。  
- **充分性评价**：  
  - **优点**：覆盖了主要参数（α、λ、拓扑）的验证，且结果与理论一致（覆盖率始终高于名义水平），实验设计合理。  
  - **不足**：  
    - 仅使用一个平面 2D 仿真环境，缺乏真实世界数据或更复杂 3D 场景的验证。  
    - 未与现有的多机器人融合基线方法（如分布式 GPOM、GMM 融合）直接比较，性能评估仅基于自身指标。  
    - 未展示时间块长度 T 或局部平稳性 δ_T 变化下的鲁棒性。  
    - 未对算法中每个组件的贡献进行消融（如无衰减、无门控、无预算等）。

## 6. 主要结论与发现
1. **融合算法满足理论覆盖界**：在所有实验 α 和拓扑下，实测覆盖率均高于名义水平 1−α，且大多接近 0.98 以上，远超 0.8。  
2. **通信拓扑显著影响效率**：  
   - 网状拓扑的分类分数为 84.8%，未观测分数仅 1.7%；环状拓扑分类分数 43.6%，未观测分数 17.4%。  
   - 预算 β_i=α/d_i 让单个活跃机器人即可决定其观测区域内的查询，因此更密集的通信扩大了有效覆盖。  
3. **衰减因子 λ 调节权衡**：增大 λ 使覆盖率趋于 1，但降低分类分数（集合增大），提供了一个有效性-效率的拨盘。  
4. **主要局限**：保证是边际的（非条件覆盖）；依赖局部平稳性和时间混合假设；未分类区域在证据不足时出现。

## 7. 优点
- **理论严谨性**：提供有限样本覆盖保证，不依赖数据可交换性或特定噪声分布，且保证对任意通信图拓扑成立。  
- **通信高效**：仅交换标量 e 值，无需共享原始点云、GP 模型或校准分数，通信量 O(|Q_k|) 且稀疏。  
- **融合操作简单**：加权平均，且权重固定（不依赖查询点），保证了 e 值的期望性质。  
- **可扩展性**：各机器人独立训练，融合阶段只需单轮广播，适合分布式部署。  
- **有效性与效率可调**：λ 提供 knob，用户可根据安全要求在保证和地图清晰度之间权衡。

## 8. 不足与局限
- **保证的边际性**：覆盖保证是在测试点和校准数据分布上的平均值，而非对每个查询位置的**条件覆盖**，在关键区域可能不满足安全要求。  
- **假设依赖**：局部平稳性（Assumption 1）和 β-混合（Assumption 2）在高度动态环境或剧烈运动下可能不成立，需仔细调整块长度 T 或引入保守安全裕度。  
- **通信成本随查询集增长**：尽管 e 值轻量，但查询集 |Q_k| 在 3D 大场景下可能很大，仍构成通信瓶颈。  
- **实验覆盖不足**：仅在单一 2D 仿真环境中测试，未在真实机器人平台或更高维度（3D）中验证；未与主流多机器人映射方法（如分布式 GP、GMM 融合）对比。  
- **参数敏感性未充分探索**：门控半径 r、预算安全因子 s、块长度 T、衰减因子 λ 等参数的选择及其鲁棒性未系统分析。  
- **结果解读**：覆盖率始终远高于名义水平（如 0.98 vs 0.8），暗示方法可能过于保守，对实际应用中地图可用性（分类比例）的牺牲程度未深入讨论。

（完）
