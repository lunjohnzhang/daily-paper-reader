---
title: "MoPe: Motion Permanence for Robust Monocular Gaussian Mapping in Dynamic Environments"
title_zh: MoPe：动态环境中基于运动持久性的鲁棒单目高斯映射
authors: Qixin Xiao
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29237v1"
tags: ["query:multi-robot"]
score: 8.0
evidence: 动态环境中的机器人世界模型
tldr: 动态环境中单目高斯映射常因逐帧独立判断动态性而吸收动态内容产生鬼影。MoPe引入运动持久性原则，通过SE(3)变形传播历史动态后验并与当前帧证据贝叶斯融合。在Wild-SLAM、Bonn和TUM序列上，MoPe提升跟踪鲁棒性并减少残留鬼影，尤其在动态人类场景中效果显著。该工作表明在场景表示中维持时间动态状态是实现可靠自主性的实用步骤。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29237v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1813, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29237v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29237v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1824, \"height\": 612, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29237v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 720, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29237v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 925, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29237v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 722, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29237v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 931, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29237v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 902, \"height\": 154, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29237v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1875, \"height\": 671, \"label\": \"Table\"}]"
motivation: 现有方法将动态性视为瞬时外观属性，缺乏时间记忆导致鬼影。
method: MoPe通过几何一致SE(3)变形传播历史动态后验并与当前帧有界贝叶斯更新融合。
result: 在Wild-SLAM等数据集上，MoPe显著提升跟踪鲁棒性并减少动态人类场景鬼影。
conclusion: 维持场景表示中的时间动态状态是迈向可靠表示中心自主性的实用步骤。
---

## 摘要
鲁棒的机器人自主性依赖于在动态环境中足够稳定以支持定位、导航和下游决策的场景表示。单目高斯溅射SLAM提供了高保真度的映射，但当前的不确定性感知方法仍将动态区域大致视为逐帧观测。这使得表示实际上是“无记忆”的：当行人减速、暂停或因遮挡后重新出现时，当前帧可能看起来是静态的，从而使动态内容被吸收到地图中，留下持久的鬼影伪影。我们认为，这种失败反映了表示层面的不匹配。动态性并非瞬时外观属性，而是由运动历史定义的时态属性。基于此观点，我们引入了“运动持久性”原则：物体的动态身份应随时间持续存在，而非独立地从每一帧重新决定。我们在MoPe中实现了这一原则，这是一种用于单目高斯映射的记忆感知不确定性滤波器。MoPe通过几何一致的SE(3)扭曲传播历史动态后验，并使用有界贝叶斯对数几率更新将其与当前帧证据融合。由此产生的持久后验指导跟踪、映射、动态感知高斯插入以及高斯级别的后清理。在Wild-SLAM、Bonn和TUM序列上，MoPe提高了跟踪鲁棒性并减少了残留鬼影，在最直接违反无记忆假设的动态人物场景中增益最强。这些结果表明，在场景表示中维护时态动态状态是在变化现实环境中迈向更可靠的以表示为中心的自主性的实际一步。

## Abstract
Robust robot autonomy depends on scene representations that remain stable enough to support localization, navigation, and downstream decision making in dynamic environments. Monocular Gaussian Splatting SLAM provides high-fidelity mapping, but current uncertainty-aware methods still treat dynamic regions largely as per-frame observations. This makes the representation effectively memoryless: when a pedestrian slows, pauses, or reappears after occlusion, the current frame may look static, allowing dynamic content to be absorbed into the map and leaving persistent ghosting artifacts. We argue that this failure reflects a representation-level mismatch. Dynamic-ness is not an instantaneous appearance property, but a temporal property defined by motion history. Building on this view, we introduce Motion Permanence: the principle that an object's dynamic identity should persist over time rather than be re-decided from each frame independently. We realize this principle in MoPe, a memory-aware uncertainty filter for monocular Gaussian mapping. MoPe propagates the historical dynamic posterior through geometry-consistent SE(3) warping and fuses it with current-frame evidence using bounded Bayesian log-odds updates. The resulting persistent posterior guides tracking, mapping, dynamic-aware Gaussian insertion, and Gaussian-level post-cleanup. On Wild-SLAM, Bonn, and TUM sequences, MoPe improves tracking robustness and reduces residual ghosting, with the strongest gains on dynamic-human scenes that most directly violate the memoryless assumption. These results show that maintaining temporal dynamic state inside the scene representation is a practical step toward more reliable representation-centric autonomy in changing real-world environments.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：在动态环境中，鲁棒的单目SLAM是实现机器人自主性的前提。现有基于3D高斯溅射（3DGS）的SLAM方法（如WildGS-SLAM）采用逐帧不确定性加权来抑制动态干扰，但其不确定性估计**无记忆**：仅依赖当前帧的瞬时外观，当动态物体（如行人）减速、暂停或遮挡后重新出现时，当前帧可能看似静态，导致动态内容被吸收进静态地图，产生持久的鬼影伪影。
- **核心问题**：现有方法将“动态性”视为**瞬时外观属性**，而实际上它是**时态属性**——由运动历史定义。这种表示层面的不匹配是失败的根本原因。
- **整体含义**：MoPe提出**运动持久性（Motion Permanence）**原则：物体的动态身份应随时间持续存在，而非每帧重新决策。该工作展示了将时态动态状态融入场景表示是迈向可靠表示中心自主性的实际步骤。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将逐帧不确定性提升为**时态传播的动态后验**，通过几何一致的SE(3)变形传播历史动态后验，并与当前帧证据通过有界贝叶斯对数几率更新融合，得到持久动态后验，用于指导跟踪、映射、高斯插入和后清理。

### 关键技术细节
1. **每帧不确定性预测**：从RGB输入预测每像素不确定性图βt(u)，转换为置信权重wt(u)和动态证据分数d_unc_t(u)。可选加弱语义先验d_sem_t(u)作为软下限。
2. **运动持久性实现**（两阶段）：
   - **跨视角传播**：通过SE(3)变形将前一帧后验映射到当前帧（利用相机运动T_{t-1→t}和深度Dt）：\(\tilde{p}_{t-1→t}(u) = W(p_{t-1}; T_{t-1→t}, D_t)(u)\)
   - **跨时间融合**：在有界对数几率空间融合传播状态与当前测量：\(L_t(u) = \text{clip}(L_{t-1→t}(u) + \gamma \text{logit}(m_t(u)), -c, c)\)，然后\(p_t(u) = \sigma(L_t(u))\)。其中m_t(u)=max(d_unc_t(u), d_sem_t(u))，γ为测量增益，c为裁剪界，σ为sigmoid。
3. **后验引导的优化与插入**：
   - 跟踪：用\(\tilde{\omega}_t(u) = (1-p_t(u)) \omega_t(u)\)降低高动态后验区域的残差权重。
   - 映射：将累积后验折回渲染目标，公式：\(L_{\text{map}} = \sum_u (L_{\text{rgb}}(u) + \lambda_d L_{\text{depth}}(u)) / \tilde{\beta}_t(u)^2 + \lambda_u L_{\text{unc}}\)
   - 高斯插入：仅当动态插入分数s_t(u)=max(p_t(u), η M_sem_t(u))低于阈值τ_ins时才插入，即**fail-closed**策略：宁可跳过也不污染地图。
4. **高斯级别后清理**：对每个高斯，在多关键帧中计算动态比率r_dyn_i，若足够高则渐进衰减其不透明度α_i ← max(ρ α_i, α_min)，避免硬删除破坏有效背景。

### 算法流程（文字说明）
- 每帧：① 从不确定性βt和语义先验得到测量mt → ② SE(3)变形前一个对数几率L_{t-1} → ③ 有界对数几率融合得当前后验pt → ④ 后验引导跟踪 → ⑤ 后验引导插入和映射 → ⑥ 若为关键帧，对持久动态高斯进行不透明度衰减。

## 3. 实验设计

### 数据集/场景
- **Wild-SLAM MoCap benchmark**（8个序列：ANYmal1/2, Ball, Crowd, Person, Racket, Stones, Table1/2, Umbrella，以及Crowd+Person平均）——用于跟踪（ATE RMSE）和新视角合成（PSNR, SSIM, LPIPS）。
- **Wild-SLAM iPhone数据集**中的`iphone_wandering`序列（缺乏真实轨迹，用背景PSNR/MAE及tail-only变体评估鬼影）。
- **Bonn动态数据集**（跟踪评估）。
- **TUM RGB-D动态基准**（跟踪评估）。

### 对比方法
- DROID-SLAM（密集SLAM基线）
- MonoGS、Splat-SLAM（高斯SLAM）
- MonST3R-SW
- **WildGS-SLAM**（主要基线，MoPe直接扩展的基础）

### 评价指标
- 跟踪：ATE RMSE（cm）
- 新视图合成：PSNR, SSIM, LPIPS
- 鬼影评估：背景PSNR, 背景MAE（包括tail-only版本）
- 效率：每帧跟踪/映射/总时间（ms）、内存（GB）

## 4. 资源与算力

- **明确提及**：运行时和内存测量在**NVIDIA RTX 4090 GPU**上进行。
- 未说明训练时长、使用的GPU数量或具体训练轮次（论文主要关注在线SLAM，无单独训练阶段；MoPe在现有骨干上添加轻量滤波模块）。
- 未提及模型参数量。

## 5. 实验数量与充分性

### 实验数量
- **跟踪性能**：在3个数据集（Wild-SLAM 10序列、Bonn、TUM）上报告ATE RMSE。
- **渲染质量**：在Wild-SLAM MoCap 10序列上逐序列报告PSNR/SSIM/LPIPS。
- **鬼影抑制**：在`iphone_wandering`上报告4个背景指标（均值PSNR/MAE、tail PSNR/MAE）。
- **消融实验**：在`iphone_wandering`上逐步添加组件（时态融合→语义先验→插入门控→不透明度衰减→完整），共5个变体。
- **超参数敏感性**：插入阈值τ_ins在[0.1, 0.9]内测试5个值。
- **效率**：对比基线和MoPe的每帧跟踪/映射时间、总时间、内存。

### 充分性评估
- **充分**：覆盖多个动态场景（室内/室外、人物/物体），对比了代表性方法，消融完整，超参数敏感性良好。
- **客观公平**：所有超参数跨序列固定，无序列级调优；基线WildGS-SLAM使用官方实现；在本文目标场景（动态人类）上增益显著，但也在某些序列（ANYmal2, Stones, Table1）上承认轻微性能下降，体现了客观性。
- **局限**：仅在单目RGB-D设置下评估（Bonn和TUM是RGB-D，但Wild-SLAM是单目RGB）；所有数据集均为实验室或受控场景，未在真实野外长时间运行测试；仅关注“间歇性静止”的动态物体，未处理高速运动、遮挡重叠等极端情况。

## 6. 主要结论与发现

1. **运动持久性原则有效**：通过在表示中维持时态动态状态，MoPe显著减少鬼影伪影，在最违反无记忆假设的动态人物场景中增益最强。
2. **跟踪鲁棒性提升**：Wild-SLAM上ATE从0.46→0.38（改善17.4%），Bonn 2.31→1.95（15.6%），TUM 1.51→1.28（15.2%）。
3. **渲染质量改善**：在大部分MoCap序列上PSNR、SSIM、LPIPS提升，尤其Person和Crowd序列。
4. **效率更优**：MoPe总时间65.3ms vs 基线69.7ms，内存5.3GB vs 5.8GB——因为fail-closed插入保持地图精简。
5. **消融验证**：每个组件（时态融合、语义先验、插入门控、不透明度衰减）都贡献最终性能，完整配置最佳。
6. **超参数稳定**：τ_ins在[0.3,0.7]范围内性能波动很小。

## 7. 优点：方法与实验设计亮点

- **问题洞察深刻**：将动态性重新定义为时态属性而非外观属性，提出了清晰的原则（运动持久性）。
- **轻量高效**：仅增加0.3ms跟踪时间，通过拒绝插入动态内容反而降低总时间和内存——自洽且高效。
- **fail-closed设计**：宁可跳过插入也不污染地图，下降低代价，符合工程鲁棒性逻辑。
- **几何-贝叶斯融合**：使用SE(3)变形保证跨视角一致性，有界对数几率避免饱和并使系统具有恢复能力。
- **消融完整**：逐步添加组件，清晰展示每个部分的贡献。
- **超参数敏感性分析**：证明方法对主要超参数不敏感。
- **承认局限性**：明确指出在连续运动或近静态杂波场景中增益有限，不夸大通用性。

## 8. 不足与局限

- **保守性代价**：在细节丰富但动态不明确的区域（如ANYmal2、Stones、Table1），fail-closed策略可能减少高斯插入密度，轻微降低PSNR/SSIM。
- **传播依赖深度**：SE(3)变形需要合理准确的深度图，不准确深度会导致传播偏差。
- **未处理连续高速运动**：更适合间歇性静止的物体；对连续快速运动物体（如奔跑的行人）可能不如专门建模运动的方法（如4DGS）。
- **实验覆盖有限**：
  - 仅使用单目RGB输入（但Bonn/TUM是RGB-D），未在纯单目长时间野外序列（如KITTI动态场景）上评估。
  - 未与基于语义掩码的方法（BDGS-SLAM）或基于对象跟踪的方法（DGS-SLAM）直接比较跟踪精度，主要对比WildGS-SLAM和传统SLAM。
  - 未报告统计显著性（如多次运行的标准差）。
- **语义先验依赖**：可选语义先验来自预训练分割模型，在开放集物体上可能失效。
- **应用限制**：适用于以静态地图为核心的机器人任务（定位、导航），若任务需要完整动态物体建模（如人机交互），则不适用。

（完）
