---
title: "Agentic Real2Sim: Physics-based World Modeling with Vision-Language Agents"
title_zh: 智能体驱动的现实到仿真：基于视觉语言代理的物理世界建模
authors: "Guanxiong Chen, Qianjun Xia, Jiawei Peng, Heng Zhang, Bole Ma, Justin Qian, Ziyi Jiao, Bingyang Zhou, Luoxin Ye, Kaifeng Zhang, Kunyi Wang, Weijia Zeng, Yunuo Chen, Pengzhi Yang, Ziqiu Zeng, Huamin Wang, Chao Liu, Alan Yuille, Fan Shi, Changxi Zheng, Yunzhu Li, Chenfanfu Jiang, Peter Yichen Chen"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19190v1"
tags: ["query:multi-robot"]
score: 8.0
evidence: 利用视觉语言代理从真实记录构建物理世界模型
tldr: 机器人物体交互的实到仿转换（Real2Sim）过程依赖手动调优和脆弱的工作流。本文提出Agentic Real2Sim框架，利用视觉-语言智能体将真实物体-机器人交互记录转化为可模拟的片段孪生体，保留几何、观测、状态和交互。在刚体操作、可变形物体和人形运动场景中验证，使用开源VLM后端即可达到与前沿模型相当的转化成功率。该工作为可扩展的物理世界建模和下游策略学习与评估迈出第一步。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19190v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1588, \"height\": 826, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19190v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1626, \"height\": 2202, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19190v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1715, \"height\": 844, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19190v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1693, \"height\": 475, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19190v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 1258, \"label\": \"Table\"}]"
motivation: 现有Real2Sim过程需要手动调优视觉模型、网格清理和坐标对齐，缺乏自动化通用框架。
method: 框架利用视觉-语言智能体，通过VLM驱动决策，将真实交互记录转化为带观测、几何和状态的可运行物理仿真。
result: 在刚体、可变形物体和人形运动场景中，使用开源VLM后端实现与前沿模型相近的转化成功率。
conclusion: Agentic Real2Sim为可扩展的物理世界建模提供了自动化方案，可用于机器人策略学习与评估。
---

## 摘要
将真实世界转换为用于机器人操作物体的仿真场景仍然需要大量人工，因为它不仅需要视觉重建：一个精简的现实到仿真（real2sim）过程必须恢复场景几何和物体状态，推断物理参数，并将执行器、物体、相机、姿态和轨迹组装成可运行的物理仿真。如今，这一过程仍依赖于视觉基础模型的手动调优、网格清理、坐标系对齐，以及跨视觉感知工具和仿真器的脆弱工作流粘合。我们提出了智能体驱动的现实到仿真（Agentic Real2Sim），一个基于视觉语言代理的通用物理世界建模框架，将真实世界中物体-机器人交互的记录转换为可仿真的情节双胞胎，保留了观测、几何、机器人交互和物体状态。我们在刚体操作、可变形物体交互和类人运动场景上评估了Agentic Real2Sim，涵盖了通常由单独的现实到仿真流水线处理的领域，这标志着向可扩展转换迈出了第一步。该框架的智能体决策可由一个开放权重的视觉语言模型后端驱动，成本仅为前沿模型的一小部分，同时达到相当的转换成功率。我们旨在将生成的真实世界对齐的双胞胎用于下游机器人任务，特别是策略学习和评估。项目网站见https://ericchen321.github.io/agentic_real2sim.github.io/。

## Abstract
Real-to-sim conversion for robotic interaction with objects remains labor-intensive because it requires more than visual reconstruction: a streamlined real2sim process must recover scene geometries and object states, infer physical parameters, and assemble actors, objects, cameras, poses, and trajectories into a runnable physical simulation. Today this process still depends on manual tuning of visual foundation models, mesh cleanup, coordinate-frame alignment, and brittle workflow glue across visual perception tools and simulators. We introduce \textit{Agentic Real2Sim}, a framework for generalized physical world modeling with vision-language agents, converting a real-world recording of object-robot interaction into a simulatable episodic twin which preserves observations, geometries, robot interactions, and object states. We evaluate Agentic Real2Sim on rigid-object manipulation, deformable-object interaction, and humanoid motion scenes, spanning domains that are usually handled by separate Real2Sim pipelines, marking a first step toward scalable conversion. The framework's agentic decisions can be driven by an open-weight VLM backend at a small fraction of the cost of frontier models, while attaining comparable conversion success rate. We aim to use the resulting real-world-aligned twins for downstream robotics tasks, specifically policy learning and evaluation. The project site is available at https://ericchen321.github.io/agentic_real2sim.github.io/.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：将真实世界的物体-机器人交互记录转换为可运行的物理仿真（Real2Sim）仍然高度依赖人工，包括手动调优视觉模型、清理网格、对齐坐标系、拼接脆弱的工作流等。现有方法要么聚焦于静态场景重建，要么需要专门的采集设备，缺乏一个通用的、自动化的转换框架。
- **背景**：随着大型机器人数据集（如DROID）和高质量仿真器（如MuJoCo）的发展，理论上可以自动将真实交互转化为仿真双胞胎，但实践中仍存在大量手动操作。作者希望填补这一空白：给定一个真实的物理交互记录，自动生成一个保留观测、几何、状态、轨迹和物理参数的、可直接用于下游策略学习与评估的仿真双胞胎。

## 2. 方法论：核心思想、关键技术细节与流程
- **核心思想**：利用**视觉-语言模型（VLM）驱动的智能体（Agent）**来协调一系列确定性视觉与仿真工具，将真实交互记录转化为格式化的**情节双胞胎**（episodic twin），形式化定义为：
  \[
  \mathcal{T} = (\mathcal{O}, \mathcal{A}, \mathcal{G}, \mathcal{S}_{1:T}, \Theta, \mathcal{B}, \mathcal{M})
  \]
  其中 $\mathcal{O}$ 为真实观测，$\mathcal{A}$ 为执行器/末端，$\mathcal{G}$ 为几何与外观资产，$\mathcal{S}_{1:T}$ 为仿真状态序列，$\Theta$ 为物理与对齐参数，$\mathcal{B}$ 为仿真后端，$\mathcal{M}$ 为度量与追踪。

- **关键技术细节**：
  - **四个智能体阶段**（以刚体操作为例）：
    1. **视觉处理代理**：读取视频 → 对象发现（VLM选择相关物体） → 关键帧选择 → 分割（SAM 3） → 几何重建（SAM 3D） → 尺度估计 → 姿态跟踪（FoundationPose） → 输出包含网格、尺度、姿态序列、机器人轨迹、相机标定等的“情节文件夹”。
    2. **物理先验推理代理**：从视觉证据推断对象身份、材料类别、质量提示等物理属性。
    3. **场景准备代理**：校准机器人基座、地面平面、相机内参外参；将资产和姿态加载到MuJoCo中；通过确定性扫描或VLM辅助循环优化物体放置。
    4. **仿真器闭环抓取优化**：对操作物体进行微小位移扫描，选出能成功抓取的位置。
  - **代理决策与确定性工具分离**：VLM仅负责需要模糊判断的高层决策（如对象相关性、掩码质量、关键帧选择等），而分割、深度估计、姿态跟踪、仿真执行等调用确定性工具。使得系统易于消融和替换VLM后端。
  - **VLM后端可互换**：在DROID-100上对比了Gemma 4 31B（开源）、Qwen 3.6 35B、GPT-5.4、Claude Haiku 4.5，仅改变VLM，其他配置一致。

- **算法流程**（文字说明）：
  - 输入：DROID格式的RGB视频、深度/立体、标定、机器人轨迹、任务描述。
  - 输出：MuJoCo仿真场景，包含机器人、操作物体、相机、物理参数、重放轨迹。
  - 步骤：
    1. 提取关键帧和对象掩码（SAM 3）。
    2. 重建每个对象的网格（SAM 3D）并估计真实尺度。
    3. 使用FoundationPose进行6D姿态追踪。
    4. 推断物理先验（材料、质量）。
    5. 校准场景（机器人基座、地面、相机）。
    6. 在MuJoCo中加载场景，运行抓取优化。
    7. 生成重放评估（VLM裁判打分）。

## 3. 实验设计
- **数据集与场景**：
  - **刚体操作**：DROID-100（从DROID数据集中随机抽取100个情节，涵盖不同物体、视角、遮挡模式和动词（pick/place/push/insert））。
  - **可变形物体**：采用PhysTwin风格的情节（绳索、布料、软包等），定性展示。
  - **人形运动**：采用BFM-Zero风格的Unitree G1运动片段，定性展示。
- **评估指标**：**重放成功（Replay Success）**—— 基于VLM裁判打分的多角度一致性评估。三位裁判（不同VLM后端）分别对比真实与仿真关键帧（start, middle_1, middle_2, end），根据目标对象身份、最终位置、动作相似性、末端位置等打分（0-10），8分及以上算“通过”（success），7分算“部分”（partial），6分及以下或缺少记录算“失败”（fail）。一个情节至少有1个裁判给出≥8分则记为成功。
- **对比方法**：主要对比不同VLM后端（Gemma 4 31B、Qwen 3.6 35B、GPT-5.4、Claude Haiku 4.5）在相同流水线下的成功率与成本。未与其他完整的Real2Sim系统（如Scalable Real2Sim、TwinAligner）进行直接定量对比，因为本文聚焦于“情节级转换”这一不同设定。

## 4. 资源与算力
- 文中未明确说明使用的GPU型号、数量、训练时长等硬件细节。
- 只报告了**模型调用成本**（美元）：Gemma 4 31B完成100个情节共花费 **$2.62**，GPT-5.4花费 **$82.30**，Claude Haiku 4.5约 $9.17，Qwen 3.6 35B约 $13.10。这些成本仅包含VLM调用费用，不包含推理硬件的显式说明。
- 由于未提供GPU资源细节，难以评估完整流程的计算开销。

## 5. 实验数量与充分性
- **数量**：
  - 刚体操作：100个DROID情节，统计了成功/部分/失败数量（图3给出四个VLM后端的详细结果）。
  - 多VLM对比：4种后端，同一流水线。
  - 可变形和人形：仅定性展示（图4），未提供定量分数。
- **充分性**：
  - **优点**：使用随机选择的100个情节，避免了偏好偏差；采用多裁判打分机制增加客观性；多VLM对比覆盖了开源和专有模型。
  - **不足**：
    - 可变形和人形缺乏定量评估，仅为概念验证。
    - 仅使用了单一数据集（DROID）进行刚体评估，未在其他机器人操作数据集（如PointWorld、Bridge Data等）上验证泛化性。
    - 没有对比其他完整的Real2Sim框架（如Scalable Real2Sim、TwinAligner），因为本文被定位为首次“情节级转换”，但不同的问题设定可能导致比较困难。
    - 没有进行消融实验（如去除某个智能体或批评环节的效果）。
    - 成功率最高仅为48%，说明仍有较大提升空间，但文中分析瓶颈在于感知和仿真组件而非VLM，这一结论主要基于VLM对比而非消融实验。

## 6. 主要结论与发现
1. **Agentic Real2Sim能够自动将DROID操作情节转换为可运行的MuJoCo仿真双胞胎**，在100个情节中取得最高48%、最低37%的重放成功率。
2. **开源31B VLM（Gemma 4 31B）可以达到甚至超过专有模型（GPT-5.4、Claude）的重放成功率**，同时成本仅为GPT-5.4的约3%（$2.62 vs $82.30），性价比极高。
3. **框架的瓶颈主要在于上游视觉感知和仿真组件的精度，而非VLM的推理能力**，因为不同VLM后端的成功率差异不大（37-48%）。
4. **相同的“情节合同”可扩展到可变形物体和人形运动**，展示了框架的通用性，但仅作为定性验证。
5. **VLM的角色被限制为高层决策（如对象发现、关键帧选择、质量批评），不直接执行几何或物理模拟**，这种分离使得系统易于替换和扩展。

## 7. 优点
- **高度自动化**：显著减少了Real2Sim过程中的手动干预，从视频直接生成仿真场景。
- **模块化设计**：智能体决策与确定性工具分离，便于调试、替换和升级单个组件。
- **VLM后端无关性**：支持多种VLM后端，且开源模型取得有竞争力的结果，降低使用成本和依赖。
- **成本效益突出**：使用Gemma 4 31B完成100个情节仅需$2.62，适合大规模数据转换。
- **跨领域泛化**：同一框架可处理刚体、可变形、人形三种不同的物理域（尽管后两者只有定性示例）。
- **评估指标有创新**：采用多VLM裁判打分，从多个维度（对象、位置、动作、末端）衡量重放一致性，减少单一VLM偏差。

## 8. 不足与局限
- **成功率仍然偏低**：最高仅48%，大量情节转换失败，实用性受限。失败原因主要来自感知误差（分割失败、姿态跟踪丢失、深度估计不准）和仿真器反馈（接触不物理等），而非VLM。
- **可变形和人形部分仅为定性展示**，缺少定量结果，无法评估框架在这些域的实际效能。
- **实验覆盖窄**：仅使用DROID数据集进行刚体评估，未在更多真实世界数据集上验证，泛化性存疑。
- **缺乏消融实验**：未严格分析每个智能体组件（如视觉处理、物理先验、抓取优化）的贡献，也未评估批评机制的效果。
- **计算资源信息不透明**：未报告GPU型号、数量、总执行时间等，影响可复现性和成本估算的完整性。
- **潜在偏差风险**：评估本身依赖VLM裁判，可能存在喜好偏差（如偏好视觉一致性而忽略物理正确性），且裁判本身也来自不同VLM，打分一致性未严格校验。
- **应用限制**：当前主要面向DROID格式的输入，对其他格式（如G-drive、ROS bag等）可能需要额外适配；且假设场景中包含机器人、姿态轨迹等明确信息，对于无标定无轨迹的纯视频转换尚未验证。
- **物理参数推断较为简单**：物理先验仅基于VLM推断类别和质量，未进行系统辨识或在线优化，可能影响重放真实性。

（完）
